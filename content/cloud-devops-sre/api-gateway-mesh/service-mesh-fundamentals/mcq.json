{
  "topicId": "service-mesh-fundamentals",
  "mcqs": [
    {
      "id": "service-mesh-fundamentals-s02-q1",
      "slideId": "service-mesh-fundamentals-s02",
      "question": "Why can't an API gateway solve the problems a service mesh addresses?",
      "options": [
        "Because gateways are always slower than sidecar proxies",
        "Because gateways can only route to a single backend service",
        "Because gateways don't support HTTPS",
        "Because the gateway only sees traffic entering from outside the system, with no visibility into or control over service-to-service (east-west) calls happening internally"
      ],
      "correctIndex": 3,
      "explanation": "A gateway's scope is the system's edge — north-south traffic from external clients. It has no view into calls happening entirely between internal services, which is exactly the east-west traffic problem a mesh is built to handle. Speed, HTTPS support, and routing to multiple backends aren't the actual limitation.",
      "difficulty": "easy",
      "level": "beginner"
    },
    {
      "id": "service-mesh-fundamentals-s04-q1",
      "slideId": "service-mesh-fundamentals-s04",
      "question": "In a service mesh, what's the key functional difference between the data plane and the control plane?",
      "options": [
        "The data plane (sidecar proxies) actually moves and secures every request; the control plane computes configuration and policy and pushes it to the proxies, without touching traffic itself",
        "The data plane stores application data; the control plane stores user credentials",
        "They are two names for the exact same component",
        "The control plane only runs during a deployment, then shuts down"
      ],
      "correctIndex": 0,
      "explanation": "The data plane is the set of proxies handling live traffic on every hop; the control plane is the separate brain that computes routing rules, certificates, and policy and distributes them to proxies, never touching request traffic directly. They are distinct, continuously-running components with different jobs, not aliases or a one-time deploy-time process.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "service-mesh-fundamentals-s05-q1",
      "slideId": "service-mesh-fundamentals-s05",
      "question": "Why does a service mesh's approach to mTLS and retries generalize well across a polyglot organization (services in Python, Go, Java, etc.)?",
      "options": [
        "Because the mesh converts all services to run in a single language",
        "Because the logic lives in the language-agnostic sidecar proxy, so every service gets the same behavior regardless of what language it's written in",
        "Because polyglot organizations don't actually need mTLS",
        "Because the mesh only works with statically-typed languages"
      ],
      "correctIndex": 1,
      "explanation": "Because the sidecar sits outside the application process and intercepts traffic at the network level, it doesn't care what language the service is written in — every service gets identical mTLS and retry behavior for free. The mesh doesn't require language conversion, isn't restricted to any type system, and doesn't reduce the actual need for mTLS.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "service-mesh-fundamentals-s06-q1",
      "slideId": "service-mesh-fundamentals-s06",
      "question": "What's the real trade-off between handling cross-cutting concerns (retries, mTLS) via a shared library versus via a service mesh?",
      "options": [
        "Shared libraries only work with a service mesh already installed",
        "There is no trade-off — a mesh is strictly better in every dimension",
        "A shared library avoids an extra network hop but must be reimplemented per language and redeployed everywhere for a fix; a mesh centralizes the fix but adds a per-call proxy hop",
        "A mesh eliminates all network latency compared to a shared library"
      ],
      "correctIndex": 2,
      "explanation": "A shared library keeps logic in-process (no extra hop) but multiplies maintenance across every language and requires every service to redeploy for a fix; a mesh centralizes the fix in the control plane at the cost of an added proxy hop per call. It's a genuine trade-off, not a strict win either way, and the two approaches aren't dependent on each other.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
