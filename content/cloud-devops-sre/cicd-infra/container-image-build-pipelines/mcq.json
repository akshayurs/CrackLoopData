{
  "topicId": "container-image-build-pipelines",
  "mcqs": [
    {
      "id": "container-image-build-pipelines-s01-q1",
      "slideId": "container-image-build-pipelines-s01",
      "question": "Why does CI-side image caching need to be wired up differently from a local developer's Docker cache?",
      "options": [
        "Because CI runners always build multi-arch images, which caching doesn't support",
        "CI systems don't support Docker's layer cache mechanism at all",
        "Local builds don't use layer caching, only CI builds do",
        "CI runners are often ephemeral, so the layer cache from a previous run must be explicitly persisted (e.g. via a registry cache or cache volume) or it's simply gone on the next run"
      ],
      "correctIndex": 3,
      "explanation": "The core difference is runner lifetime: a developer's laptop keeps its Docker cache between builds by default, but a fresh, ephemeral CI runner starts with nothing unless the pipeline explicitly restores a cache. Docker's caching works the same way in both places — it's persistence across runs that differs.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "container-image-build-pipelines-s02-q1",
      "slideId": "container-image-build-pipelines-s02",
      "question": "What's the recommended tagging approach for a CI-built image?",
      "options": [
        "Tag every build with the immutable commit SHA always, adding semantic-version or `latest` tags as additional human-friendly aliases",
        "Use only `latest` since it's simplest for developers to reference",
        "Use only semantic versions, and skip commit SHAs since they aren't human-readable",
        "Tag builds randomly since the registry deduplicates identical layers anyway"
      ],
      "correctIndex": 0,
      "explanation": "The commit SHA is what gives you a traceable, immutable reference — every build gets one. Semantic versions and `latest` are useful human-facing aliases layered on top, but using them alone loses the guarantee that a tag always points at the same bits.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "container-image-build-pipelines-s06-q1",
      "slideId": "container-image-build-pipelines-s06",
      "question": "A team's CI builds are consistently slow because every run reinstalls all OS packages and dependencies. What's the most likely root cause?",
      "options": [
        "The base image is too small to support caching",
        "The Dockerfile places a frequently-changing `COPY . .` before the dependency-install layer, or the CI system never persists the layer cache between runs, so every build starts cold",
        "Vulnerability scanning is what's slowing the build down, not caching",
        "Multi-arch builds are inherently uncacheable"
      ],
      "correctIndex": 1,
      "explanation": "Slow, uncached builds trace to instruction ordering (an early-changing layer invalidates everything after it) or a missing persisted cache between ephemeral runners — not image size, scanning, or multi-arch support, which are separate concerns.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
