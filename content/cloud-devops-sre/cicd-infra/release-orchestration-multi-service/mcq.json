{
  "topicId": "release-orchestration-multi-service",
  "mcqs": [
    {
      "id": "release-orchestration-multi-service-s02-q1",
      "slideId": "release-orchestration-multi-service-s02",
      "question": "A schema migration adds a column that a downstream service needs to read. In what order should the migration and the service deploy?",
      "options": [
        "The migration must land first, so the column exists before any deployed code tries to read it",
        "The service deploy must land first, since code changes are always safer to ship first",
        "Order doesn't matter as long as both happen within the same day",
        "The migration and the deploy must happen in the exact same second to avoid any gap"
      ],
      "correctIndex": 0,
      "explanation": "Reading a column that doesn't exist yet fails immediately, so the migration has to land first. Deploying the reading code first would be the unsafe order; simultaneous timing isn't achievable or necessary once the correct sequence is respected.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "release-orchestration-multi-service-s06-q1",
      "slideId": "release-orchestration-multi-service-s06",
      "question": "Why is 'trigger every service's pipeline at the same time' considered an anti-pattern for a multi-service release?",
      "options": [
        "It removes any explicit ordering or staging, so a dependency mistake or a slow deploy can cascade into a hard-to-diagnose outage spanning multiple services and owners",
        "It's actually the fastest and safest way to release when you have many services",
        "It only fails if the services are owned by different teams",
        "The problem is purely about CI resource contention, not correctness"
      ],
      "correctIndex": 0,
      "explanation": "The risk is correctness, not speed or resource contention: without explicit dependency ordering or staged rollout, a multi-service release has no safeguard against services landing in an order that breaks a contract between them -- and the resulting failure is harder to trace than a single-service bug.",
      "difficulty": "hard",
      "level": "advanced"
    }
  ]
}
