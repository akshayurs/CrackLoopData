{
  "topicId": "configuration-and-secrets",
  "mcqs": [
    {
      "id": "configuration-and-secrets-s02-q1",
      "slideId": "configuration-and-secrets-s02",
      "question": "An app expects its settings as an actual config file on disk, not environment variables. What's the right way to deliver a ConfigMap's data to it?",
      "options": [
        "You can't — ConfigMaps only support environment variables",
        "Mount the ConfigMap as a volume so its keys appear as files in the container",
        "Copy the ConfigMap's YAML into the image at build time",
        "Use a Secret instead, since only Secrets support file mounts"
      ],
      "correctIndex": 1,
      "explanation": "ConfigMaps support both delivery mechanisms: environment variables via envFrom, or a mounted volume where each key becomes a file. Both ConfigMaps and Secrets support mounting; baking config into the image defeats the purpose of externalizing it.",
      "difficulty": "easy",
      "level": "beginner"
    },
    {
      "id": "configuration-and-secrets-s03-q1",
      "slideId": "configuration-and-secrets-s03",
      "question": "By default, how are Secret values actually stored in etcd?",
      "options": [
        "Encrypted with a cluster-wide key automatically",
        "Base64-encoded — reversible, not encrypted, unless encryption at rest is separately configured",
        "Hashed one-way, so they can never be retrieved",
        "Stored outside etcd, in a separate encrypted vault by default"
      ],
      "correctIndex": 1,
      "explanation": "Out of the box, Kubernetes only base64-encodes Secret values before storing them in etcd — a trivially reversible encoding, not encryption. Real encryption at rest requires explicitly configuring an encryption provider for the API server; it isn't on by default, and Secrets aren't hashed or stored elsewhere automatically.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "configuration-and-secrets-s07-q1",
      "slideId": "configuration-and-secrets-s07",
      "question": "What makes the described database credential rotation zero-downtime?",
      "options": [
        "The Deployment is scaled to zero during the swap",
        "Both old and new passwords are valid on the database during a brief overlap, so Pods roll over one at a time without a moment where all Pods hold an invalid credential",
        "Kubernetes automatically re-encrypts traffic during rotation",
        "The old Secret is deleted immediately once the new one is created"
      ],
      "correctIndex": 1,
      "explanation": "The overlap window — old and new credentials both valid — is what lets a rolling restart swap Pods over gradually with no window where a live Pod holds a credential the database has already rejected. Scaling to zero would cause downtime, and deleting the old Secret immediately would break any Pod not yet rolled.",
      "difficulty": "medium",
      "level": "beginner"
    }
  ]
}
