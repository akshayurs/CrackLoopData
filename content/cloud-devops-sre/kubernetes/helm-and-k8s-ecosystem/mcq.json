{
  "topicId": "helm-and-k8s-ecosystem",
  "mcqs": [
    {
      "id": "helm-and-k8s-ecosystem-s02-q1",
      "slideId": "helm-and-k8s-ecosystem-s02",
      "question": "What does `helm rollback payments-prod 3` actually do?",
      "options": [
        "Deletes and recreates the release from scratch",
        "Reverts the entire set of manifests for that release back to revision 3's rendered state",
        "Rolls back only the Deployment resource, leaving other resources untouched",
        "Downgrades the chart's version number in the repository"
      ],
      "correctIndex": 1,
      "explanation": "Helm tracks full release history, so a rollback reapplies the complete rendered manifest set from that earlier revision — every resource the chart manages, not just one. It doesn't touch the chart repository's published versions, and it's a revert-and-reapply, not a delete-and-recreate.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "helm-and-k8s-ecosystem-s04-q1",
      "slideId": "helm-and-k8s-ecosystem-s04",
      "question": "Why can a Kustomize base directory be applied directly with `kubectl apply -f base/`, unlike a Helm chart's templates directory?",
      "options": [
        "Kustomize doesn't actually support overlays",
        "The Kustomize base is plain, valid YAML with no template placeholders, while Helm templates contain unrendered `{{ }}` syntax",
        "Helm charts are binary files that kubectl can't read",
        "kubectl has no built-in support for Kustomize"
      ],
      "correctIndex": 1,
      "explanation": "Kustomize's base is ordinary Kubernetes YAML — no templating syntax — so it's valid input to kubectl on its own; overlays only add patches on top. Helm templates contain Go template placeholders that must be rendered by the Helm engine first. kubectl also has Kustomize support built in natively via `-k`.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "helm-and-k8s-ecosystem-s08-q1",
      "slideId": "helm-and-k8s-ecosystem-s08",
      "question": "Someone runs `kubectl patch` directly against a Deployment that a Helm release manages. What's the risk the next time someone runs `helm upgrade`?",
      "options": [
        "Nothing — Helm always detects and preserves manual patches automatically",
        "The manual change can be silently reverted, since Helm's recorded release state doesn't know about it",
        "The upgrade will fail outright and refuse to proceed",
        "Helm will merge the patch permanently into the chart's template"
      ],
      "correctIndex": 1,
      "explanation": "Helm compares against its own recorded release manifest, not the live cluster state — a manual patch outside Helm isn't reflected there, so the next upgrade can overwrite it without warning. Helm doesn't auto-detect or auto-merge out-of-band changes, and it won't refuse to proceed just because of drift.",
      "difficulty": "hard",
      "level": "intermediate"
    }
  ]
}
