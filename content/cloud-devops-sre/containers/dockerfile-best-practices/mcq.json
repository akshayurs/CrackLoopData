{
  "topicId": "dockerfile-best-practices",
  "mcqs": [
    {
      "id": "dockerfile-best-practices-s02-q1",
      "slideId": "dockerfile-best-practices-s02",
      "question": "Why does a multi-stage build result in a much smaller final image than a single-stage build?",
      "options": [
        "It compresses the final image using a stronger algorithm",
        "The final stage only copies the compiled artifact from the build stage, leaving the compiler and build-time dependencies behind",
        "Multi-stage builds automatically delete unused files from the OS",
        "It uses a completely different container runtime"
      ],
      "correctIndex": 1,
      "explanation": "The size win comes from what doesn't get copied forward: the build stage's compiler, source code, and build dependencies never make it into the final stage, which only receives the finished artifact via `COPY --from=`.",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "dockerfile-best-practices-s05-q1",
      "slideId": "dockerfile-best-practices-s05",
      "question": "Why does running a container's process as a non-root user matter, given that containers are already isolated?",
      "options": [
        "It has no real security benefit since containers are isolated anyway",
        "It reduces the impact if the process is compromised or escapes its isolation, since it won't have root-level privileges",
        "It's required for the container to start at all",
        "It only affects the container's network configuration"
      ],
      "correctIndex": 1,
      "explanation": "Isolation reduces but doesn't eliminate risk — if a vulnerability lets a process break out of its container boundary, running as non-root limits what it can do next. It's a defense-in-depth measure, not redundant with container isolation.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "dockerfile-best-practices-s09-q1",
      "slideId": "dockerfile-best-practices-s09",
      "question": "A Dockerfile does `RUN echo $SECRET > key.txt` in one instruction, then `RUN rm key.txt` in a later one. Is the secret actually removed from the shipped image?",
      "options": [
        "Yes, rm permanently deletes it from every layer",
        "No — the file still exists in the earlier layer; the later layer only hides it in the merged view",
        "It depends on which base image is used",
        "Yes, but only if the image is rebuilt from scratch afterward"
      ],
      "correctIndex": 1,
      "explanation": "Each RUN instruction creates its own layer, and layers are additive history, not destructive edits. The rm only removes the file from the final merged filesystem view — the file still physically exists in the earlier layer and can be extracted from the image.",
      "difficulty": "hard",
      "level": "intermediate"
    }
  ]
}
