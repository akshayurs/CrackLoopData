{
  "topicId": "terraform-modules-and-reuse",
  "mcqs": [
    {
      "id": "terraform-modules-and-reuse-s01-q1",
      "slideId": "terraform-modules-and-reuse-s01",
      "question": "What is a Terraform module, conceptually?",
      "options": [
        "A special resource type only cloud providers can define",
        "A file format only used for storing state",
        "A synonym for a Terraform provider",
        "A reusable directory of Terraform config with defined inputs and outputs, similar to a function"
      ],
      "correctIndex": 3,
      "explanation": "A module packages reusable infrastructure logic behind explicit inputs (variables) and outputs, the same idea as a function — call it with different inputs to get differently-configured infrastructure without duplicating the underlying resource logic. It's unrelated to providers or state file format.",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "terraform-modules-and-reuse-s03-q1",
      "slideId": "terraform-modules-and-reuse-s03",
      "question": "Why do most teams prefer separate state files (or directories) per environment over Terraform workspaces for dev/stage/prod?",
      "options": [
        "Separate state provides stronger physical isolation, reducing the risk of accidentally applying against the wrong environment",
        "Workspaces aren't supported for production infrastructure",
        "Workspaces can only be used with a single provider",
        "Separate state files require no backend configuration at all"
      ],
      "correctIndex": 0,
      "explanation": "Workspaces share the same backend and code path, switched by a command — a typo or forgotten `workspace select` can mean applying against the wrong environment. Fully separate state (and often separate configs) makes that mistake structurally harder, which is why higher-stakes environments usually favor it. Workspaces work with any provider and both approaches need a backend.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "terraform-modules-and-reuse-s06-q1",
      "slideId": "terraform-modules-and-reuse-s06",
      "question": "What's the main risk of putting dev, staging, and prod resources in one giant root module?",
      "options": [
        "Terraform doesn't allow more than one environment per root module",
        "Every apply's blast radius spans all environments at once, and it's easy to accidentally plan/apply against the wrong one",
        "It makes the HCL syntax invalid",
        "It prevents you from using any modules at all"
      ],
      "correctIndex": 1,
      "explanation": "The technical risk is entirely about blast radius and accident-proofing: one shared state and one shared apply means a mistake isn't contained to a single environment. Terraform has no syntactic restriction against this structure — the problem is operational, not technical, and modules remain usable regardless.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
