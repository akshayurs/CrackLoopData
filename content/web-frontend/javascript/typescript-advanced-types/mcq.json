{
  "topicId": "typescript-advanced-types",
  "mcqs": [
    {
      "id": "typescript-advanced-types-s02-q1",
      "slideId": "typescript-advanced-types-s02",
      "question": "```typescript\ninterface User { id: number; name: string; email: string; }\ntype PublicUser = Omit<User, \"email\">;\n```\nIf `User` later gains a new field `phone: string`, what happens to `PublicUser` without touching its definition?",
      "options": [
        "`PublicUser` automatically includes `phone`, since `Omit` re-derives from the current shape of `User` every time it's referenced",
        "`PublicUser` stays exactly as originally defined and never picks up `phone`, since `Omit` is evaluated once at declaration and frozen",
        "TypeScript throws a compile error requiring `PublicUser` to be manually updated",
        "`phone` is added to `PublicUser` but marked optional automatically"
      ],
      "correctIndex": 0,
      "explanation": "`Omit<User, \"email\">` derives its shape from `User` wherever it's used — it isn't a one-time snapshot, so adding `phone` to `User` means `PublicUser` includes it automatically on the next compile, still excluding only `email`. There's no error forcing a manual update, and nothing makes the new field optional by default — it inherits whatever `phone`'s own declared optionality is.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "typescript-advanced-types-s04-q1",
      "slideId": "typescript-advanced-types-s04",
      "question": "```typescript\ntype ToArray<T> = T extends any ? T[] : never;\ntype Result = ToArray<string | number>;\n```\nWhat is `Result`?",
      "options": [
        "`(string | number)[]` — one array that can hold either type",
        "`never`, since a union can't satisfy `T extends any`",
        "`string[] | number[]` — the conditional distributes over each union member separately, then unions the results",
        "`string[]` only, since TypeScript picks the first union member"
      ],
      "correctIndex": 2,
      "explanation": "A conditional type applied to a naked type parameter distributes over a union — TypeScript evaluates the condition once per union member and unions the results, giving `string[] | number[]`, not one array that could hold either type mixed together. Nothing here evaluates to `never` or arbitrarily picks the first member; distribution runs for every member.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "typescript-advanced-types-s06-q1",
      "slideId": "typescript-advanced-types-s06",
      "question": "```typescript\nfunction handle(value: unknown) {\n  value.toUpperCase();\n}\n```\nWhy does this fail to compile, when the same code with `value: any` would compile fine?",
      "options": [
        "`unknown` can only hold string values, so `.toUpperCase()` should actually be allowed",
        "`unknown` disables all further use of the value entirely, even after narrowing",
        "`unknown` can hold any value like `any`, but requires narrowing (e.g. a `typeof` check) before any property or method access is allowed — `any` skips that requirement entirely",
        "This is a bug in the example; both `unknown` and `any` behave identically here"
      ],
      "correctIndex": 2,
      "explanation": "`unknown` is intentionally stricter than `any`: it can hold any value, but the compiler forces you to prove what it actually is — via a `typeof`, `instanceof`, or similar check — before allowing any operation on it. `any` opts out of that requirement entirely. `unknown` isn't restricted to strings, and it doesn't permanently block use — it's usable immediately after a narrowing check.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "typescript-advanced-types-s09-q1",
      "slideId": "typescript-advanced-types-s09",
      "question": "A `switch` over a discriminated union ends with `default: const _exhaustive: never = shape;`. A new variant is later added to the union but no matching `case` is added. What happens?",
      "options": [
        "Nothing — the `default` branch silently handles the new variant the same as before",
        "The assignment to `_exhaustive` fails to compile, because `shape` in the `default` branch is now narrowed to the new variant's type, not `never`, flagging the missed case",
        "TypeScript automatically generates the missing `case` at compile time",
        "The `switch` statement throws a runtime error the first time the new variant is encountered"
      ],
      "correctIndex": 1,
      "explanation": "If every existing case is handled, `shape` narrows to `never` in `default`, which type-checks against a `never`-typed variable. Once a case is missing, `shape` in `default` narrows to that unhandled variant's type instead, and assigning that to `never` is a compile error — catching the gap before runtime. TypeScript never generates code automatically, and this is a compile-time signal, not a runtime throw.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "typescript-advanced-types-s10-q1",
      "slideId": "typescript-advanced-types-s10",
      "question": "```typescript\nfunction getUser(id: number): User | undefined { return db.find(id); }\nconst user = getUser(1) as User;\nuser.name.toUpperCase();\n```\nWhy is using `as User` here risky, given that `getUser` is typed to possibly return `undefined`?",
      "options": [
        "It isn't risky — `as` performs a runtime check that guarantees the value matches",
        "The assertion only silences the compiler's warning about the `undefined` case; it performs no runtime check, so if `getUser` actually returns `undefined`, the crash still happens, just without any compile-time warning beforehand",
        "`as User` converts `undefined` into a default-constructed `User` automatically",
        "This code is guaranteed to be safe because TypeScript validates all assertions against actual runtime values"
      ],
      "correctIndex": 1,
      "explanation": "A type assertion is purely a compiler-level instruction with zero runtime effect — it doesn't check, convert, or guarantee anything about the actual value. If `getUser` really does return `undefined`, `user.name` still crashes at runtime; the only thing the assertion changed is silencing the compiler's legitimate warning that this could happen. TypeScript assertions are never runtime-checked.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "typescript-advanced-types-s11-q1",
      "slideId": "typescript-advanced-types-s11",
      "question": "In the typed event emitter (`emit<K extends keyof Events>(event: K, payload: Events[K])`), what stops `emitter.emit(\"login\", { wrong: true })` from compiling, given an `EventMap` where `login` expects `{ userId: number }`?",
      "options": [
        "The generic constraint ties `K` to the specific event name passed, so `payload`'s required type becomes `Events[K]` for that exact key — here `EventMap[\"login\"]`, which `{ wrong: true }` doesn't structurally satisfy",
        "The `Emitter` class hardcodes the `login` event's shape directly, unrelated to generics",
        "It wouldn't actually fail to compile — the example understates what mapped types can catch",
        "TypeScript checks the payload only at runtime when `emit` is actually called"
      ],
      "correctIndex": 0,
      "explanation": "`K extends keyof Events` binds `K` to `\"login\"` for this call, making the expected `payload` type exactly `Events[\"login\"]` via the indexed access `Events[K]` — `{ wrong: true }` lacks the required `userId: number`, so it fails structural typing at compile time. Nothing here is hardcoded per-event; the whole point is that it generalizes to any key in `Events`, and the check happens at compile time, not runtime.",
      "difficulty": "hard",
      "level": "expert"
    }
  ]
}
