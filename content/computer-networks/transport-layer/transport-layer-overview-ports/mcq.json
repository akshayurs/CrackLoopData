{
  "topicId": "transport-layer-overview-ports",
  "mcqs": [
    {
      "id": "transport-layer-overview-ports-s01-q1",
      "slideId": "transport-layer-overview-ports-s01",
      "question": "What is the key capability the transport layer adds on top of what IP already provides?",
      "options": [
        "Encrypting data so it cannot be read in transit",
        "Delivering packets from process to process on a host, using ports",
        "Finding the best physical path between two routers",
        "Translating domain names into IP addresses"
      ],
      "correctIndex": 1,
      "explanation": "IP only gets a packet to the right host; the transport layer's job is process-to-process delivery via ports. Encryption is TLS's job (option A), path selection is routing (C), and name resolution is DNS (D) — both plausible-sounding but wrong layer.",
      "difficulty": "easy",
      "level": "beginner"
    },
    {
      "id": "transport-layer-overview-ports-s03-q1",
      "slideId": "transport-layer-overview-ports-s03",
      "question": "A web server on port 443 is handling requests from 500 different clients at once. What actually distinguishes one client's connection from another's?",
      "options": [
        "Each client is assigned a different server-side port automatically",
        "The full socket tuple: protocol, local IP/port, and remote IP/port",
        "The order in which connections arrived",
        "The TLS session ticket issued to each client"
      ],
      "correctIndex": 1,
      "explanation": "The server keeps listening on 443 for everyone; what makes each connection unique is the 5-tuple including the client's own IP and ephemeral port. Option A is a common misconception — the server-side port doesn't change per client.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "transport-layer-overview-ports-s07-q1",
      "slideId": "transport-layer-overview-ports-s07",
      "question": "Why is assuming 'port 8080 always means HTTP traffic' a risky assumption for a firewall rule or monitoring dashboard?",
      "options": [
        "Port 8080 is reserved by IANA exclusively for HTTPS",
        "A port number is just an addressing convention — any protocol can be run on any port",
        "Port 8080 cannot be used by TCP, only UDP",
        "Browsers block non-HTTP traffic on port 8080"
      ],
      "correctIndex": 1,
      "explanation": "Ports carry no enforcement of what protocol rides on them — that's convention only. Attackers and tunneling tools exploit exactly this gap by running unexpected protocols on 'trusted' ports like 443 or 8080.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}