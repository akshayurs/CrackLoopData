{
  "topicId": "vpns-network-security-tools",
  "mcqs": [
    {
      "id": "vpns-network-security-tools-s01-q1",
      "slideId": "vpns-network-security-tools-s01",
      "question": "What does a VPN fundamentally provide?",
      "options": [
        "A rule set that decides which traffic is allowed into a network",
        "An encrypted tunnel that protects traffic as it crosses an untrusted network",
        "A faster route between two networks by avoiding congested links",
        "A guarantee that no malware can reach the destination host"
      ],
      "correctIndex": 1,
      "explanation": "A VPN encapsulates and encrypts traffic so a party on the path sees only ciphertext \u2014 it protects confidentiality on an untrusted path. Deciding what is *allowed* is a firewall's job, not a VPN's; a VPN doesn't optimize routing for speed, and it can't inspect for or block malware inside its own encrypted tunnel.",
      "difficulty": "easy",
      "level": "beginner"
    },
    {
      "id": "vpns-network-security-tools-s02-q1",
      "slideId": "vpns-network-security-tools-s02",
      "question": "A company wants its branch office network permanently reachable from headquarters without any software on employee machines. Which VPN type fits?",
      "options": [
        "Remote-access VPN, with a client installed on every employee laptop",
        "A TLS portal each employee logs into in a browser",
        "Site-to-site VPN, tunneling between the two networks' gateways",
        "No VPN \u2014 this requires a leased physical line"
      ],
      "correctIndex": 2,
      "explanation": "Site-to-site VPNs join whole networks via gateway devices, so end users need no client \u2014 exactly the requirement. Remote-access VPNs and clientless browser portals connect individual users instead, and a VPN over the public Internet avoids the cost of a dedicated leased line.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "vpns-network-security-tools-s03-q1",
      "slideId": "vpns-network-security-tools-s03",
      "question": "Why do TLS-based VPNs often traverse restrictive firewalls and NAT more easily than IPsec?",
      "options": [
        "They operate below the IP layer, bypassing packet filters entirely",
        "They do not encrypt traffic, so firewalls let them pass",
        "They require no configuration on either endpoint",
        "They ride over TLS on common ports like 443, which is usually already open"
      ],
      "correctIndex": 3,
      "explanation": "TLS VPNs look like ordinary HTTPS on port 443, which firewalls and NAT already permit. They are encrypted, so it is not that firewalls wave them through as plaintext; they run above IP rather than below it; and they still need endpoint setup, just less finicky than IPsec's matched policies.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "vpns-network-security-tools-s04-q1",
      "slideId": "vpns-network-security-tools-s04",
      "question": "What can a stateful firewall do that a basic packet-filtering firewall cannot?",
      "options": [
        "Allow return traffic for a connection it saw initiated and drop unsolicited packets",
        "Decrypt and inspect the application payload of every packet",
        "Encrypt traffic between two sites over the public Internet",
        "Route packets along the shortest available path"
      ],
      "correctIndex": 0,
      "explanation": "Stateful firewalls track active connections, so they distinguish legitimate return traffic from unsolicited packets \u2014 context a header-only packet filter lacks. Decrypting and inspecting payloads is an application-layer firewall's role, encrypting site-to-site traffic is a VPN's, and choosing shortest paths is a router's.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "vpns-network-security-tools-s06-q1",
      "slideId": "vpns-network-security-tools-s06",
      "question": "A team encrypts all remote traffic with a VPN but puts no firewall behind the concentrator. What's the main risk?",
      "options": [
        "The VPN tunnel will silently drop its encryption after connecting",
        "Allowed-in traffic \u2014 including from a compromised client \u2014 reaches internal systems unfiltered",
        "Remote users will be unable to resolve internal DNS names",
        "IPsec and TLS VPNs cannot coexist on the same network"
      ],
      "correctIndex": 1,
      "explanation": "A VPN secures the path but doesn't decide what's allowed; without a firewall, anything delivered through the tunnel \u2014 including malware from an infected laptop \u2014 reaches internal systems freely. The tunnel does not silently drop its encryption, internal DNS resolution is unaffected, and IPsec and TLS VPNs can coexist on one network.",
      "difficulty": "hard",
      "level": "beginner"
    },
    {
      "id": "vpns-network-security-tools-s09-q1",
      "slideId": "vpns-network-security-tools-s09",
      "question": "A company's security policy grants any VPN-connected device broad access to internal systems, on the reasoning that VPN access implies trust. What is the main risk this creates?",
      "options": [
        "VPN connections are inherently unencrypted, so this exposes data in transit",
        "A compromised device that successfully connects over VPN inherits the same broad internal reach as a legitimate one, turning one endpoint compromise into full internal lateral movement",
        "This policy has no real risk since VPN authentication is equivalent to verifying device health",
        "VPNs cannot be used for remote access under this kind of policy"
      ],
      "correctIndex": 1,
      "explanation": "VPN connections are encrypted, so exposure in transit isn't the issue, and VPN authentication verifies the tunnel/credentials, not the device's current security state. The real risk is that broad trust granted purely on 'connected via VPN' means one compromised endpoint gets the same wide internal reach as a legitimate device, which is exactly the lateral-movement pattern zero-trust segmentation is designed to prevent.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
