{
  "topicId": "subnetting-cidr-vlsm",
  "mcqs": [
    {
      "id": "subnetting-cidr-vlsm-s03-q1",
      "slideId": "subnetting-cidr-vlsm-s03",
      "question": "What does bitwise ANDing an IP address with its subnet mask actually compute?",
      "options": [
        "The broadcast address for that subnet",
        "The network address — host bits (where the mask is 0) get zeroed out, leaving only the network portion",
        "A random host address within the same subnet",
        "The next available IP address in the subnet"
      ],
      "correctIndex": 1,
      "explanation": "AND-ing with the mask zeroes out every bit where the mask is 0 (the host portion), leaving exactly the network address. The broadcast address instead requires setting all host bits to 1, not ANDing (ruling out the broadcast-address option); the operation is deterministic, not random (ruling out the random-host-address option); and it computes the network's identity, not a next-available address (ruling out the next-available-address option).",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "subnetting-cidr-vlsm-s05-q1",
      "slideId": "subnetting-cidr-vlsm-s05",
      "question": "Splitting 192.168.1.0/24 into 4 equal subnets requires borrowing how many additional bits, and why?",
      "options": [
        "1 bit, because that's always enough for any split",
        "8 bits, to create a full additional octet per subnet",
        "2 bits, because 2^2 = 4 matches the number of subnets needed exactly",
        "4 bits, to match the number of octets in an IPv4 address"
      ],
      "correctIndex": 2,
      "explanation": "The number of subnets you can create by borrowing n bits is 2^n, so needing exactly 4 subnets requires borrowing 2 bits (2^2=4), moving the prefix from /24 to /26. One bit only yields 2 subnets (too few), and 4 or 8 bits would over-split far beyond what's needed.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "subnetting-cidr-vlsm-s08-q1",
      "slideId": "subnetting-cidr-vlsm-s08",
      "question": "In VLSM planning, why is it important to allocate the subnet needing 100 hosts before the subnet needing 10 hosts, rather than the reverse order?",
      "options": [
        "Order doesn't actually matter for VLSM — any allocation sequence produces the same result",
        "Allocating smallest-first is actually more efficient and is the recommended approach",
        "Smaller subnets must always come first per the IETF's official VLSM standard",
        "Allocating largest-first keeps each subnet's block cleanly aligned and avoids fragmenting the address space into pieces too small to fit the larger remaining needs"
      ],
      "correctIndex": 3,
      "explanation": "Largest-first allocation keeps address boundaries clean, since a large aligned block reserved after several small ones may no longer fit without gaps or realignment. There's no arbitrary IETF ordering mandate (the IETF-mandate claim), order does matter in practice (the claim that order doesn't matter is false), and smallest-first is the discouraged approach, not the recommended one (the claim that smallest-first is recommended is false).",
      "difficulty": "hard",
      "level": "intermediate"
    },
    {
      "id": "subnetting-cidr-vlsm-s13-q1",
      "slideId": "subnetting-cidr-vlsm-s13",
      "question": "A team needs exactly 30 usable host addresses. Which prefix length is the smallest block that fits, and why?",
      "options": [
        "/27, since 2^5=32 total addresses gives exactly 30 usable after subtracting network and broadcast",
        "/26, since it's the next size up commonly used in examples",
        "/27, since 32 total addresses means 32 usable hosts with no subtraction needed",
        "/28, since 16 addresses is closest to 30 divided by 2"
      ],
      "correctIndex": 0,
      "explanation": "A /27 gives 32 total addresses, minus 2 reserved (network and broadcast) = exactly 30 usable, precisely matching the need. The 32-usable-hosts answer makes the classic off-by-one mistake of not subtracting the reserved addresses; /26 and /28 don't match the arithmetic at all.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
