{
  "topicId": "reverse-proxies-gateways",
  "mcqs": [
    {
      "id": "reverse-proxies-gateways-s02-q1",
      "slideId": "reverse-proxies-gateways-s02",
      "question": "A company wants to hide which internal employee made a request before it reaches the public internet. What should sit in front of the client?",
      "options": [
        "An API gateway, because it always encrypts client traffic",
        "A reverse proxy \u2014 it hides backend servers from clients, which is the opposite direction",
        "A forward proxy \u2014 it represents the client to external servers, hiding the client's identity",
        "A CDN, since it caches content close to the client"
      ],
      "correctIndex": 2,
      "explanation": "A forward proxy is specifically the client-facing pattern that hides client identity from destinations. A reverse proxy solves the opposite problem, and neither an API gateway nor a CDN is defined by hiding client identity.",
      "difficulty": "easy",
      "level": "beginner"
    },
    {
      "id": "reverse-proxies-gateways-s05-q1",
      "slideId": "reverse-proxies-gateways-s05",
      "question": "What's the main reason to terminate TLS at a reverse proxy rather than at every individual backend server?",
      "options": [
        "It automatically load-balances requests regardless of backend health",
        "It's the only way HTTPS can be used at all",
        "It removes the need for DNS resolution",
        "Certificate management and encryption/decryption CPU cost live in one place instead of being duplicated across every backend"
      ],
      "correctIndex": 3,
      "explanation": "Centralizing cert management and encryption overhead is the concrete benefit. HTTPS works fine terminated anywhere; TLS termination doesn't affect DNS resolution or provide health-aware load balancing by itself.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "reverse-proxies-gateways-s06-q1",
      "slideId": "reverse-proxies-gateways-s06",
      "question": "What distinguishes an API gateway from a plain reverse proxy?",
      "options": [
        "It adds application-aware policy like authentication, rate limiting, and request routing/aggregation on top of basic proxying",
        "It cannot terminate TLS, unlike a reverse proxy",
        "It only works with GraphQL APIs, not REST",
        "It replaces the need for backend servers entirely"
      ],
      "correctIndex": 0,
      "explanation": "Application-aware policy (auth, rate limiting, aggregation) is the actual distinguishing feature. API gateways typically do terminate TLS, work with any API style, and still route to real backends.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "reverse-proxies-gateways-s07-q1",
      "slideId": "reverse-proxies-gateways-s07",
      "question": "Why do reverse proxy and load balancer often refer to the same piece of software in practice?",
      "options": [
        "Load balancers are a legacy technology fully replaced by reverse proxies",
        "A reverse proxy fronting multiple interchangeable backends is, by definition, doing load balancing \u2014 the terms describe overlapping roles, not separate products",
        "Reverse proxies cannot distribute traffic across more than one backend",
        "They're unrelated concepts that happen to share deployment tools by coincidence"
      ],
      "correctIndex": 1,
      "explanation": "The roles genuinely overlap \u2014 a reverse proxy in front of many backends is performing load balancing. Neither is obsolete or incapable of the other's job; the overlap is structural, not coincidental.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "reverse-proxies-gateways-s09-q1",
      "slideId": "reverse-proxies-gateways-s09",
      "question": "A backend uses the X-Forwarded-For header to decide which requests to rate-limit by client IP, and trusts whatever value arrives in that header on any request. What's the risk?",
      "options": [
        "There is no risk \u2014 X-Forwarded-For is cryptographically signed by the proxy",
        "A client that can reach the backend directly (bypassing the trusted proxy) can set an arbitrary value in that header, spoofing its apparent IP and evading the rate limit",
        "The header only works over HTTP/1.1 and will simply be ignored on newer protocols",
        "X-Forwarded-For can only be set by the reverse proxy itself, never by a client"
      ],
      "correctIndex": 1,
      "explanation": "X-Forwarded-For is a plain, unsigned header \u2014 anything setting it, including a malicious client that can reach the backend directly, can put any value there. The safe pattern is to only trust the header when it's known to arrive through a specific, controlled proxy hop (and to strip/overwrite any value a client tries to set directly), not to trust it unconditionally.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
