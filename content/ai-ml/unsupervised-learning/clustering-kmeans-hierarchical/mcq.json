{
  "topicId": "clustering-kmeans-hierarchical",
  "mcqs": [
    {
      "id": "clustering-kmeans-hierarchical-s03-q1",
      "slideId": "clustering-kmeans-hierarchical-s03",
      "question": "K-means is run twice from two different random initializations and produces two different final clusterings. What does this tell you?",
      "options": [
        "K-means is broken and shouldn't be used",
        "You must have picked the wrong value of k",
        "The data has no real clusters",
        "K-means converged to different local minima — initialization matters"
      ],
      "correctIndex": 3,
      "explanation": "K-means always converges, but only to a local minimum of inertia — different starting centroids can land in different ones. In practice you run it with several random restarts (e.g. k-means++) and keep the best result, not just once.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "clustering-kmeans-hierarchical-s04-q1",
      "slideId": "clustering-kmeans-hierarchical-s04",
      "question": "In the elbow method, why do you look for a bend rather than the k with the lowest inertia?",
      "options": [
        "Inertia strictly decreases as k grows, so the minimum is always at k = number of data points",
        "Lowest inertia is always at k=1",
        "The bend is easier to compute than inertia itself",
        "Silhouette score always agrees with the lowest-inertia k"
      ],
      "correctIndex": 0,
      "explanation": "Inertia keeps falling as you add more clusters — at k = n (one point per cluster) it hits zero. That's not useful, so you look for where adding another cluster stops meaningfully reducing inertia, i.e. the elbow.",
      "difficulty": "easy",
      "level": "beginner"
    },
    {
      "id": "clustering-kmeans-hierarchical-s07-q1",
      "slideId": "clustering-kmeans-hierarchical-s07",
      "question": "What does 'linkage' control in agglomerative hierarchical clustering?",
      "options": [
        "How many clusters the algorithm stops at",
        "How the distance between two clusters is defined when deciding which pair to merge next",
        "The learning rate used when merging clusters",
        "Whether the algorithm runs top-down or bottom-up"
      ],
      "correctIndex": 1,
      "explanation": "Linkage (single, complete, or average) defines cluster-to-cluster distance, which changes which pair merges next and therefore the shape of the resulting dendrogram. It has nothing to do with a learning rate or the direction of the algorithm.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "clustering-kmeans-hierarchical-s09-q1",
      "slideId": "clustering-kmeans-hierarchical-s09",
      "question": "You have 5 million rows and need clusters fast, with roughly round, similarly-sized groups expected. Which is the better default?",
      "options": [
        "Hierarchical clustering, because it doesn't require picking k",
        "Neither works on data this large",
        "K-means, because it scales roughly linearly and fits the expected cluster shape",
        "Hierarchical clustering, because it's deterministic"
      ],
      "correctIndex": 2,
      "explanation": "Hierarchical clustering's O(n²)-or-worse cost makes 5 million rows impractical. K-means' near-linear cost and its fit to roughly spherical clusters make it the right call here — determinism and not needing k upfront don't outweigh hierarchical's scaling problem at this size.",
      "difficulty": "medium",
      "level": "beginner"
    }
  ]
}
