{
  "topicId": "cost-latency-optimization",
  "interviewQuestions": [
    {
      "id": "cost-latency-optimization-iq1",
      "question": "Your AI feature costs 10x the projection in production — what do you check first?",
      "answerMarkdown": "I'd check the cheap, high-leverage suspects before anything architectural. First, actual token counts per request versus what was projected — a system prompt or retrieved context that grew silently over time, or a few-shot example set that's larger than intended, directly multiplies cost on every single call. Second, whether every request is actually going to the model tier that was budgeted for — a routing bug or a misconfigured default can silently send easy requests to the expensive model.\n\nThird, I'd check for redundant calls: is caching (exact-match or semantic) actually working, or is a bug causing cache misses on requests that should hit? Is a chain of multiple LLM calls doing work that a single well-structured call could do, multiplying cost per user action? I'd also check for runaway loops if any part of the system is agentic — an unbounded retry or tool-calling loop can burn 10x cost on a small fraction of requests without showing up as a single obvious outlier.\n\nOnce I've ruled those out, I'd look at request volume itself — maybe usage is genuinely higher than projected, which is a product/business question, not an engineering one.\n\n**Testing:** whether you have a systematic checklist (token bloat, misrouting, cache failures, redundant chains, runaway loops) rather than guessing at one cause, and whether you separate engineering causes from a genuine increase in real usage.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Diagnosing a 10x cost overrun in production"
    }
  ]
}
