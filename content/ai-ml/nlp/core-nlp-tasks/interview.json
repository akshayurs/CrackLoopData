{
  "topicId": "core-nlp-tasks",
  "interviewQuestions": [
    {
      "id": "core-nlp-tasks-iq1",
      "question": "How would you build a support-ticket auto-tagger?",
      "answerMarkdown": "I'd start by clarifying the shape of the problem: fixed set of tags or open-ended, single-label or multi-label (a ticket can be both \"billing\" and \"urgent\"), and how much historical labeled data exists. Assuming a fixed tag set of, say, 15–20 categories and a few thousand labeled tickets, I'd frame this as multi-label text classification: fine-tune a small encoder model (BERT-class) with independent sigmoid outputs per tag rather than one softmax, since tags aren't mutually exclusive. I'd start with a classical TF-IDF + logistic regression baseline first — it's fast to build and gives a floor to beat before investing in fine-tuning. For evaluation, I'd avoid plain accuracy given likely class imbalance (most tickets probably cluster into 2–3 common categories) and use per-label precision/recall/F1, watching specifically for rare-but-important tags like \"legal\" or \"security\" where a missed tag is costly even if rare. In production, I'd log low-confidence predictions for human review and periodically retrain as new labeled tickets accumulate — this is also a natural place to route ambiguous cases to a human rather than force a guess.\n\n**Testing:** whether you can turn a vague product ask into a concrete ML problem statement (multi-label classification, imbalance-aware metrics) rather than jumping straight to \"I'd use an LLM\".",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Framing an open-ended NLP product question"
    }
  ]
}
