{
  "topicId": "text-preprocessing-representation",
  "mcqs": [
    {
      "id": "text-preprocessing-representation-s01-q1",
      "slideId": "text-preprocessing-representation-s01",
      "question": "You're building a grammar-checking tool. Why might removing stopwords hurt this task?",
      "options": [
        "Stopword removal is only defined for languages other than English",
        "Stopwords always increase vocabulary size beyond what any model can handle",
        "Stopwords like \"is\" and \"not\" carry the grammatical and negation signal the tool needs to catch",
        "Removing stopwords makes tokenization fail on punctuation"
      ],
      "correctIndex": 2,
      "explanation": "Stopword removal is task-dependent: for keyword search it cuts noise, but for grammar checking or translation, function words (\"is\", \"not\", \"the\") carry structural and negation meaning that the task needs. Option B misdescribes vocabulary growth (that's an n-gram/subword issue), C is false (stopword lists exist per-language, not \"non-English only\"), and D confuses stopword removal with tokenization, a separate pipeline step.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "text-preprocessing-representation-s02-q1",
      "slideId": "text-preprocessing-representation-s02",
      "question": "In a corpus of 10,000 product reviews, why would TF-IDF assign a low score to the word \"product\" even if it appears many times in every review?",
      "options": [
        "TF-IDF caps the maximum count any single word can contribute per document",
        "TF-IDF ignores words that appear in the title of a review",
        "TF-IDF only scores nouns, and \"product\" is misclassified as a stopword",
        "\"Product\" appears in nearly every document, so its inverse-document-frequency term drives its weight down despite high local frequency"
      ],
      "correctIndex": 3,
      "explanation": "TF-IDF multiplies local frequency (TF) by inverse document frequency (IDF), and a word appearing in almost every document gets an IDF near zero \u2014 that's the whole point of the metric, distinguishing globally common words from locally distinctive ones. The other options invent mechanisms TF-IDF doesn't have (no count cap, no POS filtering, no title-awareness).",
      "difficulty": "easy",
      "level": "beginner"
    },
    {
      "id": "text-preprocessing-representation-s04-q1",
      "slideId": "text-preprocessing-representation-s04",
      "question": "A sentiment classifier using unigram Bag-of-Words misclassifies \"not good\" as positive. Which change most directly addresses this specific failure?",
      "options": [
        "Add bigrams to the vocabulary so \"not good\" is captured as one feature distinct from \"good\" alone",
        "Switch from Bag-of-Words counts to raw TF-IDF weighting",
        "Increase the size of the training set without changing the representation",
        "Apply stemming to reduce \"good\" and \"not\" to a common root"
      ],
      "correctIndex": 0,
      "explanation": "The failure is a representation problem: unigram BoW sees \"not\" and \"good\" as independent counts, losing the negation relationship entirely. Bigrams add \"not_good\" as its own feature, directly encoding the negation. TF-IDF re-weights unigram counts but doesn't add order information (still tempting since it sounds like an upgrade); more data doesn't fix a representation that structurally can't see word order; stemming operates on individual words and has no effect on this issue.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "text-preprocessing-representation-s05-q1",
      "slideId": "text-preprocessing-representation-s05",
      "question": "A word-level tokenizer trained on English news text sees the informal word \"gonna\" for the first time at inference. What happens, and how would a BPE tokenizer differ?",
      "options": [
        "Both tokenizers would fail identically, since neither can handle unseen words",
        "The word-level tokenizer maps it to an out-of-vocabulary token, losing information; BPE decomposes it into known subword pieces, preserving partial signal",
        "The word-level tokenizer automatically expands its vocabulary at inference time to include \"gonna\"",
        "BPE would also emit an out-of-vocabulary token, since it only operates on whole words"
      ],
      "correctIndex": 1,
      "explanation": "This is precisely the gap subword tokenization closes: word-level vocabularies are fixed and finite, so any unseen word becomes a single `<UNK>` token with no information; BPE/WordPiece can always fall back to smaller known pieces (or characters), so \"gonna\" might become \"gon\" + \"na\", preserving some signal. Vocabularies don't expand live at inference (C is wrong), and BPE's entire design point is operating below the word level (D is wrong).",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "text-preprocessing-representation-s07-q1",
      "slideId": "text-preprocessing-representation-s07",
      "question": "Why does lemmatizing \"better\" correctly produce \"good\" while stemming leaves it unchanged?",
      "options": [
        "Stemming and lemmatization are identical processes, so this shouldn't happen",
        "Lemmatization uses linguistic knowledge (irregular forms, part of speech) to look up a word's true dictionary base form, while stemming applies crude rule-based suffix stripping that has no concept of irregular forms like comparatives",
        "Stemming only works on verbs, and \"better\" is being treated as a noun",
        "Lemmatization is simply a slower version of stemming with identical output"
      ],
      "correctIndex": 1,
      "explanation": "The difference is real linguistic grounding: lemmatization consults a vocabulary/POS-aware process that knows \"better\" is the irregular comparative of \"good,\" while stemming only strips common suffix patterns and has no mechanism to recognize irregular forms at all. The two are not equivalent, stemming's limitation isn't about part of speech restriction to verbs, and their outputs meaningfully differ, not just their speed.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "text-preprocessing-representation-s08-q1",
      "slideId": "text-preprocessing-representation-s08",
      "question": "A Named Entity Recognition model's accuracy on distinguishing company names from common nouns drops sharply after the team adds lowercasing to their preprocessing pipeline. What's the most likely cause?",
      "options": [
        "Lowercasing removed one of the strongest signals NER relies on \u2014 capitalization patterns that mark proper nouns \u2014 collapsing distinctions like \"Apple\" (company) versus \"apple\" (fruit) into an identical token",
        "Lowercasing always improves NER accuracy, so this result indicates a bug elsewhere",
        "Lowercasing changes the number of tokens produced by the tokenizer",
        "Lowercasing is unrelated to entity recognition and could not cause this"
      ],
      "correctIndex": 0,
      "explanation": "Capitalization is one of the strongest surface signals for proper-noun detection, so removing it via lowercasing directly destroys a cue the model relied on, especially for words that mean something different capitalized versus not. Lowercasing does not generally improve NER (it's task-dependent, and here it clearly hurt), it doesn't change token count on its own, and it is directly related to entity recognition performance.",
      "difficulty": "medium",
      "level": "beginner"
    }
  ]
}