{
  "topicId": "model-serving-patterns",
  "interviewQuestions": [
    {
      "id": "model-serving-patterns-iq1",
      "question": "Design the serving path for a real-time fraud-scoring model.",
      "answerMarkdown": "I'd start by naming the hard constraint: a fraud decision has to happen within the transaction flow, typically under 100ms, so this is unambiguously an online-serving problem, not batch. I'd containerize the model behind a low-latency serving layer (gRPC internally for speed, since this is a service-to-service call, not a public API), load the model into memory once at startup, and put it behind a load balancer with multiple replicas for both throughput and redundancy — a fraud endpoint going down can't simply queue requests for later. For features, I'd pull precomputed features from a low-latency online feature store rather than computing them on the fly, to keep the request path fast and to guarantee the exact same feature values the model saw in training (avoiding training-serving skew). Before ever serving live traffic, I'd run the new model in shadow deployment against the current production model to compare behavior with zero user risk, then roll out via a canary (a small traffic percentage) while monitoring both model-level metrics (score distributions) and business metrics (fraud caught, false-positive rate on legitimate transactions), ramping up only if those stay healthy, with an instant rollback path if not. I'd close by noting monitoring doesn't stop at launch — fraud patterns drift, so this needs ongoing drift detection and a retraining trigger, not a one-time deployment.\n\n**Testing:** whether you can design an end-to-end serving path — latency budget, feature consistency, safe rollout, ongoing monitoring — rather than just naming \"deploy it behind an API\".",
      "mostAsked": true,
      "level": "beginner",
      "subTitle": "End-to-end low-latency serving design"
    }
  ]
}
