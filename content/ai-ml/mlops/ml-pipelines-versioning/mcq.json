{
  "topicId": "ml-pipelines-versioning",
  "mcqs": [
    {
      "id": "ml-pipelines-versioning-s01-q1",
      "slideId": "ml-pipelines-versioning-s01",
      "question": "A team's model pipeline is a single long Jupyter notebook someone manually re-runs top to bottom whenever new data arrives. What specifically does converting this to an explicit DAG (via Airflow/Kubeflow-style orchestration) improve?",
      "options": [
        "It removes the need for feature engineering entirely",
        "It makes the model itself more accurate, since DAGs improve statistical performance",
        "It makes each step's inputs/outputs trackable, lets individual failed steps retry without rerunning everything, and enables automatic triggering \u2014 none of which a manually re-run notebook provides",
        "Notebooks and DAG-based pipelines are functionally identical; there is no operational benefit either way"
      ],
      "correctIndex": 2,
      "explanation": "The value of a DAG-based pipeline is entirely operational, not statistical: it gives step-level tracking, targeted retries instead of full reruns, and automatic scheduling/triggering \u2014 capabilities a manually-run notebook fundamentally lacks. It has no effect on model accuracy (B is a category error), doesn't eliminate the need for feature engineering (C is false \u2014 features are still a pipeline step), and the operational differences between the two approaches are substantial, not negligible (D is false).",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "ml-pipelines-versioning-s02-q1",
      "slideId": "ml-pipelines-versioning-s02",
      "question": "Three weeks after a promising experiment, a team can't reproduce the result because nobody recorded the exact hyperparameters used. What does experiment tracking directly prevent?",
      "options": [
        "It automatically improves every future model's accuracy",
        "It only tracks metrics, not hyperparameters or data versions",
        "It eliminates the need for a model registry",
        "It logs hyperparameters, dataset version, metrics, and artifacts for every run automatically, turning \"which configuration produced this result\" into a searchable record instead of relying on memory or terminal history"
      ],
      "correctIndex": 3,
      "explanation": "Experiment tracking's whole purpose is capturing exactly this kind of run metadata automatically at training time, so reproducing or comparing past results doesn't depend on anyone's memory \u2014 this is precisely the failure mode described in the scenario. It doesn't directly change model accuracy (A), it's a distinct, complementary practice to a model registry, not a replacement for it (C is false \u2014 registries track promoted model versions, tracking tracks experiment runs), and comprehensive experiment tracking explicitly includes hyperparameters and data versions, not metrics alone (D is false).",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "ml-pipelines-versioning-s03-q1",
      "slideId": "ml-pipelines-versioning-s03",
      "question": "A newly promoted model version causes a revenue regression in production. What does having a model registry make faster and safer compared to an ad-hoc file-copy deployment process?",
      "options": [
        "Rolling back to the last known-good model version becomes a lookup-and-redeploy against tracked, versioned model lineage, rather than a scramble to remember which file was previously running",
        "The registry automatically retrains a better model without human intervention",
        "Model registries eliminate the possibility of any future regression",
        "A registry is only useful for tracking hyperparameters, not model artifacts"
      ],
      "correctIndex": 0,
      "explanation": "The registry's operational payoff shows up exactly in this rollback scenario: because every promoted version is tracked with clear lineage and stage metadata, reverting to the previous production version is a deliberate, auditable lookup rather than reconstructing what was deployed from memory or scattered file copies. A registry doesn't perform retraining itself (A is a category error), it reduces regression risk through better process but doesn't eliminate regressions outright (C overclaims), and registries specifically track model artifacts and their lineage, not hyperparameters in isolation \u2014 that's the role of experiment tracking (D is false).",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "ml-pipelines-versioning-s04-q1",
      "slideId": "ml-pipelines-versioning-s04",
      "question": "A team pins the exact code (via git commit) used to train a model six months ago but did not version the training data. When they try to reproduce the result, they get different metrics. What is the most likely explanation?",
      "options": [
        "Git commits are insufficient to pin code, which must be the real problem",
        "The underlying data table has changed since training (new rows, corrections, schema evolution), so the same code now runs against a different dataset than the one originally used, even though the code itself is identical",
        "Reproducing ML results is fundamentally impossible regardless of versioning practices",
        "Model training is inherently non-deterministic and can never be reproduced under any circumstances"
      ],
      "correctIndex": 1,
      "explanation": "This illustrates exactly why data versioning matters alongside code versioning: an unpinned data source can silently change underneath identical code, breaking reproducibility even when the code itself is provably unchanged \u2014 this is the specific gap data versioning tools (DVC, dataset snapshots) close. Git pinning code correctly is not the issue here (A misdiagnoses the cause), reproducibility is achievable with proper data versioning, not fundamentally impossible (C overclaims), and while some training randomness exists, it's typically controllable via seeding and isn't the described failure mode here (D overstates the non-determinism claim).",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "ml-pipelines-versioning-s05-q1",
      "slideId": "ml-pipelines-versioning-s05",
      "question": "A team sets up drift-triggered automated retraining that immediately promotes any newly retrained model straight to production with no validation step. What risk does this design introduce?",
      "options": [
        "None \u2014 automation always produces a correct model by construction",
        "Automated retraining pipelines cannot technically skip validation steps",
        "A newly retrained model could be worse than the current production model (due to a data quality issue, a labeling bug, or bad luck) and would auto-promote without anyone catching it, since the validation gate that would normally catch this was skipped",
        "This design only affects training speed, not model quality in production"
      ],
      "correctIndex": 2,
      "explanation": "Automation removes the human-in-the-loop review a manual process would otherwise provide, so skipping the validation gate means a broken or degraded retrain (from a data issue, a bug, or randomness) could reach production directly \u2014 retaining the same quality gates a manual run would go through is essential precisely because automation removes that safety net. Automation doesn't guarantee correctness by construction (A is false), skipping validation is a real design choice teams can and do make, unfortunately (C is false), and the risk described is specifically about production model quality, not training speed (D is false).",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "ml-pipelines-versioning-s07-q1",
      "slideId": "ml-pipelines-versioning-s07",
      "question": "A pipeline retrains only on a fixed weekly schedule. What's the main risk of relying solely on that trigger?",
      "options": [
        "It retrains too often, wasting compute even when nothing has changed",
        "It cannot react quickly to a sudden distribution shift that happens between scheduled runs, since nothing is watching for drift in between",
        "Scheduled retraining always produces a worse model than event-triggered retraining",
        "Weekly schedules are incompatible with model registries"
      ],
      "correctIndex": 1,
      "explanation": "A fixed schedule does retrain reliably, but it has no awareness of what's happening between runs \u2014 if a real distribution shift hits on day two of a seven-day cycle, the model keeps serving degraded predictions for up to five more days before the next scheduled run even looks at it. A drift-triggered alert closes exactly that gap; a schedule alone can't, regardless of how it's tuned.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
