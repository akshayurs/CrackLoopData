{
  "topicId": "recommender-fundamentals",
  "mcqs": [
    {
      "id": "recommender-fundamentals-s02-q1",
      "slideId": "recommender-fundamentals-s02",
      "question": "Why do large-scale recommenders split into candidate generation and ranking instead of scoring the whole catalog for every user?",
      "options": [
        "Because scoring every item for every user is computationally infeasible at millions-of-items scale, so a cheap stage first narrows to a plausible subset",
        "Because ranking models can't process more than a few hundred items in their training data",
        "Because implicit feedback only works on a small number of items at a time",
        "Because collaborative filtering requires items to be pre-sorted alphabetically before scoring"
      ],
      "correctIndex": 0,
      "explanation": "The two-stage split exists purely for compute cost: a cheap, coarse retrieval stage cuts millions of items down to hundreds, and only those get the expensive, precise ranking model. Ranking models have no inherent item-count limit (wrong distractor), and neither implicit feedback nor sort order is the reason for the split.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "recommender-fundamentals-s05-q1",
      "slideId": "recommender-fundamentals-s05",
      "question": "A brand-new item just got uploaded with zero interactions. Which approach can still recommend it, and why?",
      "options": [
        "Collaborative filtering, because it recomputes user similarity in real time",
        "Content-based filtering, because it can match the item's own features (tags, description) to a user's history without needing any interaction data on the item itself",
        "Neither approach can recommend a zero-interaction item until it accumulates clicks",
        "Collaborative filtering, because popularity signals bootstrap new items automatically"
      ],
      "correctIndex": 1,
      "explanation": "Content-based filtering only needs the item's own features, so a new item is scorable the moment it exists. Collaborative filtering needs interaction data to place an item near others in latent space — with zero interactions it has literally no signal, regardless of how 'real-time' the similarity computation is.",
      "difficulty": "easy",
      "level": "beginner"
    }
  ]
}
