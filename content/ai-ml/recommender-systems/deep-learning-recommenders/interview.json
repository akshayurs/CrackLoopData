{
  "topicId": "deep-learning-recommenders",
  "interviewQuestions": [
    {
      "id": "deep-learning-recommenders-iq1",
      "question": "Why do two-tower models scale to hundreds of millions of items?",
      "answerMarkdown": "⭐ **Q: Why do two-tower models scale to hundreds of millions of items?**\n\nI'd start with the architecture: a two-tower model has two separate networks — one embeds the user (plus context), one embeds the item — and never combines their features until a final dot product. That separation is the whole trick.\n\nBecause the item tower doesn't depend on the user at all, you can run it once, offline, over your *entire* catalog and store every item's embedding in an index ahead of time. At request time you only need to run the user tower once, then do an approximate nearest-neighbor search (FAISS, ScaNN) against that precomputed item index — which is sublinear in catalog size, not a forward pass per candidate item.\n\nConcretely, this is why YouTube and Instagram use two-tower models for candidate generation specifically: it lets them cut hundreds of millions of items down to a few hundred candidates in milliseconds, before handing off to a heavier, feature-rich ranking model that can afford to actually run a full network per candidate on that much smaller shortlist.\n\n**Testing:** whether you understand the precompute-vs-serve-time trade that makes retrieval at scale possible — this is the same principle behind every large-scale embedding-based search system, not just recommenders.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Why two-tower models scale"
    }
  ]
}
