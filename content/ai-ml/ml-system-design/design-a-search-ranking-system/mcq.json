{
  "topicId": "design-a-search-ranking-system",
  "mcqs": [
    {
      "id": "design-a-search-ranking-system-s02-q1",
      "slideId": "design-a-search-ranking-system-s02",
      "question": "Why does a mistake in query understanding (e.g. a missed typo correction) compound so badly downstream in a search system?",
      "options": [
        "Because retrieval then searches for entirely the wrong thing, and no amount of good ranking afterward can recover relevant results that were never retrieved in the first place",
        "Because query understanding errors only affect the ranking stage, not retrieval",
        "Because typos are corrected automatically by the ranking model regardless of retrieval",
        "Because search engines discard any query containing a spelling error"
      ],
      "correctIndex": 0,
      "explanation": "Query understanding runs before retrieval, so an error there means retrieval fetches candidates for the wrong query entirely — ranking can only reorder whatever retrieval returned, it can't add back documents that were never fetched. The other options misplace where in the pipeline the correction happens or invent behavior search systems don't actually have.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "design-a-search-ranking-system-s06-q1",
      "slideId": "design-a-search-ranking-system-s06",
      "question": "Why is training or evaluating a ranker on raw click-through data risky without correcting for position bias?",
      "options": [
        "Users click higher-ranked results more often simply because they're seen first, so raw clicks reinforce whatever the current ranking already does rather than reflecting true relevance",
        "Click data cannot be logged for more than the top 3 search results",
        "Position bias only affects mobile search interfaces, not desktop",
        "Clicks are a perfectly unbiased measure of relevance and need no correction"
      ],
      "correctIndex": 0,
      "explanation": "The core issue is a feedback loop: position, not just relevance, drives clicks, so uncorrected click data can make a mediocre-but-highly-placed result look better than a genuinely superior one ranked lower. Logging isn't limited to top-3, the bias isn't device-specific, and clicks are precisely not an unbiased relevance signal on their own.",
      "difficulty": "hard",
      "level": "advanced"
    }
  ]
}
