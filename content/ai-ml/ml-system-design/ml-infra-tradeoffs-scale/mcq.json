{
  "topicId": "ml-infra-tradeoffs-scale",
  "mcqs": [
    {
      "id": "ml-infra-tradeoffs-scale-s02-q1",
      "slideId": "ml-infra-tradeoffs-scale-s02",
      "question": "Why are retrieval-stage models in a multi-stage pipeline deliberately simpler than ranking-stage models?",
      "options": [
        "Because retrieval models are trained on less data than ranking models by convention",
        "Because the whole request shares one latency SLA split across stages, so retrieval's slice of the budget hard-constrains how much compute its model can spend, regardless of what would be most accurate in isolation",
        "Because ranking models are always more accurate than retrieval models on any metric",
        "Because retrieval only ever runs on cached results and never needs a model at all"
      ],
      "correctIndex": 1,
      "explanation": "The constraint is architectural, not a training-data or accuracy preference — each stage only gets a fraction of the total latency budget, and retrieval's slice is small because it has to scan the largest candidate pool. It's not about relative accuracy in the abstract, and retrieval very much uses a model (just a cheaper one), not pure caching.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "ml-infra-tradeoffs-scale-s06-q1",
      "slideId": "ml-infra-tradeoffs-scale-s06",
      "question": "In a common multi-region ML serving compromise, why is training kept centralized while inference is replicated across regions?",
      "options": [
        "Because training requires a physical data center in every region by law",
        "Because inference is latency-sensitive to user proximity while training is not, so replicating the trained artifact for low-latency serving captures the benefit without paying the cost of distributing the training process itself",
        "Because model artifacts cannot be copied between regions",
        "Because centralizing training eliminates the need for a model registry"
      ],
      "correctIndex": 1,
      "explanation": "The split follows directly from which part of the system actually needs to be geographically close to users — inference latency matters per-request, training does not, since it happens offline ahead of time. There's no such legal requirement, model artifacts are routinely replicated across regions, and a registry is still needed regardless of where training happens.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
