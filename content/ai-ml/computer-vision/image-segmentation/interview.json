{
  "topicId": "image-segmentation",
  "interviewQuestions": [
    {
      "id": "image-segmentation-iq1",
      "question": "Semantic vs instance segmentation — give a real product example of each.",
      "answerMarkdown": "I'd define the distinction first, then anchor it in products so it's concrete rather than textbook. Semantic segmentation labels every pixel by class but doesn't distinguish individual objects of the same class — a real example is an autonomous-driving perception stack labeling \"drivable road surface\" as one connected region; the car doesn't need to know it's looking at \"road instance #1 and #2\", it just needs to know which pixels are safely drivable. Instance segmentation separates individual object instances within the same class — a real example is a photo-editing app's \"select subject\" or background-blur feature (like portrait mode), where if there are three people in frame, the app needs to isolate each person as a distinct mask so you can, say, blur the background behind all three but keep each person individually editable. I'd close by noting the two aren't competing choices — they're suited to different questions, which is exactly why panoptic segmentation exists, to combine both into one unified output when a product genuinely needs both (e.g. \"road\" as semantic stuff plus \"car #1\", \"car #2\" as instances, in one self-driving frame).\n\n**Testing:** whether you can map the abstract distinction onto concrete product decisions, not just recite the textbook definitions.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Grounding the distinction in real products"
    }
  ]
}
