{
  "id": "image-segmentation",
  "title": "Image Segmentation",
  "area": "ai-ml",
  "group": "computer-vision",
  "order": 3,
  "slug": "image-segmentation",
  "summary": "Per-pixel understanding of an image — semantic, instance, and panoptic segmentation, and the architectures behind them.",
  "level": "advanced",
  "slideCount": 6,
  "estReadMinutes": 9,
  "slides": [
    {
      "id": "image-segmentation-s01",
      "order": 1,
      "sectionTitle": "Segmentation as per-pixel classification",
      "subTitle": "Beyond boxes to exact shapes",
      "type": "concept",
      "markdown": "Object detection tells you a bounding box around an object; segmentation tells you **exactly which pixels** belong to it. Formally, segmentation assigns a class label to **every pixel** in the image, producing a full-resolution label map rather than a handful of boxes. This is strictly more informative — and strictly more expensive to label and predict — than detection: a bounding box takes 4 numbers to describe, a segmentation mask takes one label per pixel. That extra cost buys real value wherever exact shape matters: medical imaging (tumor boundary, not just \"tumor present\"), autonomous driving (drivable road surface, not just \"road region roughly here\"), and photo editing (precise background removal).",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 45,
      "mcqIds": ["image-segmentation-s01-q1"]
    },
    {
      "id": "image-segmentation-s02",
      "order": 2,
      "sectionTitle": "Semantic vs instance vs panoptic",
      "subTitle": "What each one distinguishes",
      "type": "compare",
      "markdown": "| | Semantic | Instance | Panoptic |\n|---|---|---|---|\n| Question answered | Which class does each pixel belong to? | Which specific object instance does each pixel belong to? | Both, unified |\n| Two dogs side by side | Both labeled \"dog\" — indistinguishable as individuals | Labeled \"dog #1\" and \"dog #2\" separately | \"dog #1\", \"dog #2\", plus background classes like \"grass\", \"sky\" |\n| Handles uncountable stuff (sky, road)? | Yes | No — only countable objects | Yes, alongside countable instances |\n\nSemantic segmentation can't distinguish individuals of the same class; instance segmentation can't label uncountable background regions; panoptic segmentation was introduced specifically to unify both into one consistent output.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 40,
      "mcqIds": ["image-segmentation-s02-q1"]
    },
    {
      "id": "image-segmentation-s03",
      "order": 3,
      "sectionTitle": "Encoder-decoder architectures",
      "subTitle": "U-Net and its skip connections",
      "type": "concept",
      "markdown": "Segmentation needs two things a plain classifier CNN doesn't naturally give you: high-level semantic understanding (what is this?) *and* precise spatial detail (exactly where is the boundary?). **U-Net**'s encoder (contracting path) downsamples the image, building up semantic understanding like a normal CNN classifier — but this loses fine spatial detail along the way. Its decoder (expanding path) upsamples back to full resolution to produce the pixel-wise output. The key trick: **skip connections** copy feature maps directly from each encoder stage across to the matching decoder stage, re-injecting the fine spatial detail that downsampling destroyed — without skip connections, boundaries in the output would be blurry and imprecise.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 45,
      "mcqIds": ["image-segmentation-s03-q1"]
    },
    {
      "id": "image-segmentation-s04",
      "order": 4,
      "sectionTitle": "U-Net's shape",
      "subTitle": "Contracting and expanding paths",
      "type": "diagram",
      "markdown": "The architecture's namesake \"U\" shape: the contracting path narrows and deepens (more channels, smaller spatial size); the expanding path widens back out (fewer channels, larger spatial size), mirroring the contracting path stage-for-stage so each decoder stage has a matching encoder stage to receive a skip connection from.\n\n![U-Net's U-shaped architecture, showing the contracting encoder path narrowing down and the expanding decoder path widening back up, connected by skip connections at matching resolutions](assets/unet-architecture.svg)",
      "assets": [{"alt": "U-Net's U-shaped architecture, showing the contracting encoder path narrowing down and the expanding decoder path widening back up, connected by skip connections at matching resolutions", "path": "assets/unet-architecture.svg"}],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 30,
      "mcqIds": []
    },
    {
      "id": "image-segmentation-s05",
      "order": 5,
      "sectionTitle": "Mask R-CNN",
      "subTitle": "Extending detection to per-instance masks",
      "type": "concept",
      "markdown": "**Mask R-CNN** extends Faster R-CNN (a two-stage object detector) by adding a third output branch: alongside the existing box-regression and classification branches, it adds a small segmentation network that predicts a binary mask **within each detected region**. This is a clean architectural insight — detection already tells you roughly where each object instance is, so instance segmentation becomes \"detect first, then segment each detected region\" rather than segmenting the whole image at once and figuring out instances afterward. This is why Mask R-CNN is naturally an **instance** segmentation method (built on per-object detection), while U-Net-style architectures are naturally **semantic** segmentation methods (built on whole-image, per-pixel classification with no inherent notion of separate instances).",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 45,
      "mcqIds": ["image-segmentation-s05-q1"]
    },
    {
      "id": "image-segmentation-s06",
      "order": 6,
      "sectionTitle": "Evaluating segmentation",
      "subTitle": "Pixel-wise IoU and the Dice coefficient",
      "type": "concept",
      "markdown": "Segmentation quality is scored per pixel, not per box. **Pixel-wise IoU**: same idea as detection's IoU, but computed over pixel sets — overlap between predicted and ground-truth mask, divided by their union. **Dice coefficient**: 2 × overlap / (predicted mask size + ground-truth mask size) — mathematically related to IoU (Dice is always ≥ IoU for the same masks) but more sensitive to small objects, which is why Dice is the standard metric in medical imaging, where the structure of interest (a tumor, a lesion) is often a tiny fraction of the image and small errors matter disproportionately.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 40,
      "mcqIds": ["image-segmentation-s06-q1"]
    }
  ]
}
