{
  "topicId": "svm-kernels",
  "mcqs": [
    {
      "id": "svm-kernels-s02-q1",
      "slideId": "svm-kernels-s02",
      "question": "Two candidate boundaries both perfectly separate the training classes. Why would an SVM prefer the one with the wider margin?",
      "options": [
        "A wider margin always means higher training accuracy",
        "SVMs cannot represent boundaries with a narrow margin, so this is not actually a choice",
        "A wider margin requires fewer support vectors, which speeds up training",
        "A wider margin gives more room before small perturbations in new data cross the boundary, which tends to generalize better"
      ],
      "correctIndex": 3,
      "explanation": "Both boundaries already achieve perfect training separation, so training accuracy is identical \u2014 the wider margin is preferred for robustness to new data, not for fitting the training set better. Margin width doesn't determine the number of support vectors, and narrow-margin boundaries are perfectly representable, just not preferred.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "svm-kernels-s04-q1",
      "slideId": "svm-kernels-s04",
      "question": "You set C very high on a soft-margin SVM and notice training accuracy is near-perfect but validation accuracy is poor. What's happening?",
      "options": [
        "A high C heavily penalizes margin violations, producing a narrow margin that fits training data closely and overfits",
        "A high C is unrelated to overfitting; the issue must be the choice of kernel",
        "A high C always improves both training and validation accuracy",
        "A high C forces the SVM to ignore the support vectors entirely"
      ],
      "correctIndex": 0,
      "explanation": "High C makes the optimizer intolerant of margin violations, so it shrinks the margin to fit training points tightly \u2014 classic overfitting. This is directly tied to C, not the kernel choice, and it does not improve validation accuracy or eliminate support vectors (it usually reduces how many are needed, but doesn't remove the concept).",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "svm-kernels-s07-q1",
      "slideId": "svm-kernels-s07",
      "question": "You need well-calibrated probability estimates (not just a class label) for a downstream risk score. Which model directly gives you that, and why?",
      "options": [
        "SVM, because the margin distance is itself a calibrated probability",
        "Logistic regression, because it's trained by maximizing log-likelihood, which directly produces a probability estimate",
        "Both models produce equally calibrated probabilities by default",
        "Neither model can produce probabilities without a kernel transformation"
      ],
      "correctIndex": 1,
      "explanation": "Logistic regression's training objective is likelihood-based, so its sigmoid output is a genuine probability estimate. A raw SVM outputs a margin/distance, not a probability \u2014 getting probability-like scores out of it requires an extra calibration step (e.g. Platt scaling), and even then it's an approximation layered on top.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "svm-kernels-s08-q1",
      "slideId": "svm-kernels-s08",
      "question": "A team wants to train a kernel SVM on a dataset with 5 million rows and expects it to train about as fast as logistic regression. What should you tell them?",
      "options": [
        "That's a reasonable expectation \u2014 SVMs scale linearly like logistic regression",
        "SVMs are always faster than logistic regression regardless of dataset size",
        "Kernel SVM training scales roughly quadratically to cubically with sample count, so at that scale it will be dramatically slower and likely impractical",
        "The scaling issue only applies to the linear kernel, not other kernels"
      ],
      "correctIndex": 2,
      "explanation": "Kernel SVMs compute pairwise kernel values across the training set, giving roughly quadratic-to-cubic scaling in sample count \u2014 a serious problem at millions of rows. Logistic regression scales much more gracefully. The scaling issue is actually worse for non-linear kernels, not limited to the linear one.",
      "difficulty": "easy",
      "level": "advanced"
    },
    {
      "id": "svm-kernels-s09-q1",
      "slideId": "svm-kernels-s09",
      "question": "Why does scikit-learn's SVM implementation default to one-vs-one rather than one-vs-rest for multi-class problems, despite training more classifiers overall?",
      "options": [
        "One-vs-one is always more accurate than one-vs-rest regardless of dataset size",
        "One-vs-rest cannot be implemented for SVMs at all",
        "Each one-vs-one classifier trains on only two classes' worth of data, keeping individual training problems small even as class count grows, whereas one-vs-rest's classifier count grows more slowly but each sees the full imbalanced one-vs-all split",
        "One-vs-one requires no additional classifiers beyond the base binary SVM"
      ],
      "correctIndex": 2,
      "explanation": "The trade-off is about the shape of each sub-problem: OvO's per-classifier data stays small and balanced (two classes only), while OvR keeps classifier count linear in the number of classes but each one faces a large, imbalanced binary split. OvO isn't universally more accurate, OvR is a valid and implementable strategy for SVMs, and OvO does train k(k-1)/2 classifiers, clearly more than one.",
      "difficulty": "hard",
      "level": "expert"
    }
  ]
}