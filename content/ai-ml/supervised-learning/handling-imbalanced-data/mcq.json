{
  "topicId": "handling-imbalanced-data",
  "mcqs": [
    {
      "id": "handling-imbalanced-data-s02-q1",
      "slideId": "handling-imbalanced-data-s02",
      "question": "A fraud model trained on 1% positive data reports 99% accuracy but catches almost no actual fraud. What does this indicate?",
      "options": [
        "The model is performing extremely well and is ready for production",
        "The model must have a bug in its loss function implementation",
        "The training data needs more features to fix this",
        "Accuracy is misleading here — the model likely learned to predict the majority class almost always, and recall/precision should be checked instead"
      ],
      "correctIndex": 3,
      "explanation": "With 99% of data in one class, predicting that class alone yields 99% accuracy while providing zero value for detecting fraud — the actual goal. This is a metric-choice problem, not a bug or a missing-feature problem; recall, precision, F1, or PR-AUC reveal what accuracy hides.",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "handling-imbalanced-data-s05-q1",
      "slideId": "handling-imbalanced-data-s05",
      "question": "A trained model ranks positives above negatives reasonably well (good AUC), but using the default 0.5 threshold gives poor recall on the rare positive class. What's the most direct fix?",
      "options": [
        "Lower the decision threshold based on the precision/recall trade-off you need, without retraining",
        "Retrain the entire model from scratch with a different algorithm",
        "Discard the model since a good AUC with poor recall is contradictory",
        "Apply SMOTE to the test set to rebalance it"
      ],
      "correctIndex": 0,
      "explanation": "A good AUC means the model ranks predictions well; a poor recall at 0.5 just means the default cutoff doesn't match your goals. Threshold moving fixes this without retraining. Retraining or switching algorithms is unnecessary extra cost, the situation isn't contradictory, and resampling the test set would corrupt your evaluation.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "handling-imbalanced-data-s07-q1",
      "slideId": "handling-imbalanced-data-s07",
      "question": "A team applies SMOTE to the entire dataset, then splits into train and test. Validation metrics look excellent but production performance is much worse. What went wrong?",
      "options": [
        "SMOTE always overestimates performance regardless of when it's applied",
        "Synthetic training points may have been interpolated using real minority points that ended up in the test set, leaking test information into training",
        "The test set was too small to be reliable",
        "SMOTE should never be used for fraud-style problems"
      ],
      "correctIndex": 1,
      "explanation": "Applying SMOTE before the split lets synthetic training rows draw on real minority points now sitting in the test set, so the model has effectively seen fragments of test data during training — classic leakage. It's the ordering that's wrong, not SMOTE itself or the test set's size, and SMOTE is a legitimate tool for exactly this kind of problem when applied correctly.",
      "difficulty": "hard",
      "level": "intermediate"
    }
  ]
}
