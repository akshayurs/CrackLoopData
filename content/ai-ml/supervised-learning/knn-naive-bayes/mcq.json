{
  "topicId": "knn-naive-bayes",
  "mcqs": [
    {
      "id": "knn-naive-bayes-s03-q1",
      "slideId": "knn-naive-bayes-s03",
      "question": "A KNN model with k=1 gets much lower training error than the same model with k=25, but performs worse on the test set. What does this indicate?",
      "options": [
        "k=1 has lower variance, so it should generalize better",
        "k=1 is overfitting to individual training points, including noise, while k=25 averages over more neighbors and generalizes better",
        "The distance metric must be implemented incorrectly at k=1",
        "k=25 is definitely the better choice for any dataset"
      ],
      "correctIndex": 1,
      "explanation": "k=1 always matches the single nearest training point exactly, so training error is artificially low while variance is high — a classic overfitting signature. k=25 trades some bias for lower variance and often generalizes better, but that doesn't mean a large k is universally correct; the right k still depends on the data and should be tuned via validation.",
      "difficulty": "medium",
      "level": "beginner"
    },
    {
      "id": "knn-naive-bayes-s04-q1",
      "slideId": "knn-naive-bayes-s04",
      "question": "A KNN model trained on unscaled features where one column ranges 0-1 and another ranges 0-500,000 performs poorly. What's the most likely cause?",
      "options": [
        "KNN cannot handle more than one feature at a time",
        "The large-range feature dominates the distance calculation, effectively drowning out the smaller-range feature",
        "K was chosen too small regardless of scaling",
        "Naive Bayes should have been used instead since it requires no scaling"
      ],
      "correctIndex": 1,
      "explanation": "Distance metrics sum contributions across all features, so a feature with a much larger numeric range dominates the total distance unless features are standardized first. This has nothing to do with the number of features supported, k's value, or needing to switch models — the fix is scaling, not swapping algorithms.",
      "difficulty": "easy",
      "level": "beginner"
    },
    {
      "id": "knn-naive-bayes-s06-q1",
      "slideId": "knn-naive-bayes-s06",
      "question": "Naive Bayes assumes features are conditionally independent given the class, which is rarely literally true. Why does it still classify well in practice?",
      "options": [
        "The independence assumption becomes true whenever there are enough training examples",
        "Correctness only requires the correct class to rank highest, and violations of independence often bias scores similarly across classes, preserving that ranking",
        "It secretly learns the true joint distribution despite the naive assumption",
        "It only works well on datasets where features actually are independent"
      ],
      "correctIndex": 1,
      "explanation": "Classification only needs the winning class's score to come out on top, not for the underlying probabilities to be exactly correct — dependence violations frequently shift every class's score in a similar way, leaving the relative ranking intact. More data doesn't make the assumption true, and it doesn't secretly recover the true joint distribution; it performs well even when features are correlated, not only when they're independent.",
      "difficulty": "hard",
      "level": "beginner"
    }
  ]
}
