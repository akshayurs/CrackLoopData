{
  "topicId": "decoding-sampling-strategies",
  "interviewQuestions": [
    {
      "id": "decoding-sampling-strategies-iq1",
      "question": "Why doesn't temperature=0 guarantee the same output across two calls?",
      "answerMarkdown": "I'd start by clarifying what temperature=0 actually does: it makes the sampling step deterministic — effectively greedy, always the argmax token — so if everything else were fixed, two calls would produce identical output. The catch is \"everything else fixed\" rarely holds in production.\n\nConcretely: many providers batch requests together for efficiency, and floating-point operations aren't strictly associative — the exact order operations get summed in depends on what else is in the batch, GPU kernel scheduling, and numeric precision (bfloat16/fp16), so the logits themselves can differ by tiny floating-point amounts between calls. When two tokens are close in probability, that tiny numeric wobble can flip which one is the argmax, and one flipped token early in generation cascades into a different rest-of-sequence.\n\n**Testing:** whether you understand that determinism in sampling is separate from determinism in numerical computation — temperature controls the former, not the latter.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Why temperature=0 isn't fully deterministic"
    }
  ]
}
