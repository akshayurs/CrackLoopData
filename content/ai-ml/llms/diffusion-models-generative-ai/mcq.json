{
  "topicId": "diffusion-models-generative-ai",
  "mcqs": [
    {
      "id": "diffusion-models-generative-ai-s03-q1",
      "slideId": "diffusion-models-generative-ai-s03",
      "question": "What does a diffusion model's neural network actually learn to predict during training?",
      "options": [
        "The noise that was added at a given step, so it can be subtracted to recover a slightly less noisy image",
        "The exact pixel values of the final generated image directly, in a single forward pass",
        "The text prompt that was used to describe the training image",
        "The optimal number of denoising steps to use at generation time"
      ],
      "correctIndex": 0,
      "explanation": "The network is trained to predict the noise added at each step of the fixed forward process, which lets it subtract that noise to step backward toward a cleaner image \u2014 repeating this is how generation works. It doesn't directly output final pixels in one pass, doesn't predict text prompts, and step-count is a sampling choice, not something the network predicts.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "diffusion-models-generative-ai-s05-q1",
      "slideId": "diffusion-models-generative-ai-s05",
      "question": "Why does the same diffusion model and the same text prompt produce a different image on two separate generation runs?",
      "options": [
        "The model's weights change slightly between runs",
        "Generation starts from a randomly sampled noise canvas, so different random seeds produce different starting points and therefore different final images",
        "The text encoder randomly changes the prompt's meaning each time",
        "Diffusion models cache and randomly replay a past generated image"
      ],
      "correctIndex": 1,
      "explanation": "The randomness in diffusion generation comes from the initial noise sample, not from the model weights or the prompt itself \u2014 different noise means the reverse process denoises toward a different image, even under identical conditioning. Weights are fixed at inference, the text encoder is deterministic given the same prompt, and there's no image caching/replay mechanism involved.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "diffusion-models-generative-ai-s07-q1",
      "slideId": "diffusion-models-generative-ai-s07",
      "question": "What problem does latent diffusion (as used in Stable Diffusion) solve compared to running diffusion directly on raw pixels?",
      "options": [
        "It removes the need for a text encoder in text-to-image generation",
        "It eliminates randomness from the generation process entirely",
        "It runs the expensive denoising process on a much smaller compressed latent representation instead of the full pixel grid, cutting compute significantly with little quality loss",
        "It replaces the reverse diffusion process with a single-step GAN-style generator"
      ],
      "correctIndex": 2,
      "explanation": "Latent diffusion compresses the image via a separate autoencoder into a smaller latent space, runs the full noising/denoising process there, and only decodes to pixels once at the end \u2014 dramatically reducing the compute per step. It still needs a text encoder for conditioning, doesn't remove the randomness inherent to sampling, and doesn't replace the multi-step reverse process with a GAN.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "diffusion-models-generative-ai-s08-q1",
      "slideId": "diffusion-models-generative-ai-s08",
      "question": "A team needs real-time image generation on a mobile device where speed matters more than achieving the absolute best possible image quality. Which trade-off from the diffusion-vs-GAN comparison is most relevant?",
      "options": [
        "Diffusion models require no training data, unlike GANs",
        "Diffusion models are always faster than GANs at inference time",
        "GANs cannot generate images conditioned on text under any circumstances",
        "GANs generate in a single forward pass and are much faster, making them attractive despite generally lower peak quality and training instability during development"
      ],
      "correctIndex": 3,
      "explanation": "GANs generate an image in one forward pass, making them far faster at inference than diffusion's many sequential denoising steps \u2014 a real trade-off worth making when speed is the priority, even though diffusion currently wins on quality and diversity. Diffusion is slower, not faster, at inference; GANs can be conditioned on text; and both families require substantial training data.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "diffusion-models-generative-ai-s09-q1",
      "slideId": "diffusion-models-generative-ai-s09",
      "question": "How does classifier-free guidance make a text-to-image diffusion model follow the prompt more closely?",
      "options": [
        "It trains a separate classifier network to score and reject off-prompt images after generation",
        "It runs the denoiser both with and without the text conditioning at each step, then pushes the prediction further in the direction the conditioned pass diverges, scaled by a guidance weight",
        "It increases the number of denoising steps automatically",
        "It replaces the text encoder with a larger one at generation time"
      ],
      "correctIndex": 1,
      "explanation": "Classifier-free guidance compares a conditioned and unconditioned prediction at every step and amplifies the difference by a guidance scale, steering generation toward the prompt without needing any separate classifier network. It's unrelated to step count and doesn't swap out the text encoder.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "diffusion-models-generative-ai-s10-q1",
      "slideId": "diffusion-models-generative-ai-s10",
      "question": "What happens if the classifier-free guidance scale is pushed far higher than its typical sweet spot?",
      "options": [
        "Generation becomes strictly better on every axis, including realism",
        "The model starts producing over-saturated, high-contrast images with visible artifacts, because it's extrapolating past the conditioned distribution it was trained on",
        "The guidance scale has no effect beyond a certain threshold",
        "The model automatically clips the scale back down to a safe value"
      ],
      "correctIndex": 1,
      "explanation": "Guidance scale trades prompt fidelity against natural-looking diversity \u2014 pushed too high, the extrapolation overshoots the trained distribution and produces artifacts and oversaturation rather than a strictly better image. It doesn't plateau to no effect or self-clip; there's a real cost to over-cranking it.",
      "difficulty": "medium",
      "level": "advanced"
    }
  ]
}