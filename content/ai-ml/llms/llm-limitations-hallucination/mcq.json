{
  "topicId": "llm-limitations-hallucination",
  "mcqs": [
    {
      "id": "llm-limitations-hallucination-s02-q1",
      "slideId": "llm-limitations-hallucination-s02",
      "question": "Why does an LLM state a false fact with the same confident tone as a true one?",
      "options": [
        "The model deliberately lies when it detects the user won't verify the answer",
        "Its training objective optimizes for plausible, fluent next-token continuations, not for a separate truth-verification signal, so it has no distinct 'unknown' state to fall back on",
        "Hallucination only happens when the model's temperature setting is misconfigured to a high value",
        "The tokenizer inserts random incorrect tokens during generation"
      ],
      "correctIndex": 1,
      "explanation": "Next-token prediction rewards plausible continuations, not verified ones, and the model has no clean mechanism to flag uncertainty — so a confident-sounding guess and a confident-sounding correct fact are generated the same way. It's not intentional deception, purely a temperature setting, or a tokenizer defect.",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "llm-limitations-hallucination-s04-q1",
      "slideId": "llm-limitations-hallucination-s04",
      "question": "Why does giving an LLM access to a real calculator tool typically produce more reliable results than asking it to compute a multi-step arithmetic problem directly?",
      "options": [
        "LLMs cannot process numeric tokens at all without external tools",
        "A single wrong intermediate token in a step-by-step computation compounds into a wrong final answer, with no internal mechanism to detect and correct it — a real calculator has no such failure mode",
        "Calculators are always faster than LLMs, which is the only benefit",
        "Tool use eliminates the need for the model to understand the problem at all"
      ],
      "correctIndex": 1,
      "explanation": "LLMs generate step-by-step computations as plausible token sequences, so one wrong intermediate step propagates forward uncorrected; a real calculator computes exactly, sidestepping that failure mode entirely. LLMs can process numeric tokens fine — the issue is precision under compounding steps, not tool speed, and the model still needs to understand the problem to formulate the right calculator call.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "llm-limitations-hallucination-s05-q1",
      "slideId": "llm-limitations-hallucination-s05",
      "question": "A model's chain-of-thought text shows clean, logical-looking steps leading to the correct final answer. What can you conclude about that reasoning trace?",
      "options": [
        "The stated steps are guaranteed to be exactly what computation produced the final answer",
        "The trace often improves the odds of a correct answer, but isn't guaranteed to faithfully represent the actual computation behind that answer",
        "Chain-of-thought text is always unrelated to the final answer",
        "The model must have used a symbolic reasoning engine internally to produce it"
      ],
      "correctIndex": 1,
      "explanation": "Research shows chain-of-thought explanations can be fluent and lead to correct answers without faithfully reflecting the actual internal process — the text is generated the same way as any other output. It's not a guarantee of faithfulness, but it's also not meaningless; and models don't invoke a separate symbolic reasoning engine to produce it.",
      "difficulty": "hard",
      "level": "intermediate"
    },
    {
      "id": "llm-limitations-hallucination-s07-q1",
      "slideId": "llm-limitations-hallucination-s07",
      "question": "What's the most accurate way to think about mitigating hallucination in a production LLM feature?",
      "options": [
        "A single well-crafted system prompt fully eliminates hallucination",
        "Multiple complementary mitigations — grounding via retrieval, offloading precision work to tools, and verification passes — each address a different failure point, and combining them is what actually reduces hallucination",
        "Hallucination can only be fixed by fine-tuning on more data",
        "Increasing the model size to the largest available option is the only reliable fix"
      ],
      "correctIndex": 1,
      "explanation": "No single technique fully solves hallucination; RAG addresses missing/ungrounded facts, tool use addresses precision-sensitive computation, and verification passes catch what slips through — layering them is what moves real production accuracy. A prompt alone, more fine-tuning data alone, or simply a bigger model alone each address only part of the problem.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
