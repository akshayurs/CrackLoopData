{
  "id": "huge-pages-hw-support",
  "title": "Huge Pages — Hardware Support",
  "area": "computer-architecture",
  "group": "hw-virtual-memory",
  "order": 6,
  "slug": "huge-pages-hw-support",
  "summary": "Trading page granularity for TLB reach — how a handful of huge pages can cover what thousands of regular pages couldn't.",
  "level": "advanced",
  "slideCount": 9,
  "estReadMinutes": 7,
  "slides": [
    {
      "id": "huge-pages-hw-support-s01",
      "order": 1,
      "sectionTitle": "Snapshot",
      "subTitle": null,
      "type": "overview",
      "markdown": "**Huge pages** (2MB or 1GB, versus the standard 4KB) let a single TLB entry cover far more memory at once. Since TLB reach is entries × page size, making the page size 512× bigger (2MB vs 4KB) means the same TLB entry count covers 512× more memory.\n\nFor large, contiguous working sets — big in-memory databases, large ML tensors, virtual machine memory — this can be the difference between a TLB that comfortably covers the whole working set and one that's constantly missing.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 28,
      "mcqIds": []
    },
    {
      "id": "huge-pages-hw-support-s02",
      "order": 2,
      "sectionTitle": "The problem",
      "subTitle": "The TLB-reach limit",
      "type": "concept",
      "markdown": "TLB reach (entries × page size) is often surprisingly small — a 64-entry TLB with 4KB pages covers only 256KB, far less than a typical L2 cache. A program with a large working set (say, gigabytes of data it touches repeatedly) will constantly TLB-miss even though the *data* itself might comfortably fit in L2 or L3 cache.\n\nThis mismatch means a program can be cache-friendly but still pay heavily for translation overhead, purely because the TLB can't keep up with how much memory the program actively touches.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 36,
      "mcqIds": [
        "huge-pages-hw-support-s02-q1"
      ]
    },
    {
      "id": "huge-pages-hw-support-s03",
      "order": 3,
      "sectionTitle": "The fix",
      "subTitle": "Fewer, bigger entries",
      "type": "diagram",
      "markdown": "The same memory region that needs hundreds of 4KB-page TLB entries can be covered by a single 2MB-page entry instead.\n\n![The same memory region covered by many 4KB TLB entries versus a single 2MB entry](assets/huge-page-tlb-coverage.svg)\n\nFewer entries means far fewer TLB misses for the same working set — the trade is coarser granularity for much better TLB coverage.",
      "assets": [
        {
          "alt": "The same memory region covered by many 4KB TLB entries versus a single 2MB entry",
          "path": "assets/huge-page-tlb-coverage.svg"
        }
      ],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 30,
      "mcqIds": []
    },
    {
      "id": "huge-pages-hw-support-s04",
      "order": 4,
      "sectionTitle": "Supporting both",
      "subTitle": "Multiple page sizes at once",
      "type": "concept",
      "markdown": "Hardware supports huge pages by allowing a page-table entry at an intermediate level to terminate the walk early — rather than pointing to yet another level of the tree, it directly marks \"this entry covers a 2MB (or 1GB) region\" and gives the frame number right there.\n\nThis means a single page table can mix page sizes freely: most of a process's address space might use standard 4KB pages while a specific large buffer uses 2MB pages, all resolved through the same walking mechanism, just terminating at a different level depending on the page's declared size.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 38,
      "mcqIds": [
        "huge-pages-hw-support-s04-q1"
      ]
    },
    {
      "id": "huge-pages-hw-support-s05",
      "order": 5,
      "sectionTitle": "The trade-off",
      "subTitle": "Huge pages vs regular pages",
      "type": "compare",
      "markdown": "| | Regular pages (4KB) | Huge pages (2MB / 1GB) |\n|---|---|---|\n| TLB pressure | Higher for large working sets | Much lower |\n| Internal fragmentation | Minimal | Can be significant |\n| Allocation flexibility | Fine-grained | Coarse — must allocate in large chunks |\n| Best for | General-purpose, varied allocations | Large, dense, long-lived allocations |\n\nHuge pages win decisively for large, contiguous, heavily-touched memory. They lose for workloads with lots of small, short-lived, or sparse allocations, where the coarser granularity wastes memory and allocation becomes awkward.",
      "assets": [],
      "hasCode": false,
      "hasTable": true,
      "estReadSeconds": 34,
      "mcqIds": []
    },
    {
      "id": "huge-pages-hw-support-s06",
      "order": 6,
      "sectionTitle": "A common trap",
      "subTitle": "Huge pages aren't a free performance win",
      "type": "pitfall",
      "markdown": "**Wrong belief:** \"Huge pages always improve performance, so enable them everywhere.\"\n\n**Why it's wrong:** a 2MB page is one solid unit — if an app needs only a fraction, or allocates sparsely, the rest is wasted **internal fragmentation** for its whole lifetime. Allocating one also needs 2MB of physically *contiguous* memory, which can fail or force costly compaction.\n\n**Correct model:** huge pages target large, dense, long-lived regions (databases, VM memory) — not a default. Applied to sparse allocations, they can waste more than the TLB savings are worth.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 36,
      "mcqIds": [
        "huge-pages-hw-support-s06-q1"
      ]
    },
    {
      "id": "huge-pages-hw-support-s07",
      "order": 7,
      "sectionTitle": "The reach math, worked out",
      "subTitle": null,
      "type": "code",
      "markdown": "Take a TLB with 64 entries — a realistic L1 TLB size. With standard 4KB pages, its total reach (the working-set size it can cover without a miss) is:\n\n```\n64 entries * 4 KB/page = 256 KB\n```\n\nThat's tiny next to a modern working set — a single large in-memory hash table or a few megabytes of hot heap already blows past it, guaranteeing a steady stream of TLB misses each costing a multi-level walk (hardware-page-table-walk). Switch the same 64 entries to 2MB huge pages instead:\n\n```\n64 entries * 2 MB/page = 128 MB\n```\n\nA 512x jump in reach from the *same* 64 hardware entries, because each entry now covers 512x more address space. This is why database engines, JVMs, and in-memory caches explicitly configure huge pages for their largest heaps — it directly converts into fewer TLB misses on exactly the workloads (large sequential or randomly-accessed heaps) that suffer the most from small-page TLB pressure.",
      "assets": [],
      "hasCode": true,
      "hasTable": false,
      "estReadSeconds": 67,
      "mcqIds": []
    },
    {
      "id": "huge-pages-hw-support-s08",
      "order": 8,
      "sectionTitle": "Transparent vs explicit huge pages",
      "subTitle": null,
      "type": "compare",
      "markdown": "Operating systems expose huge pages two ways, and the choice matters for predictability, not just raw throughput.\n\n| | Explicit (`hugetlbfs`) | Transparent (THP) |\n|---|---|---|\n| Who decides | Application, explicitly | Kernel, automatically |\n| Allocation timing | Reserved upfront | On-demand, can compact/migrate memory to form one |\n| Latency predictability | High — no surprise pauses | Lower — background compaction can stall an allocation |\n| Ease of use | Requires app-level plumbing | Free, no app changes |\n\nExplicit huge pages are reserved from a fixed pool at boot and handed out deterministically — a database that mmaps a hugetlbfs-backed region knows exactly what it's getting, with no surprise latency. **Transparent huge pages** try to give you the benefit for free by opportunistically promoting regular pages into a huge page in the background — but that promotion can require the kernel to *compact* physical memory (move pages around to find 8 contiguous 4KB frames), and that compaction work can stall the requesting thread. This is precisely why several major databases (Redis, MongoDB historically) recommend disabling THP in production: the tail-latency spikes from compaction pauses outweigh the average-case win.",
      "assets": [],
      "hasCode": false,
      "hasTable": true,
      "estReadSeconds": 81,
      "mcqIds": []
    },
    {
      "id": "huge-pages-hw-support-s09",
      "order": 9,
      "sectionTitle": "Fragmentation is the price of huge reach",
      "subTitle": null,
      "type": "pitfall",
      "markdown": "Huge pages don't just help TLB reach for free — they trade away allocation granularity. A 4KB page wastes at most 4KB of internal fragmentation per allocation (the last partial page of a region); a 2MB huge page can waste up to **2MB** if the actual data needs are smaller or don't align to the huge boundary. A process that allocates thousands of small, short-lived objects under huge pages can burn far more physical memory than the same workload on regular pages.\n\nThere's a second, subtler cost: forming a huge page requires 512 *contiguous* physical 4KB frames. On a long-running system where memory has fragmented (normal after enough allocation/free churn), the kernel may simply be unable to find that contiguous run without expensive compaction — which is exactly the THP stall described on the previous slide.\n\nThe practical rule: huge pages are a clear win for large, long-lived, size-predictable allocations (a database buffer pool, a JVM heap sized at startup) and a real risk for workloads with many small, short-lived, or size-unpredictable allocations — measure before enabling them broadly rather than assuming bigger pages are strictly better.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 78,
      "mcqIds": []
    }
  ]
}