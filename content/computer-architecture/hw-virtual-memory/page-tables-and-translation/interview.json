{
  "topicId": "page-tables-and-translation",
  "interviewQuestions": [
    {
      "id": "page-tables-and-translation-iq1",
      "question": "How does a page table translate a virtual address to a physical one, and why are real page tables multi-level?",
      "answerMarkdown": "I'd start with the split: a virtual address breaks into a virtual page number and a page offset. The page table maps the virtual page number to a physical frame number; the offset carries through unchanged since pages and frames are the same size, so the physical address is just that frame number glued to the original offset.\n\nThen the 'why multi-level' part: a flat table needs one entry per virtual page across the *entire* address space, and for a 64-bit space that's astronomically large — completely impractical even though a typical process only touches a tiny fraction of it. Multi-level tables fix this by splitting the virtual page number into several smaller indices, one per tree level; unused regions of the address space simply never get their lower-level tables allocated, so the structure's real size tracks how much memory a process actually maps, not the theoretical address-space size. I'd mention x86-64 as the concrete example — a four-level walk from virtual address to physical frame.\n\n**Testing:** whether you can explain both the translation mechanism and the scaling reason for hierarchy, not just one or the other.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Translation mechanism and multi-level design"
    },
    {
      "id": "page-tables-and-translation-iq2",
      "question": "What information does a page-table entry hold besides the physical frame number?",
      "answerMarkdown": "I'd list the fields that matter: a **valid bit** saying whether this entry actually points to a mapped page at all; **protection bits** for read/write/execute and user/supervisor access, enforced by hardware on every single access; a **dirty bit** marking whether the page has been written since it was loaded, which the eviction policy needs to decide whether the page must be written back before reuse; and an **accessed bit** that tracks recent references, used to approximate LRU-style eviction decisions.\n\nI'd close by framing the PTE as more than a translation record — it's a small bundle of permission and bookkeeping state that hardware checks on every access, which is exactly why memory protection and virtual memory are implemented with the same underlying structure rather than as separate mechanisms.\n\n**Testing:** whether you know the PTE holds real operational state beyond just an address, since interviewers often use this to segue into protection or replacement-policy questions.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "What's inside a PTE"
    },
    {
      "id": "page-tables-and-translation-iq3",
      "question": "By hand, translate virtual address 0x2050 to a physical address, given a page size of 4KB (0x1000) and that virtual page 2 maps to physical frame 5.",
      "answerMarkdown": "I'd split the virtual address into its page number and offset first, since that split is the entire mechanism translation relies on: with a 4KB (`0x1000`) page size, the low 12 bits are the offset within the page, and everything above that is the virtual page number. `0x2050` in binary has its low 12 bits as the offset — `0x2050 = 0x2000 + 0x050`, so the offset is `0x050` and the virtual page number is `0x2050 >> 12 = 2` (confirming this address does fall in virtual page 2, matching the given mapping).\n\nThe page table maps virtual page 2 to physical frame 5, so the physical address is built by taking that frame number and reattaching the *same* offset unchanged — the offset never gets translated, only the page number does, because pages and frames are defined to be exactly the same size. So the physical address is `(frame 5 << 12) | offset 0x050 = 0x5000 | 0x050 = 0x5050`.\n\nThe part worth stating explicitly, since it's the detail most likely to be probed as a follow-up: the offset is preserved bit-for-bit because translation only ever changes *which page/frame* you're in, never your position *within* it — this is precisely why pages and frames must always be the same size in any paging scheme, and why the offset bit-width is a direct function of the page size (`log2(page size)` bits), not an independent design choice.\n\n**Testing:** whether you can execute the actual translation arithmetic correctly, including keeping the offset unchanged, rather than only being able to describe the process in words.",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "Translating an address by hand"
    }
  ]
}
