{
  "topicId": "endianness",
  "mcqs": [
    {
      "id": "endianness-s01-q1",
      "slideId": "endianness-s01",
      "question": "In little-endian memory layout, which byte of a multi-byte value is stored at the lowest memory address?",
      "options": [
        "The most-significant byte",
        "The least-significant byte",
        "Always the zero byte",
        "It's random and determined at runtime"
      ],
      "correctIndex": 1,
      "explanation": "Little-endian stores the least-significant byte first (at the lowest address) -- the mnemonic is 'little end first'. Big-endian does the opposite, storing the most-significant byte first.",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "endianness-s04-q1",
      "slideId": "endianness-s04",
      "question": "Why do little-endian machines need functions like htonl/ntohl before sending multi-byte values over the network?",
      "options": [
        "Because TCP/IP only transmits single-byte values",
        "Because network protocols standardized on big-endian byte order, so little-endian hosts must swap bytes to match the wire format",
        "Because the network card doesn't support little-endian data",
        "htonl/ntohl are only needed for floating-point numbers, not integers"
      ],
      "correctIndex": 1,
      "explanation": "Network byte order is big-endian by long-standing convention. Little-endian machines (most modern hardware) must byte-swap multi-byte fields via htonl/ntohl to match that convention when sending or receiving over the network.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "endianness-s07-q1",
      "slideId": "endianness-s07",
      "question": "What typically happens when you parse a binary file or packet using the wrong endianness assumption?",
      "options": [
        "The program immediately crashes with a clear error message",
        "It silently produces a different, plausible-looking but incorrect numeric value -- no error is raised",
        "The operating system automatically detects and corrects the mistake",
        "Only floating-point fields are affected, integers are unaffected"
      ],
      "correctIndex": 1,
      "explanation": "Reading bytes with the wrong endianness assumption doesn't fail loudly -- it just reinterprets the same bytes as a different, still-valid-looking number, which is exactly what makes this bug class dangerous (e.g., a corrupted length field driving a bad buffer size).",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
