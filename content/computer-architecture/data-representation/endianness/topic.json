{
  "id": "endianness",
  "title": "Endianness",
  "area": "computer-architecture",
  "group": "data-representation",
  "order": 8,
  "slug": "endianness",
  "summary": "Endianness is just the byte order of a multi-byte value in memory — invisible until you cross a boundary between a little-endian CPU and a big-endian network wire.",
  "level": "intermediate",
  "slideCount": 7,
  "estReadMinutes": 6,
  "slides": [
    {
      "id": "endianness-s01",
      "order": 1,
      "sectionTitle": "What it means",
      "subTitle": null,
      "type": "concept",
      "markdown": "A multi-byte value (a 32-bit int spans 4 bytes) has to be laid out in memory in *some* order — **endianness** is that order. **Little-endian** stores the least-significant byte at the lowest address; **big-endian** stores the most-significant byte first.\n\nThe value `0x12345678` in little-endian memory reads byte-by-byte as `78 56 34 12`; in big-endian, `12 34 56 78`. Neither is 'more correct' — it's purely a convention the CPU (and any code reading raw bytes) has to agree on ahead of time.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 34,
      "mcqIds": ["endianness-s01-q1"]
    },
    {
      "id": "endianness-s02",
      "order": 2,
      "sectionTitle": "What it means",
      "subTitle": "The same value, two layouts",
      "type": "diagram",
      "markdown": "Seeing the same 32-bit value laid out both ways makes the difference concrete:\n\n![The 32-bit value 0x12345678 stored little-endian vs big-endian in memory](assets/endianness-layout.svg)\n\nBoth layouts represent exactly the same number — `0x12345678` — the bytes are just written to memory addresses in opposite order. Read the little-endian bytes back as big-endian (or vice versa) without knowing which convention was used, and you get a completely different, wrong number.",
      "assets": [
        { "alt": "The 32-bit value 0x12345678 stored little-endian vs big-endian in memory", "path": "assets/endianness-layout.svg" }
      ],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 28,
      "mcqIds": []
    },
    {
      "id": "endianness-s03",
      "order": 3,
      "sectionTitle": "Why little-endian won",
      "subTitle": "Mainstream CPUs",
      "type": "concept",
      "markdown": "**x86** and most **ARM** configurations run little-endian. One practical reason: little-endian makes truncating to a smaller type free — a 32-bit int's low-order byte is *already* at the same address a smaller 8/16-bit read would use, no data movement needed. Historical inertia matters too: once dominant x86 committed to little-endian, most tooling was built and tested against it.\n\nThere's no deep technical superiority here — it's a path-dependent industry outcome, reinforced by decades of x86 dominance.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 36,
      "mcqIds": []
    },
    {
      "id": "endianness-s04",
      "order": 4,
      "sectionTitle": "Why little-endian won",
      "subTitle": "Network byte order is big-endian",
      "type": "concept",
      "markdown": "Internet protocols (IP, TCP, and most binary wire formats) standardized on **big-endian**, historically called **network byte order** — chosen decades ago partly because big-endian is the more 'natural' order to read left-to-right by a human debugging a packet dump, and partly to have one fixed convention so any two machines (regardless of their own native endianness) could interoperate.\n\nThis means any little-endian machine (nearly everything today) must **byte-swap** multi-byte fields before sending them on the wire, and swap back on receipt — `htonl`/`ntohl` in C exist for exactly this.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 34,
      "mcqIds": ["endianness-s04-q1"]
    },
    {
      "id": "endianness-s05",
      "order": 5,
      "sectionTitle": "Working with it",
      "subTitle": "Detecting and swapping",
      "type": "code",
      "markdown": "You can detect a machine's native endianness by inspecting how a multi-byte value's first byte reads:\n\n```c\nuint32_t x = 1;\nif (*(uint8_t*)&x == 1)\n    printf(\"little-endian\\n\");   // low byte (1) is at the lowest address\nelse\n    printf(\"big-endian\\n\");\n\n#include <arpa/inet.h>\nuint32_t wire_value = htonl(host_value); // host -> network (big-endian) byte order\nuint32_t back        = ntohl(wire_value); // network -> host byte order\n```\n\nOn a big-endian machine, `htonl`/`ntohl` are no-ops (already the right order); on little-endian, they byte-swap. Portable code calls them unconditionally and never assumes which case it's running on.",
      "assets": [],
      "hasCode": true,
      "hasTable": false,
      "estReadSeconds": 32,
      "mcqIds": []
    },
    {
      "id": "endianness-s06",
      "order": 6,
      "sectionTitle": "The rare middle ground",
      "subTitle": "Bi-endian architectures",
      "type": "compare",
      "markdown": "A few architectures (notably ARM and some MIPS/PowerPC variants) are **bi-endian** — the CPU can operate in either mode, selected by a configuration bit at boot or reset.\n\n| | Fixed little-endian (x86) | Bi-endian (some ARM/PowerPC) |\n|---|---|---|\n| Flexibility | None — always one order | Chosen per system, fixed at boot |\n| Common use | Desktop/server, simplicity | Embedded/networking gear needing to match a legacy big-endian protocol or codebase |\n\nIn practice bi-endian mode is rarely switched at runtime — it's usually a one-time choice baked into a board's firmware, because mixing endianness mid-execution would be a maintenance nightmare, not a genuinely dynamic per-operation feature.",
      "assets": [],
      "hasCode": false,
      "hasTable": true,
      "estReadSeconds": 32,
      "mcqIds": []
    },
    {
      "id": "endianness-s07",
      "order": 7,
      "sectionTitle": "Pitfalls",
      "subTitle": "Wrong endianness assumption",
      "type": "pitfall",
      "markdown": "Parsing a binary file or network packet with the wrong byte-order assumption doesn't crash — it silently produces a **completely different, plausible-looking wrong number**. A 4-byte length field read with swapped endianness can turn a small value into a huge one, then drive a buffer size or loop bound incorrectly.\n\nThis is why well-designed binary formats document byte order explicitly, or better, include a **magic number**/byte-order-mark at the start so a reader can detect which endianness was used — PNG does exactly this.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 32,
      "mcqIds": ["endianness-s07-q1"]
    }
  ]
}
