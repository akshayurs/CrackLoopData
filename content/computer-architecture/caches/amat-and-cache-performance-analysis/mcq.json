{
  "topicId": "amat-and-cache-performance-analysis",
  "mcqs": [
    {
      "id": "amat-and-cache-performance-analysis-s02-q1",
      "slideId": "amat-and-cache-performance-analysis-s02",
      "question": "In the multi-level AMAT formula, what does a cache level's 'miss penalty' actually equal?",
      "options": [
        "A fixed constant, typically 100 cycles regardless of the system",
        "The AMAT of the next level down in the hierarchy",
        "The hit time of the level above it",
        "The total capacity of the next level down, in bytes"
      ],
      "correctIndex": 1,
      "explanation": "The recursive insight is that a miss at level N has to go fetch from level N+1, so the cost of that miss is exactly whatever level N+1's own AMAT is — which itself may include further misses going deeper. It's not a fixed constant, has nothing to do with the level above's hit time, and capacity in bytes isn't a time quantity at all.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "amat-and-cache-performance-analysis-s03-q1",
      "slideId": "amat-and-cache-performance-analysis-s03",
      "question": "Why does L2's local miss rate look much worse than its global miss rate?",
      "options": [
        "Local miss rate is measured only against accesses that already missed L1 — a harder subset — while global miss rate is measured against all original CPU accesses",
        "Local and global miss rate are always numerically identical for L2",
        "Global miss rate only counts writes, while local miss rate counts both reads and writes",
        "Local miss rate is a made-up metric with no formal definition"
      ],
      "correctIndex": 0,
      "explanation": "Local miss rate's denominator is only the accesses that reached L2 at all (i.e., L1 misses) — a pre-filtered, harder-to-hit subset — while global miss rate's denominator is every access from the top of the hierarchy, making the same number of L2 misses look far smaller as a fraction of all accesses. The two are legitimately and predictably different, not identical, and the distinction has nothing to do with read/write splits.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "amat-and-cache-performance-analysis-s05-q1",
      "slideId": "amat-and-cache-performance-analysis-s05",
      "question": "Doubling L1's associativity reduces its miss rate but adds 1 extra cycle to its hit time. Under what condition does this change make overall AMAT worse, not better?",
      "options": [
        "It can never make AMAT worse if the miss rate improves at all",
        "If the added hit-time cost (paid on every access) outweighs the miss-penalty cycles saved by the lower miss rate",
        "Only if the cache is fully associative already",
        "Only if L2 doesn't exist in the hierarchy"
      ],
      "correctIndex": 1,
      "explanation": "AMAT = hit time + miss rate x miss penalty; the extra hit-time cycle is paid on literally every access (hit or miss), while the miss-rate improvement only pays off on the fraction of accesses that would have missed. If that trade is unfavorable, AMAT rises even though hit rate improved — which is exactly why you must compute both terms rather than assume higher hit rate wins.",
      "difficulty": "hard",
      "level": "advanced"
    }
  ]
}
