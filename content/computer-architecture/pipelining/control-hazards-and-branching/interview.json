{
  "topicId": "control-hazards-and-branching",
  "interviewQuestions": [
    {
      "id": "control-hazards-and-branching-iq1",
      "question": "What is a control hazard, and what are the ways a pipeline can handle it?",
      "answerMarkdown": "⭐ **Q: What is a control hazard, and what are the ways a pipeline can handle it?**\n\nI'd define it first: fetch has to decide what instruction to grab every single cycle, but a branch's actual outcome — taken or not, and its target — usually isn't known until it resolves later, typically in EX. That gap is the control hazard.\n\nThen I'd lay out the spectrum from simplest to most sophisticated. The naive fix is to **stall** fetch until the branch resolves — always correct, but costs several idle cycles on every single branch, which adds up since branches are frequent. Next up is **static prediction** — a fixed rule like always-not-taken or backward-taken/forward-not-taken — free when right, but blind to how any specific branch actually behaves. Beyond that is **dynamic prediction**, which tracks per-branch history at runtime and gets meaningfully more accurate. I'd close by noting that when any prediction is wrong, the pipeline has to **flush** the wrongly-fetched instructions and restart fetch from the correct target — and that flush penalty is exactly what all this prediction machinery exists to avoid paying often.\n\n**Testing:** whether you can lay out the full spectrum of fixes and explain the fundamental trade-off (cost when right vs cost when wrong) rather than jumping straight to branch prediction.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Control hazards and the fix spectrum"
    },
    {
      "id": "control-hazards-and-branching-iq2",
      "question": "Why does branch misprediction get more expensive as pipelines get deeper?",
      "answerMarkdown": "**Q: Why does branch misprediction get more expensive as pipelines get deeper?**\n\nMy answer is purely about counting stages. The misprediction penalty is the number of pipeline stages between fetch and wherever the branch's outcome is actually resolved — every instruction fetched in that window down the wrong path has to be discarded. In a shallow 5-stage pipeline resolving branches in EX, that's only 2 wasted instructions. In a modern CPU with 15-20+ stages, the branch might not resolve until much later, so a misprediction can flush a dozen or more in-flight instructions.\n\nI'd connect this to why deep-pipeline CPUs invest so heavily in accurate dynamic branch prediction and large branch target buffers — the deeper the pipeline, the higher the stakes of getting a branch wrong, so squeezing out even a percent or two more prediction accuracy pays off disproportionately.\n\n**Testing:** whether you can connect pipeline depth directly to flush cost with the actual mechanism, not just state 'deeper pipelines are riskier' as received wisdom.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Misprediction cost vs pipeline depth"
    }
  ]
}
