{
  "topicId": "classic-5-stage-pipeline",
  "mcqs": [
    {
      "id": "classic-5-stage-pipeline-s02-q1",
      "slideId": "classic-5-stage-pipeline-s02",
      "question": "In the classic 5-stage pipeline, what happens during the MEM stage for an ADD instruction that only uses registers?",
      "options": [
        "It passes through the stage doing no memory work, since it has nothing to access",
        "It writes its result directly to memory instead of a register",
        "It is skipped entirely and moves straight from EX to WB",
        "It stalls until a load instruction frees the memory port"
      ],
      "correctIndex": 0,
      "explanation": "Every instruction passes through all five stages in order to keep the pipeline uniform, even when a stage has no work for it — a register-only ADD simply passes through MEM idle. It isn't skipped (that would break the uniform timing every other instruction relies on), and it doesn't write to memory or need to wait for a load.",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "classic-5-stage-pipeline-s04-q1",
      "slideId": "classic-5-stage-pipeline-s04",
      "question": "Why must pipeline registers carry control signals forward, not just data values?",
      "options": [
        "Control signals are only needed by the decode stage, so they can be discarded after that",
        "Later stages still need to know what kind of instruction they're handling, even cycles after decode produced that information",
        "Control signals determine the clock frequency for the next stage",
        "Data values already encode which instruction type produced them"
      ],
      "correctIndex": 1,
      "explanation": "A stage several cycles downstream (e.g. write-back deciding whether to write a register) still needs to know facts decode determined, like whether this is a load or a branch — that information has to ride along in the pipeline registers just like the data does. Data values alone don't self-describe the instruction type, and control signals don't affect clock frequency.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "classic-5-stage-pipeline-s06-q1",
      "slideId": "classic-5-stage-pipeline-s06",
      "question": "In the 4-instruction trace, by which cycle are all five pipeline stages simultaneously occupied by different instructions?",
      "options": [
        "Cycle 1",
        "Cycle 3",
        "Cycle 4",
        "Cycle 5"
      ],
      "correctIndex": 2,
      "explanation": "With one new instruction entering fetch each cycle, it takes 4 cycles to fill a 5-stage pipeline with 4 distinct instructions across 4 of the 5 stages; by cycle 4 (I1 in MEM, I2 in EX, I3 in ID, I4 in IF) the pipeline is at its fullest overlap for this trace. Cycle 1 has only one instruction in flight; cycle 5 is when I1 finishes and I4 reaches MEM.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "classic-5-stage-pipeline-s08-q1",
      "slideId": "classic-5-stage-pipeline-s08",
      "question": "For 200 independent instructions on a 5-stage pipeline, what is the total cycle count and the approximate speedup over non-pipelined execution?",
      "options": [
        "204 cycles, about 4.9x speedup",
        "200 cycles, exactly 5x speedup",
        "1000 cycles, no speedup",
        "5 cycles, since all instructions overlap perfectly"
      ],
      "correctIndex": 0,
      "explanation": "Total cycles = N + (k-1) = 200 + 4 = 204. Non-pipelined would take N*k = 1000 cycles, so speedup = 1000/204 ~= 4.9x -- close to but always under the naive 5x, because the 4-cycle fill overhead is a fixed cost that pipelining can't eliminate. Exactly 5x would require zero fill overhead, which no real pipeline has.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}