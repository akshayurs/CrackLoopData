{
  "topicId": "multi-cycle-datapath",
  "mcqs": [
    {
      "id": "multi-cycle-datapath-s01-q1",
      "slideId": "multi-cycle-datapath-s01",
      "question": "What does splitting instruction execution into multiple shorter cycles allow a multi-cycle datapath to do that single-cycle can't?",
      "options": [
        "Execute two instructions simultaneously on the same hardware",
        "Reuse the same ALU and memory port across different steps of an instruction, instead of needing duplicate hardware",
        "Skip the decode stage for simple instructions entirely",
        "Run at a slower overall clock frequency with no other benefit"
      ],
      "correctIndex": 1,
      "explanation": "Because steps are separated in time, one ALU can serve both address calculation and arithmetic, and one memory port can serve both fetch and data access. This isn't simultaneous execution of two instructions (that's pipelining/superscalar), decode still happens for every instruction, and the design's benefit is hardware reuse, not merely a slower clock.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "multi-cycle-datapath-s03-q1",
      "slideId": "multi-cycle-datapath-s03",
      "question": "Why does a register-register add typically take fewer cycles than a load in a multi-cycle datapath?",
      "options": [
        "The add instruction skips the memory-access cycle since it doesn't need to touch memory",
        "The ALU runs faster specifically for add instructions",
        "Loads are always executed twice for correctness",
        "The register file is slower to access than memory"
      ],
      "correctIndex": 0,
      "explanation": "Add has no memory step, so its cycle count is naturally shorter (e.g., 4 vs 5 cycles) — the core efficiency win multi-cycle design recovers. The ALU's speed doesn't vary by instruction type, loads aren't executed twice, and register file access is generally faster than memory, not slower.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "multi-cycle-datapath-s04-q1",
      "slideId": "multi-cycle-datapath-s04",
      "question": "What determines the exact sequence of states an FSM controller steps through for a given instruction?",
      "options": [
        "The clock frequency of the CPU",
        "The instruction type being processed, since different instructions need different steps",
        "A random selection made each cycle",
        "The amount of available cache memory"
      ],
      "correctIndex": 1,
      "explanation": "The FSM's state sequence depends on the instruction currently in progress — a load takes a different path through states than an add. Clock frequency affects timing, not the sequence itself, the sequence is deterministic (not random), and cache size is unrelated to FSM state transitions.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "multi-cycle-datapath-s05-q1",
      "slideId": "multi-cycle-datapath-s05",
      "question": "What's the key difference between multi-cycle design and pipelining, given both aim to improve on single-cycle inefficiency?",
      "options": [
        "Multi-cycle overlaps multiple instructions; pipelining processes one instruction at a time",
        "Multi-cycle processes one instruction at a time across several cycles; pipelining overlaps multiple instructions in different stages simultaneously",
        "They are functionally identical approaches with different names",
        "Pipelining shares hardware across steps, while multi-cycle duplicates it per stage"
      ],
      "correctIndex": 1,
      "explanation": "Multi-cycle still finishes one instruction before meaningfully starting the next's steps; pipelining keeps several instructions in flight at once across stages. They aren't identical (throughput differs substantially), and it's multi-cycle that reuses shared hardware, while pipelining typically duplicates resources per stage — the reverse of what's suggested.",
      "difficulty": "hard",
      "level": "advanced"
    }
  ]
}
