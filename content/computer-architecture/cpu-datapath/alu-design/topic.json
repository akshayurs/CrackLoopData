{
  "id": "alu-design",
  "title": "ALU Design",
  "area": "computer-architecture",
  "group": "cpu-datapath",
  "order": 5,
  "slug": "alu-design",
  "summary": "Building the arithmetic-logic unit and the flags every downstream branch and overflow check depends on.",
  "level": "intermediate",
  "slideCount": 6,
  "estReadMinutes": 5,
  "slides": [
    {
      "id": "alu-design-s01",
      "order": 1,
      "sectionTitle": "The ALU's job",
      "subTitle": null,
      "type": "concept",
      "markdown": "The **Arithmetic Logic Unit (ALU)** is the component that actually computes — addition, subtraction, AND, OR, XOR, shifts, comparisons — selected by a **function code** the control unit derives from the opcode.\n\nStructurally, it's just combinational logic: given two operands and a function-select signal, it produces a result (and, importantly, a set of status flags) with no memory of past operations. Every arithmetic and logic instruction in the ISA ultimately routes through this one shared block.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 38,
      "mcqIds": ["alu-design-s01-q1"]
    },
    {
      "id": "alu-design-s02",
      "order": 2,
      "sectionTitle": "A simple ALU",
      "subTitle": null,
      "type": "diagram",
      "markdown": "At its simplest, an ALU is an adder plus a handful of logic gates, with a **mux selecting which result to output**:\n\n![A simple ALU: adder and logic gates feeding a mux selected by a function code](assets/simple-alu.svg)\n\nEvery operation — add, and, or — computes in parallel, and the function-select signal simply picks which computed result actually reaches the output. Wasteful in one sense (unused results are thrown away every cycle) but simple and fast, since nothing has to wait for a decision before computing.",
      "assets": [
        { "alt": "A simple ALU: adder and logic gates feeding a mux selected by a function code", "path": "assets/simple-alu.svg" }
      ],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 30,
      "mcqIds": []
    },
    {
      "id": "alu-design-s03",
      "order": 3,
      "sectionTitle": "Flag generation",
      "subTitle": null,
      "type": "concept",
      "markdown": "Alongside its numeric result, the ALU sets **condition flags**, side effects that later instructions read:\n\n- **Zero** — set when the result is exactly 0; a conditional branch on equality reads this.\n- **Negative** — set to the result's sign bit; feeds signed comparisons.\n- **Carry** — set when unsigned addition overflows the register width; used for multi-word arithmetic and unsigned overflow checks.\n- **Overflow** — set when signed addition/subtraction produces a mathematically wrong sign; feeds signed-overflow detection.\n\nThese flags are exactly how a comparison instruction \"talks to\" the branch that follows it — no separate channel, just shared flag bits.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 40,
      "mcqIds": ["alu-design-s03-q1"]
    },
    {
      "id": "alu-design-s04",
      "order": 4,
      "sectionTitle": "Subtraction from addition",
      "subTitle": null,
      "type": "concept",
      "markdown": "The ALU doesn't need a separate subtractor circuit at all — **two's complement** makes `a - b` computable as `a + (~b + 1)`: invert `b`'s bits and add 1, then feed both into the *same* adder used for regular addition.\n\nThis is a direct hardware payoff of the two's complement representation ([signed-integer-representation](signed-integer-representation)): one adder plus a small inverter and a \"+1\" carry-in handles both operations, instead of building and wiring two separate circuits.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 38,
      "mcqIds": ["alu-design-s04-q1"]
    },
    {
      "id": "alu-design-s05",
      "order": 5,
      "sectionTitle": "Adder design in the critical path",
      "subTitle": null,
      "type": "compare",
      "markdown": "The adder inside the ALU sits on the CPU's **critical path** — the longest chain of logic the clock period must accommodate — so its internal design matters a lot:\n\n| | Ripple-carry adder | Carry-lookahead adder |\n|---|---|---|\n| Mechanism | Each bit's carry waits for the previous bit's carry | Carries computed in parallel from all input bits directly |\n| Latency | Grows linearly with bit width — slow for 64-bit | Much lower, roughly logarithmic — but more gates |\n| Hardware cost | Simple, small | More complex, larger |\n\nReal CPUs use carry-lookahead (or hybrid) adders specifically because a slow adder directly caps the maximum clock speed — this is the ALU showing up as a first-order performance bottleneck, not an implementation detail.",
      "assets": [],
      "hasCode": false,
      "hasTable": true,
      "estReadSeconds": 40,
      "mcqIds": ["alu-design-s05-q1"]
    },
    {
      "id": "alu-design-s06",
      "order": 6,
      "sectionTitle": "Composing an operation table",
      "subTitle": null,
      "type": "code",
      "markdown": "A minimal ALU's function-select table, mapping a 3-bit selector to an operation:\n\n```\nALUOp   Operation           Notes\n000     A + B               ADD\n001     A + (~B + 1)        SUB (via two's complement)\n010     A AND B             bitwise AND\n011     A OR B              bitwise OR\n101     (A - B) < 0 ? 1 : 0 SLT (set-less-than, uses subtractor + sign bit)\n```\n\nEvery real instruction's arithmetic (`add`, `sub`, `and`, `or`, `slt`) is just \"pick the right 3-bit selector\" — the control unit's job is exactly generating this selector from the opcode.",
      "assets": [],
      "hasCode": true,
      "hasTable": false,
      "estReadSeconds": 42,
      "mcqIds": []
    }
  ]
}
