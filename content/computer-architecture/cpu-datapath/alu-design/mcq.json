{
  "topicId": "alu-design",
  "mcqs": [
    {
      "id": "alu-design-s01-q1",
      "slideId": "alu-design-s01",
      "question": "What determines which operation an ALU performs on a given cycle?",
      "options": [
        "A function-select signal derived from the instruction's opcode",
        "The clock frequency of the CPU",
        "The contents of the data memory",
        "The number of pipeline stages in the CPU"
      ],
      "correctIndex": 0,
      "explanation": "The control unit decodes the opcode into a function-select signal that tells the ALU which operation (add, and, or, etc.) to perform. Clock frequency affects timing, not which operation runs, data memory contents are unrelated to ALU operation selection, and pipeline stage count doesn't determine a single ALU's function select.",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "alu-design-s03-q1",
      "slideId": "alu-design-s03",
      "question": "How does a conditional branch instruction 'know' the result of a preceding comparison?",
      "options": [
        "It re-executes the comparison itself before branching",
        "It reads condition flags (zero, negative, carry, overflow) that the ALU set as a side effect of the comparison",
        "The compiler embeds the comparison result directly into the branch instruction's immediate field",
        "It queries the operating system for the result"
      ],
      "correctIndex": 1,
      "explanation": "Flags are the shared channel connecting a comparison's ALU operation to the branch that follows it — no re-execution or OS involvement needed. The compiler can't embed a run-time result into a static immediate field, since the comparison's outcome isn't known until execution.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "alu-design-s04-q1",
      "slideId": "alu-design-s04",
      "question": "Why doesn't an ALU need a completely separate subtractor circuit alongside its adder?",
      "options": [
        "Subtraction is never actually implemented in hardware",
        "The ALU performs subtraction using a completely different, unrelated circuit each time",
        "Two's complement lets a - b be computed as a + (~b + 1), reusing the same adder with an inverter and a set carry-in",
        "Subtraction is handled entirely in software, not hardware"
      ],
      "correctIndex": 2,
      "explanation": "Two's complement representation makes subtraction expressible as an addition after bit-inversion and a +1, letting the same adder circuit handle both. Subtraction is genuinely implemented in hardware (not skipped or software-only), and it reuses the adder rather than needing a wholly separate circuit.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "alu-design-s05-q1",
      "slideId": "alu-design-s05",
      "question": "Why do most modern CPUs use carry-lookahead (or similar) adders instead of simple ripple-carry adders?",
      "options": [
        "Ripple-carry adders cannot add numbers wider than 8 bits",
        "Ripple-carry adders produce incorrect results for negative numbers",
        "Carry-lookahead adders use fewer gates than ripple-carry adders",
        "Ripple-carry adder latency grows linearly with bit width, directly limiting maximum clock speed for wide operands"
      ],
      "correctIndex": 3,
      "explanation": "The adder sits on the critical path, so a ripple-carry adder's linear latency growth (worse for 64-bit than 8-bit) becomes a real clock-speed bottleneck. Ripple-carry can add any bit width (just slower), carry-lookahead uses more gates, not fewer, and ripple-carry produces correct results for negative numbers under two's complement — it's just slower, not wrong.",
      "difficulty": "hard",
      "level": "intermediate"
    },
    {
      "id": "alu-design-s07-q1",
      "slideId": "alu-design-s07",
      "question": "Adding two 4-bit signed numbers 0111 (+7) and 0001 (+1) produces the bit pattern 1000, which is -8 as a signed value — clearly wrong. What hardware condition flags this as a signed overflow?",
      "options": [
        "The carry-out of the most significant bit alone being 1",
        "The carry into the most significant bit differing from the carry out of the most significant bit",
        "The result being negative in two's complement, on its own",
        "Both operands having their sign bit set to 0"
      ],
      "correctIndex": 1,
      "explanation": "Signed overflow is detected by XORing the carry into the sign bit with the carry out of the sign bit — in this example, 1 XOR 0 = 1, correctly flagging overflow. Carry-out alone doesn't distinguish signed from unsigned overflow, a negative result alone doesn't mean overflow occurred, and checking only the operands' sign bits misses this specific same-sign-in-different-sign-out case.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "alu-design-s09-q1",
      "slideId": "alu-design-s09",
      "question": "You're comparing two signed integers and need to know if the subtraction overflowed. Which flag should you check?",
      "options": [
        "The overflow flag, since carry and overflow track different things and only overflow reflects signed-arithmetic correctness",
        "The carry flag, since it applies to both signed and unsigned arithmetic identically",
        "Either flag works interchangeably for this case",
        "Neither flag is relevant to subtraction, only to addition"
      ],
      "correctIndex": 0,
      "explanation": "Carry tracks unsigned overflow, overflow tracks signed overflow — for signed data you need the overflow flag specifically, and the two flags can disagree on the same operation. They are not interchangeable, and subtraction (implemented via addition of the negation) sets both flags exactly like addition does.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
