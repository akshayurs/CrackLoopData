{
  "topicId": "amdahls-law-fundamentals",
  "mcqs": [
    {
      "id": "amdahls-law-fundamentals-s02-q1",
      "slideId": "amdahls-law-fundamentals-s02",
      "question": "In Amdahl's Law formula, speedup = 1 / ((1-p) + p/s), what happens to the (1-p) term as more processors are added?",
      "options": [
        "It stays exactly the same, since it represents work that can't be sped up at all",
        "It shrinks proportionally with processor count",
        "It grows, because more processors add coordination overhead to the serial term",
        "It disappears entirely once s exceeds 2"
      ],
      "correctIndex": 0,
      "explanation": "(1-p) represents the strictly serial portion of the work, which by definition doesn't benefit from more processors — it's a fixed cost regardless of s. It doesn't shrink, doesn't grow with processor count in the basic formula, and never vanishes at any value of s.",
      "difficulty": "easy",
      "level": "intermediate"
    },
    {
      "id": "amdahls-law-fundamentals-s04-q1",
      "slideId": "amdahls-law-fundamentals-s04",
      "question": "A program is 50% parallelizable (p=0.5) and gets an ideal 4x speedup (s=4) on that portion. What is the overall speedup?",
      "options": [
        "4x",
        "1.6x",
        "2x",
        "1x"
      ],
      "correctIndex": 1,
      "explanation": "speedup = 1 / ((1-0.5) + 0.5/4) = 1 / (0.5 + 0.125) = 1 / 0.625 = 1.6x. The large serial half caps the achievable speedup well below the 4x applied to the parallel portion alone.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "amdahls-law-fundamentals-s05-q1",
      "slideId": "amdahls-law-fundamentals-s05",
      "question": "A program has a serial fraction of 25% (p=0.75 parallelizable). What is the maximum possible speedup, even with infinite processors?",
      "options": [
        "Unbounded — infinite processors mean unbounded speedup",
        "0.75x",
        "4x, since 1/(1-0.75) = 1/0.25 = 4",
        "25x"
      ],
      "correctIndex": 2,
      "explanation": "As s approaches infinity, speedup approaches 1/(1-p) = 1/0.25 = 4x — a hard ceiling regardless of processor count. Speedup is never unbounded when any serial fraction exists at all, however small.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "amdahls-law-fundamentals-s07-q1",
      "slideId": "amdahls-law-fundamentals-s07",
      "question": "Why does using a static count of \"parallelizable instructions\" as p often overestimate real-world speedup?",
      "options": [
        "Because instruction counts are always measured incorrectly by profilers",
        "Because compilers always parallelize more instructions than a static count predicts",
        "Because Amdahl's Law only applies to single-threaded programs",
        "Because it omits synchronization, communication, and coordination overhead introduced by parallelizing, which behaves as additional serial-equivalent cost"
      ],
      "correctIndex": 3,
      "explanation": "Real parallel execution introduces overhead — locks, barriers, data movement between processors — that a static instruction count never sees, and that overhead behaves exactly like serial time in Amdahl's formula. It's not a profiler-measurement error, and the law is specifically about parallel vs serial execution, so it doesn't only apply to single-threaded code.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "amdahls-law-fundamentals-s08-q1",
      "slideId": "amdahls-law-fundamentals-s08",
      "question": "With p = 0.95, going from 64 to 1024 processors (16x more hardware) only raises speedup from about 15.4x to 19.6x. What does this show?",
      "options": [
        "Returns on additional processors shrink sharply as speedup approaches the 1/(1-p) ceiling, here 20x, regardless of how much more hardware is added",
        "The formula must be wrong, since more processors should always yield proportionally more speedup",
        "This workload has no serial fraction at 1024 processors",
        "Doubling processor count always doubles speedup regardless of p"
      ],
      "correctIndex": 0,
      "explanation": "As s grows large, speedup approaches 1/(1-p) asymptotically -- here 20x -- so each additional processor contributes less and less once already close to that ceiling. This is exactly the diminishing-returns behavior Amdahl's Law predicts, not a formula error or proportional scaling.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "amdahls-law-fundamentals-s09-q1",
      "slideId": "amdahls-law-fundamentals-s09",
      "question": "Why does reducing lock contention in a real multithreaded system count as a high-leverage performance change under Amdahl's Law?",
      "options": [
        "It only speeds up the parallel portion, which was already scaling well",
        "It directly shrinks the serial fraction (1-p), which raises the speedup ceiling every other optimization is bounded by",
        "Lock contention has no relationship to the serial fraction in Amdahl's Law",
        "It increases the number of available processors automatically"
      ],
      "correctIndex": 1,
      "explanation": "In practice, the serial fraction is often exactly the time threads spend blocked or contended -- on locks, barriers, or single-writer structures. Shrinking that time directly reduces (1-p), which raises the ceiling of 1/(1-p) that bounds every other optimization, making it more valuable than speeding up work that already scales.",
      "difficulty": "medium",
      "level": "advanced"
    }
  ]
}