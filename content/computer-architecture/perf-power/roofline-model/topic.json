{
  "id": "roofline-model",
  "title": "The Roofline Model",
  "area": "computer-architecture",
  "group": "perf-power",
  "order": 3,
  "slug": "roofline-model",
  "summary": "Diagnosing whether a workload is compute-bound or memory-bound before spending time optimizing it.",
  "level": "advanced",
  "slideCount": 7,
  "estReadMinutes": 7,
  "slides": [
    {
      "id": "roofline-model-s01",
      "order": 1,
      "sectionTitle": "The question it answers",
      "subTitle": "Compute-bound or memory-bound?",
      "type": "concept",
      "markdown": "Before optimizing a piece of numerical code, you need to know **what's actually limiting it**: is the CPU/GPU's raw compute throughput the bottleneck, or is it waiting on data to arrive from memory? Optimizing the wrong one wastes effort entirely.\n\nThe **roofline model** answers this visually — it plots a workload's achievable performance against a hardware's compute and memory-bandwidth limits, making the bottleneck a matter of \"which line is this workload sitting under\" rather than guesswork.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 28,
      "mcqIds": []
    },
    {
      "id": "roofline-model-s02",
      "order": 2,
      "sectionTitle": "Arithmetic intensity",
      "subTitle": "FLOPs per byte moved",
      "type": "concept",
      "markdown": "**Arithmetic intensity** = FLOPs performed ÷ bytes moved from memory. It's the single number that places a workload on the roofline plot's x-axis.\n\nA workload doing a huge amount of computation per byte fetched (high intensity) can afford to be limited by compute throughput, since memory traffic is comparatively small. A workload that fetches a lot of data but does little math per byte (low intensity) is far more likely to be capped by how fast bytes can arrive, not by how fast the ALU can crunch them.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 28,
      "mcqIds": ["roofline-model-s02-q1"]
    },
    {
      "id": "roofline-model-s03",
      "order": 3,
      "sectionTitle": "The plot",
      "subTitle": "Two ceilings, one graph",
      "type": "diagram",
      "markdown": "The roofline overlays two hardware limits on one chart.\n\n![A roofline plot with a compute ceiling and a memory-bandwidth slope, with a workload point placed](assets/roofline-plot.svg)\n\nThe flat line is peak compute throughput — a ceiling independent of intensity. The diagonal line is peak memory bandwidth translated into achievable FLOPs — it rises with intensity, since more compute per byte means bandwidth constrains you less.",
      "assets": [{"alt": "A roofline plot with a compute ceiling and a memory-bandwidth slope, with a workload point placed", "path": "assets/roofline-plot.svg"}],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 28,
      "mcqIds": []
    },
    {
      "id": "roofline-model-s04",
      "order": 4,
      "sectionTitle": "Reading your workload's point",
      "subTitle": "Under the slope, or under the flat line",
      "type": "concept",
      "markdown": "Plot your workload's arithmetic intensity and achieved performance as one point. Under the **diagonal** (memory-bandwidth) line: **memory-bound** — it hits the bandwidth ceiling before the compute ceiling. Under the **flat** (compute) line at high intensity: **compute-bound** — bandwidth was never the constraint.\n\nThe point's horizontal position (intensity) is a property of the algorithm; its vertical position is what you're trying to raise — the ceiling directly above it tells you which lever to pull.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 30,
      "mcqIds": ["roofline-model-s04-q1"]
    },
    {
      "id": "roofline-model-s05",
      "order": 5,
      "sectionTitle": "What each region implies",
      "subTitle": "Different regions, different fixes",
      "type": "concept",
      "markdown": "If you're **memory-bound**, adding more compute throughput (wider vector units, more cores) does nothing — the bottleneck is bytes per second, not FLOPs per second. You need to move less data, or move it more efficiently (better locality, compression, fusing operations to reuse data already in registers/cache).\n\nIf you're **compute-bound**, more memory bandwidth is wasted money — you need more raw compute throughput, or a way to do the same math in fewer operations (algorithmic improvement, better use of SIMD/vector units).",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 30,
      "mcqIds": []
    },
    {
      "id": "roofline-model-s06",
      "order": 6,
      "sectionTitle": "Computing arithmetic intensity",
      "subTitle": "A dot product",
      "type": "code",
      "markdown": "```text\nDot product of two length-N float32 vectors:\n  FLOPs   = N multiplies + N adds = 2N\n  Bytes   = 2 vectors x N elements x 4 bytes = 8N\n\nArithmetic intensity = 2N / 8N = 0.25 FLOPs/byte\n```\n\nThat's a very low intensity — a dot product does almost no math relative to the data it streams through, which is exactly why it's a textbook memory-bound kernel: no amount of extra compute throughput speeds it up until memory bandwidth is addressed first.",
      "assets": [],
      "hasCode": true,
      "hasTable": false,
      "estReadSeconds": 30,
      "mcqIds": ["roofline-model-s06-q1"]
    },
    {
      "id": "roofline-model-s07",
      "order": 7,
      "sectionTitle": "Optimizing each kind",
      "subTitle": "Memory-bound vs compute-bound fixes",
      "type": "compare",
      "markdown": "| Bound by | Example fix | Why it works |\n|---|---|---|\n| Memory bandwidth | Cache blocking/tiling (cross-link: cache-conscious-programming) | Reuses data already fetched instead of re-streaming it from memory |\n| Compute throughput | Vectorization / SIMD (cross-link: multicore -> simd-and-vector-processing) | Does more arithmetic per instruction issued, raising the achievable-FLOPs ceiling |\n\n**How to decide:** the roofline point tells you which fix applies — cache blocking helps a memory-bound point by effectively raising its arithmetic intensity (same FLOPs, fewer bytes actually fetched from memory), while vectorization helps a compute-bound point by raising the compute ceiling itself. Applying the wrong one (e.g. vectorizing a memory-bound kernel) buys little to nothing.",
      "assets": [],
      "hasCode": false,
      "hasTable": true,
      "estReadSeconds": 32,
      "mcqIds": ["roofline-model-s07-q1"]
    }
  ]
}
