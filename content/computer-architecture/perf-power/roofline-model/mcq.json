{
  "topicId": "roofline-model",
  "mcqs": [
    {
      "id": "roofline-model-s02-q1",
      "slideId": "roofline-model-s02",
      "question": "What does a low arithmetic intensity (FLOPs per byte) suggest about a workload?",
      "options": [
        "It's almost certainly compute-bound",
        "It's more likely to be memory-bound, since it does relatively little math per byte fetched",
        "It has no bearing on whether the workload is compute- or memory-bound",
        "It means the workload has a bug"
      ],
      "correctIndex": 1,
      "explanation": "Low arithmetic intensity means lots of data movement relative to computation, which pushes the workload's roofline point toward the memory-bandwidth-limited region. It's a direct, meaningful signal about the likely bottleneck, not noise or a sign of a defect.",
      "difficulty": "easy",
      "level": "advanced"
    },
    {
      "id": "roofline-model-s04-q1",
      "slideId": "roofline-model-s04",
      "question": "On a roofline plot, a workload's point sits directly under the diagonal (memory-bandwidth) line, well below the flat compute-ceiling line. What does this indicate?",
      "options": [
        "The workload is compute-bound and needs more ALU throughput",
        "The workload is memory-bound — it's constrained by data movement, not by compute capacity",
        "The workload has zero arithmetic intensity",
        "The hardware has no memory bandwidth limit at all"
      ],
      "correctIndex": 1,
      "explanation": "Sitting under the memory-bandwidth slope (rather than up near the compute ceiling) is exactly the signature of a memory-bound workload — bandwidth is the binding constraint. It isn't necessarily zero intensity (that would place it at the origin), and the plot's slope existing at all confirms the hardware does have a finite bandwidth limit.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "roofline-model-s06-q1",
      "slideId": "roofline-model-s06",
      "question": "A dot product of two N-element float32 vectors performs 2N FLOPs and moves 8N bytes. What is its arithmetic intensity, and what does that value typically imply?",
      "options": [
        "4 FLOPs/byte — typically compute-bound",
        "0.25 FLOPs/byte — typically memory-bound",
        "2 FLOPs/byte — typically balanced",
        "8 FLOPs/byte — always compute-bound regardless of hardware"
      ],
      "correctIndex": 1,
      "explanation": "2N / 8N = 0.25 FLOPs/byte, a low value indicating very little computation relative to data moved — the textbook signature of a memory-bound kernel. It is not compute-bound, balanced, or a value that guarantees any bound \"always\" regardless of the actual hardware's ceilings, though 0.25 is low enough to be memory-bound on virtually every real system.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "roofline-model-s07-q1",
      "slideId": "roofline-model-s07",
      "question": "A workload is diagnosed as memory-bound on the roofline plot. Why does adding wider vector (SIMD) units fail to speed it up?",
      "options": [
        "SIMD units only work on compute-bound workloads by design",
        "Wider vector units raise the compute ceiling, but the workload is already limited by memory bandwidth, which those units don't change",
        "SIMD units actually slow down memory-bound workloads",
        "Memory-bound workloads cannot be run on any hardware with vector units"
      ],
      "correctIndex": 1,
      "explanation": "The roofline point for a memory-bound workload sits under the bandwidth slope, not near the compute ceiling — so raising the compute ceiling (more/wider ALUs) doesn't move the achievable performance at all, since bandwidth was the binding constraint the whole time. SIMD isn't restricted to compute-bound code by design, doesn't inherently slow anything down, and memory-bound code runs fine on vector-capable hardware — it just doesn't benefit from the vector width.",
      "difficulty": "hard",
      "level": "advanced"
    }
  ]
}
