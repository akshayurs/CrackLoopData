{
  "topicId": "ssd-and-flash-internals",
  "interviewQuestions": [
    {
      "id": "ssd-and-flash-internals-iq1",
      "question": "Why can't an SSD simply overwrite a page of data the way you'd overwrite a value in RAM?",
      "answerMarkdown": "I'd start with the physical mechanism: a NAND flash cell stores data as trapped electric charge on a floating gate. Adding charge to a cell is straightforward and can be done at page granularity, but clearing charge back to the erased state requires a much higher voltage applied to an entire **block** — not a single page. A block holds many pages, often 128 or more.\n\nSo if you want to change a bit that's already been written, you can't just flip it — the entire containing block has to be erased first, which would destroy every other page in that block too. That's why SSDs never actually overwrite in place: instead, the controller writes the new version of the data to a different, already-erased page, marks the old page as stale, and reclaims the stale space later through garbage collection.\n\nI'd close by noting this is the root cause of write amplification and the whole flash translation layer's existence — it all traces back to this one physical asymmetry between writing and erasing.\n\n**Testing:** whether you understand the physical root cause deeply enough to connect it to downstream concepts like write amplification and garbage collection, not just recite 'you can't overwrite flash.'",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Why flash can't overwrite in place"
    },
    {
      "id": "ssd-and-flash-internals-iq2",
      "question": "What's the difference between SLC, MLC, TLC, and QLC flash, and why does it matter when choosing storage?",
      "answerMarkdown": "I'd explain that the letters refer to how many bits each flash cell stores by distinguishing finer gradations of trapped charge: SLC stores 1 bit per cell, MLC 2, TLC 3, and QLC 4. More bits per cell means more capacity packed into the same physical silicon, which directly lowers cost per gigabyte.\n\nThe trade-off is endurance and speed: distinguishing four charge levels (QLC) instead of two (SLC) requires finer-grained sensing, which is slower and more error-prone as cells wear, so QLC tolerates far fewer program/erase cycles before failing — commonly hundreds to low thousands of writes per cell, versus tens of thousands for SLC.\n\nFor a concrete recommendation: a consumer laptop doing mostly reads and occasional writes is a great fit for TLC/QLC's cost advantage; a database server doing constant heavy random writes benefits from SLC/MLC's endurance, or at minimum needs enough over-provisioning and wear leveling to compensate.\n\n**Testing:** whether you can connect a hardware spec (bits per cell) to a real workload decision, not just list the acronyms.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Flash cell types and endurance trade-offs"
    }
  ]
}
