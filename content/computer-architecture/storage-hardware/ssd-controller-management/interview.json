{
  "topicId": "ssd-controller-management",
  "interviewQuestions": [
    {
      "id": "ssd-controller-management-iq1",
      "question": "What is write amplification, and why does it matter for both SSD performance and lifespan?",
      "answerMarkdown": "I'd define it as the ratio of physical writes the SSD actually performs internally to the logical writes the host requested. It exists because flash can't overwrite in place: a logical overwrite gets redirected to a fresh page, the old page is marked stale, and garbage collection later relocates any still-live neighbors before erasing that block — all of that relocation counts as extra physical writes the host never asked for.\n\nThe performance angle: under sustained random writes, the SSD may be doing several times more physical I/O than the host thinks it's issuing, which shows up as degraded sustained write throughput compared to burst performance. The lifespan angle: every physical write consumes one of a flash cell's finite erase cycles, so a write amplification factor of 5 effectively divides the drive's usable lifetime by 5 for that workload.\n\nConcretely, this is why enterprise SSDs are often shipped with significant **over-provisioning** — extra spare flash the host never sees — specifically to give the garbage collector more room to work with and keep write amplification low.\n\n**Testing:** whether you understand write amplification as a consequence of the erase-before-write constraint, and can connect it to real operational decisions like over-provisioning.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Write amplification, cause and consequence"
    },
    {
      "id": "ssd-controller-management-iq2",
      "question": "Why does an SSD need both garbage collection and wear leveling — aren't they solving the same problem?",
      "answerMarkdown": "I'd separate the two by the problem each actually solves. Garbage collection reclaims **space**: it finds blocks with a mix of live and stale pages, relocates the live pages elsewhere, and erases the block so it can be reused — without this, the SSD would eventually run out of writable space even though much of the data on it is logically deleted.\n\nWear leveling manages **durability**: since each cell only survives a finite number of erase cycles, wear leveling makes sure erases are spread evenly across all physical blocks, so no single 'hot' block wears out and fails long before the rest of the drive. Without it, a naive FTL that always reused the most convenient free blocks would create failure hotspots.\n\nThey do interact, though — garbage collection's relocation naturally moves data around, which wear leveling can use as an opportunity to also rebalance wear, sometimes even relocating cold, rarely-changed data purely to free up an over-worn block. So they're complementary: GC keeps space usable, wear leveling keeps that usable space alive for as long as possible.\n\n**Testing:** whether you can distinguish two related-but-distinct FTL responsibilities instead of conflating them into one vague 'flash management' bucket.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Garbage collection vs wear leveling"
    },
    {
      "id": "ssd-controller-management-iq3",
      "question": "What is TRIM, and why does the SSD controller need the operating system to explicitly tell it about deleted files?",
      "answerMarkdown": "I'd start from the gap TRIM exists to close: when you delete a file, the OS only updates its own filesystem metadata (marking those blocks free in its allocation table) — it doesn't touch the actual data on disk at all, since erasing it immediately would be wasted work if that space just gets reused for something else soon. On a hard disk this is harmless, because overwriting a 'deleted' sector later works exactly like any other write.\n\nOn an SSD, this creates a real problem: the SSD controller has no visibility into filesystem-level concepts like 'deleted' at all — from its perspective, every page it ever wrote is still valid data, forever, because it only sees raw logical block addresses, not files. So the garbage collector treats those logically-deleted-but-never-told-about pages as live data, relocating them during garbage collection just like real live data — wasted work that also inflates write amplification and consumes spare capacity for no benefit.\n\nTRIM is the command that closes this gap: the OS explicitly tells the SSD controller 'these logical blocks no longer hold meaningful data,' letting the controller immediately mark the corresponding physical pages as stale — safe to skip during the next garbage collection pass — well before that space would otherwise get reused. Concrete consequence: an SSD used without TRIM support (an older OS, or a drive behind a RAID controller that doesn't pass TRIM through) gradually suffers rising write amplification and degraded sustained write performance as more and more 'dead' pages accumulate that garbage collection doesn't know it can skip.\n\n**Testing:** understanding TRIM as bridging a genuine visibility gap between filesystem semantics and the FTL's raw block view, not just 'a command that speeds things up.'",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "What TRIM solves and why it's needed"
    }
  ]
}
