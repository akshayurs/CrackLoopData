{
  "topicId": "nvme-and-storage-protocols",
  "interviewQuestions": [
    {
      "id": "nvme-and-storage-protocols-iq1",
      "question": "Why did the industry move from AHCI/SATA to NVMe instead of just making AHCI faster?",
      "answerMarkdown": "I'd frame it as AHCI hitting a structural, not incremental, limit. AHCI was designed around a single command queue with a depth of 32, matched to an era where a mechanical HDD was always the bottleneck — a spinning disk genuinely could only be doing about one thing at a time, so a single queue was never the constraint. AHCI also carries protocol overhead built for compatibility with that disk-oriented model.\n\nOnce SSDs dropped access latency to microseconds, and started exposing many independent internal flash channels capable of true parallel service, AHCI's single queue became the actual bottleneck — you couldn't submit enough concurrent requests to keep the SSD's internal parallelism busy, no matter how fast the flash itself was. That's a queueing-model limitation, not something you can tune away with faster AHCI hardware.\n\nNVMe was designed from scratch for this reality: it talks to storage over PCIe directly, skips AHCI's disk-era translation layers, and supports up to 65,535 queues with deep queue depths — letting software parallelism finally match the SSD's real internal parallelism.\n\n**Testing:** whether you understand this as a queueing-architecture problem specific to fast, internally-parallel devices, not just 'NVMe is the newer, faster standard.'",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Why AHCI couldn't just be sped up"
    },
    {
      "id": "nvme-and-storage-protocols-iq2",
      "question": "Is NVMe the same thing as PCIe? Can NVMe run over something other than PCIe?",
      "answerMarkdown": "I'd clarify the distinction directly: PCIe is a physical/electrical interconnect standard — lanes carrying data between the CPU and peripherals. NVMe is a protocol — a command set and queueing model for talking to non-volatile storage. NVMe's most common deployment today is over PCIe, via M.2 or U.2 connectors, which is probably why the two get conflated.\n\nBut NVMe isn't defined to require PCIe specifically. **NVMe-oF (NVMe over Fabrics)** carries the same NVMe command set and queueing semantics over a network fabric — Ethernet (via RDMA or TCP), Fibre Channel, or InfiniBand — to talk to storage that isn't locally attached at all, with far lower overhead than older network storage protocols like iSCSI.\n\nSo I'd summarize: PCIe is one transport NVMe can run over, not a synonym for NVMe itself, and NVMe-oF is exactly the proof that the protocol is transport-independent.\n\n**Testing:** whether you can separate protocol from physical transport cleanly, since this distinction comes up directly when discussing networked/disaggregated storage architectures.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "NVMe as a protocol, not a connector"
    },
    {
      "id": "nvme-and-storage-protocols-iq3",
      "question": "For a beginner: what does it actually mean for NVMe to support 'thousands of queues,' and why does that matter for an SSD?",
      "answerMarkdown": "I'd unpack 'queue' concretely: it's a data structure in memory that software fills with pending storage commands (reads, writes) and the drive drains as it completes them, with a matching completion queue the drive fills in and software drains for results. AHCI/SATA had exactly one such command queue, shared by everything on the system that wanted to talk to that drive, with a maximum depth of 32 outstanding commands at a time.\n\nNVMe allows up to 65,535 separate queues, each with up to 65,536 commands outstanding — and critically, each CPU core can be given its **own** queue pair. This matters because a modern SSD's internal flash isn't one monolithic thing — it's built from many independent flash dies and channels that can genuinely service requests in parallel. A single shared queue limits how many requests can be in flight at once, which caps how much of that internal parallelism ever gets exercised, no matter how fast the flash chips themselves are. Giving each core its own queue lets many cores submit and complete requests without contending for a shared queue's lock, and lets the drive keep far more of its internal channels simultaneously busy.\n\nConcrete effect: a database server with 32 cores, each issuing its own I/O against its own NVMe queue, can drive dramatically higher aggregate IOPS than the same drive accessed through one shared 32-deep AHCI queue — not because the flash got faster, but because the software-to-hardware parallelism finally matches what the flash could always do.\n\n**Testing:** connecting the abstract 'many queues' number to a concrete reason it matters — matching software concurrency to the drive's real internal parallelism.",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "What NVMe's many queues actually buy you"
    }
  ]
}
