{
  "id": "monotonic-deque",
  "title": "Monotonic Deque",
  "area": "data-structures-algorithms",
  "group": "stacks-queues",
  "order": 4,
  "slug": "monotonic-deque",
  "summary": "The deque-mechanics half of the monotonic-window pattern — maintaining an ordered invariant while evicting from both ends, the structural piece behind sliding-window maximum.",
  "level": "advanced",
  "slideCount": 8,
  "estReadMinutes": 8,
  "slides": [
    {
      "id": "monotonic-deque-s01",
      "order": 1,
      "sectionTitle": "Snapshot",
      "subTitle": null,
      "type": "overview",
      "markdown": "A monotonic stack only ever evicts from one end. Some problems — most notably sliding-window maximum — need to evict from **both** ends: from the back to maintain monotonic order (same idea as a stack), and from the front to drop elements that have aged out of a moving window.\n\nThat second eviction reason is what a stack alone can't express, and it's exactly what a **deque** (double-ended queue) gives you. This topic covers the deque mechanics themselves — implementing O(1) push/pop from both ends and maintaining the monotonic invariant while doing it. The sliding-window-maximum application that motivates all of this lives in the `two-pointers-sliding-window` group, which owns that specific problem.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 54,
      "mcqIds": []
    },
    {
      "id": "monotonic-deque-s02",
      "order": 2,
      "sectionTitle": "Why some problems need eviction from both ends",
      "subTitle": "The case for a deque over a single-ended stack",
      "type": "concept",
      "markdown": "A monotonic stack answers \"what's the next greater/smaller element,\" which only ever needs to look **backward** from the current position — hence popping only from one end (the top) is enough.\n\nSliding-window maximum needs a different guarantee: \"what's the max over the last k elements,\" where the window itself is moving forward. That requires two independent evictions happening at once: pop from the back whenever a new, bigger element arrives (same monotonic-order reasoning as a stack), **and** pop from the front whenever the window's oldest element falls outside the current k-sized range — a reason to evict that has nothing to do with value comparison at all, purely about position.\n\nA structure that only evicts from one end can express one of these but not both simultaneously — which is exactly the gap a deque fills.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 58,
      "mcqIds": [
        "monotonic-deque-s02-q1"
      ]
    },
    {
      "id": "monotonic-deque-s03",
      "order": 3,
      "sectionTitle": "Implementing a deque with O(1) push/pop from both ends",
      "subTitle": null,
      "type": "code",
      "markdown": "A doubly linked list gives O(1) push/pop at both ends natively — each end just needs a head and tail pointer to rewire. A circular buffer (from the fundamentals topic) also supports this, by allowing both the front and back indices to advance or retreat modulo capacity.\n\n```python\nfrom collections import deque\n\nd = deque()\nd.append(x)       # push back, O(1)\nd.appendleft(x)    # push front, O(1)\nd.pop()            # pop back, O(1)\nd.popleft()        # pop front, O(1)\n```\n\nMost languages provide this as a library type precisely because implementing it correctly by hand (whichever backing structure you choose) is fiddly enough that it's rarely worth hand-rolling outside of an interview specifically asking you to build one.",
      "assets": [],
      "hasCode": true,
      "hasTable": false,
      "estReadSeconds": 55,
      "mcqIds": []
    },
    {
      "id": "monotonic-deque-s04",
      "order": 4,
      "sectionTitle": "A monotonic deque shrinking from both ends",
      "subTitle": null,
      "type": "diagram",
      "markdown": "![A sliding window over an array with a deque below it, showing a large arriving value popping smaller entries off the back, separately from the window's edge evicting the front by position](assets/monotonic-deque-two-evictions.svg)",
      "assets": [
        {
          "alt": "A sliding window over an array with a deque below it, showing a large arriving value popping smaller entries off the back, separately from the window's edge evicting the front by position",
          "path": "assets/monotonic-deque-two-evictions.svg"
        }
      ],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 44,
      "mcqIds": []
    },
    {
      "id": "monotonic-deque-s05",
      "order": 5,
      "sectionTitle": "The front answers the query, the back maintains the invariant",
      "subTitle": "Monotonic deque as a generalization",
      "type": "concept",
      "markdown": "The clean mental model for any monotonic-deque problem: the **front** of the deque always holds the current answer (e.g., the max of the current window), and every operation on the **back** exists purely to maintain the invariant that makes that true.\n\nWhen a new element arrives, pop from the back while it's smaller than the incoming element — those popped elements can never become the max of any future window that still includes the incoming element, since the incoming element is both bigger and will outlast them positionally. Push the new element onto the back. Separately, whenever the window's left boundary moves past the index at the front, pop the front — that index has aged out and can't be anyone's answer anymore, regardless of its value.\n\nThis two-part discipline — back eviction for value, front eviction for position — generalizes beyond sliding-window maximum to any \"best answer over a moving window\" problem.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 60,
      "mcqIds": [
        "monotonic-deque-s05-q1"
      ]
    },
    {
      "id": "monotonic-deque-s06",
      "order": 6,
      "sectionTitle": "Forgetting to evict front entries",
      "subTitle": "That have fallen outside the relevant range",
      "type": "pitfall",
      "markdown": "```python\n# BUG: maintains value-order at the back but never checks\n# whether the front index is still inside the current window\nwhile dq and nums[dq[-1]] < nums[i]:\n    dq.pop()\ndq.append(i)\n# missing: while dq[0] <= i - k: dq.popleft()\n```\n\nWithout the front-eviction check, the deque's front can hold an index that's numerically the largest value seen so far but is no longer inside the current k-sized window — the algorithm would report a max from several windows ago as if it were current. This is the exact failure mode a plain monotonic stack can't have (it never had a \"window\" concept to age out of), and it's the single detail that distinguishes correct monotonic-deque code from code that only got the value-ordering half right.",
      "assets": [],
      "hasCode": true,
      "hasTable": false,
      "estReadSeconds": 56,
      "mcqIds": [
        "monotonic-deque-s06-q1"
      ]
    },
    {
      "id": "monotonic-deque-s07",
      "order": 7,
      "sectionTitle": "Where the sliding-window application lives",
      "subTitle": null,
      "type": "concept",
      "markdown": "Sliding-window maximum (and minimum) — the problem this whole structure exists to serve — is intentionally covered as an application inside the `two-pointers-sliding-window` group, not repeated here. That group owns the window-management half (how the left and right boundaries move, when to shrink, how to combine with other window techniques); this topic owns the deque-mechanics half (the eviction discipline itself).\n\nThe reasoning behind that split: the monotonic-deque *mechanism* is a reusable tool that shows up in more than one place, while the specific sliding-window problem is one particular application of it. Learning the mechanism here first means the application, wherever it's covered, only has to add the window-boundary logic on top of an already-understood eviction discipline.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 52,
      "mcqIds": []
    },
    {
      "id": "monotonic-deque-s08",
      "order": 8,
      "sectionTitle": "Monotonic deque vs monotonic stack",
      "subTitle": "Single-ended eviction vs eviction from both ends",
      "type": "compare",
      "markdown": "| | Monotonic stack | Monotonic deque |\n|---|---|---|\n| Eviction ends | one (the top) | both (front and back independently) |\n| Eviction trigger | value comparison only | value comparison (back) + position/window aging (front) |\n| Typical problem shape | next greater/smaller element | best answer over a moving window |\n| Underlying structure | array or linked list as a stack | doubly linked list or circular buffer as a deque |\n\nEvery monotonic stack is, in a sense, a monotonic deque that never needs front eviction — there's no \"window\" concept aging elements out from the front. The deque is the strict generalization: reach for it the moment a problem introduces a moving window boundary on top of a next-greater/smaller-style comparison, and a plain stack the moment it doesn't.",
      "assets": [],
      "hasCode": false,
      "hasTable": true,
      "estReadSeconds": 56,
      "mcqIds": [
        "monotonic-deque-s08-q1"
      ]
    }
  ]
}
