{
  "topicId": "monotonic-deque",
  "mcqs": [
    {
      "id": "monotonic-deque-s02-q1",
      "slideId": "monotonic-deque-s02",
      "question": "Why can't a plain monotonic stack solve sliding-window maximum, even though it solves next-greater-element fine?",
      "options": [
        "Sliding-window maximum doesn't involve comparisons at all",
        "A stack can't store more than one element at a time",
        "Next-greater-element only needs backward-looking, value-based eviction; sliding-window maximum also needs to evict elements purely because they've aged out of the window's position, which a single-ended stack can't express",
        "Sliding-window maximum requires sorting the array first"
      ],
      "correctIndex": 2,
      "explanation": "A stack's single eviction end works fine when eviction only ever depends on value comparison, as in next-greater-element. Sliding-window maximum adds a second, independent eviction reason — position aging out of the window — which needs eviction from the opposite end, something a one-ended stack has no way to represent. It doesn't require pre-sorting, and a stack certainly can hold multiple elements.",
      "difficulty": "medium",
      "level": "intermediate"
    },
    {
      "id": "monotonic-deque-s05-q1",
      "slideId": "monotonic-deque-s05",
      "question": "In a monotonic deque solving \"max over the last k elements,\" why is popping smaller elements from the back correct even before knowing whether the window will eventually include them?",
      "options": [
        "Because any element smaller than the incoming one can never become the max of a window that still contains the incoming element, since the incoming one is bigger and outlasts it positionally",
        "Because smaller elements are always outside the current window already",
        "Because the deque has a fixed maximum size",
        "Popping smaller elements is only a performance optimization, not required for correctness"
      ],
      "correctIndex": 0,
      "explanation": "As long as the incoming, larger element remains within the window, it will beat any smaller element still present for the max — so those smaller elements can be safely discarded regardless of their position, since they can never win once a bigger, longer-lasting candidate exists. This is required for correctness, not just an optimization, and it isn't about a size cap or about elements already being outside the window.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "monotonic-deque-s06-q1",
      "slideId": "monotonic-deque-s06",
      "question": "A sliding-window-maximum implementation maintains the back-eviction (value) invariant correctly but never checks whether the front index is still inside the window. What happens?",
      "options": [
        "The algorithm crashes immediately",
        "Nothing — the back-eviction alone is sufficient for correctness",
        "The deque's front can report a max from a window several steps in the past, since a large but now out-of-range index never gets removed",
        "The time complexity becomes O(n²) but the answer stays correct"
      ],
      "correctIndex": 2,
      "explanation": "Without checking the front index against the window's left boundary, a numerically large value that has fallen outside the current window can sit at the front indefinitely and get reported as the current window's max — a silent correctness bug, not a crash. Complexity isn't the issue here; the front-eviction check is required for correctness, and skipping it does not degrade gracefully to a merely slower but correct algorithm.",
      "difficulty": "hard",
      "level": "intermediate"
    },
    {
      "id": "monotonic-deque-s08-q1",
      "slideId": "monotonic-deque-s08",
      "question": "In what sense is every monotonic stack \"a monotonic deque that never needs front eviction\"?",
      "options": [
        "Because stacks and deques are implemented identically under the hood",
        "Because a stack problem has no moving-window concept, so there's never a position-based reason to evict from the front — only the value-based back eviction a deque also performs",
        "Because a monotonic stack always has fewer elements than a monotonic deque",
        "Because stacks can be converted to deques by reversing them"
      ],
      "correctIndex": 1,
      "explanation": "The generalization holds because the only difference between the two patterns is the presence of a window that ages elements out from the front — remove that concept and a deque's behavior collapses to exactly a stack's. The two structures aren't implemented the same way internally, element count isn't inherently different between them, and reversal has nothing to do with the relationship.",
      "difficulty": "medium",
      "level": "intermediate"
    }
  ]
}
