{
  "topicId": "elevator-system",
  "interviewQuestions": [
    {
      "id": "elevator-system-iq1",
      "question": "How do you prevent starvation formally?",
      "answerMarkdown": "I'd make the scoring function's cost formula include a wait-time term that grows the longer a request has been pending — something like cost = distance minus k times waitTimeSoFar, so a request that's been waiting long enough eventually wins the assignment regardless of how far it is from the nearest elevator.\n\nThe key design point isn't the exact formula, it's that wait time has to be an explicit input the scorer tracks per request, not something the system reasons about after the fact. I'd store a timestamp on Request at creation and recompute the wait-time term on every scoring pass, so priority naturally increases the longer a request sits unassigned.\n\nI'd close by noting this is a tunable trade-off, not a solved problem: too much weight on wait time and you start ignoring genuinely efficient nearby assignments; the right coefficient is a real parameter to reason about, not a constant to wave away.\n\n**Testing:** whether you can name starvation as a concrete failure mode and fix it with a formal, tunable mechanism rather than a vague 'we'd add fairness somehow.'",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Formally preventing starvation"
    },
    {
      "id": "elevator-system-iq2",
      "question": "What changes under a destination-dispatch panel, where riders enter their floor before boarding?",
      "answerMarkdown": "With a destination-dispatch panel, the rider enters their destination floor before boarding, at a hallway kiosk, instead of pressing an up/down button and choosing a floor once inside. That changes when and how assignment happens: the controller now knows the full origin-and-destination pair at request time, not just direction, and assigns a specific car right away — the kiosk tells the rider which car to board.\n\nI'd model this as Request gaining both floors up front, origin plus destination, instead of arriving as an external call and only later becoming an internal destination once someone's inside. Controller.assignRequest runs the same scoring logic, just with better information — it can deliberately group riders headed to similar floors onto the same car, which is the real efficiency gain destination dispatch is known for in practice.\n\nThe elevator's own state machine and the LOOK-ordering logic inside each car don't change at all; only the point at which routing decisions get made, and how much information is available when they're made, shifts earlier.\n\n**Testing:** whether you see this as richer input to the same scoring and assignment model, not a reason to redesign the whole system.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Destination-dispatch panels"
    },
    {
      "id": "elevator-system-iq3",
      "question": "Walk me through what happens, step by step, when a rider on floor 3 presses 'up' while your three elevators are at floors 1, 5, and 8 — how does the controller decide who gets it?",
      "answerMarkdown": "The request first becomes a `Request(floor=3, direction=UP)` object handed to the `ElevatorController`. The controller doesn't just pick the physically nearest elevator, it scores each elevator based on whether it can serve the request without adding unnecessary direction reversals. The elevator at floor 1 is below floor 3, so if it's idle or already moving up, it can pick up the rider on the way, low cost. The elevator at floor 5 is above floor 3, so serving this request means it has to reverse direction if it's currently going up, or it's already a natural fit if it's heading down through floor 3, the cost depends on its current direction and queue. The elevator at floor 8 is furthest and, if heading up away from floor 3, would need a full reversal, the highest cost of the three.\n\nAssuming all three are idle, the controller scores primarily on distance and picks the elevator at floor 1, distance 2, over the one at floor 5, also distance 2, a tie gets broken by a secondary rule, commonly prefer the elevator already moving in the requested direction, or simple ID order for determinism.\n\nOnce assigned, that elevator's own request queue gets floor 3 inserted in sorted order relative to its current direction, this is the SCAN/LOOK logic, so it picks up floor 3 in its natural sweep rather than as a separate special trip.\n**Testing:** whether you can trace a single request through both layers of the design, controller-level assignment and elevator-level scheduling, concretely, not just describe each layer in isolation.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Tracing a request"
    }
  ]
}
