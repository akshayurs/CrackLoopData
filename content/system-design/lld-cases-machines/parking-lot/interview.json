{
  "topicId": "parking-lot",
  "interviewQuestions": [
    {
      "id": "parking-lot-iq1",
      "question": "How do you handle the lot being full — reject entry or queue?",
      "answerMarkdown": "I'd start by asking what \"full\" means for this lot — no free spot of any type, or no free spot of the *requested* type (a lot can have zero large spots free while compacts sit empty). That distinction changes the check: I'd query the free-spot index by type before opening the entry gate, not after a car is already at the barrier.\n\nThe default behavior I'd propose is reject at the gate: don't open it, show a \"lot full\" sign, and let the driver leave rather than idle inside with nowhere to park. As an extension, I'd add a waiting queue that holds a small number of cars in a holding lane and lets them in as spots free up via the release side of the index — useful for airports with predictable turnover, overkill for a small lot.\n\nI'd close by noting the check has to happen before the gate opens, not after, or you've just moved the queue from the street to the inside of the lot.\n\n**Testing:** whether you check availability at the right point in the flow, and whether you can extend a simple reject into a queue without redesigning the core model.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Handling a full lot"
    },
    {
      "id": "parking-lot-iq2",
      "question": "How would you support monthly subscribers with a dedicated spot?",
      "answerMarkdown": "I'd introduce a `Reservation` entity linking a subscriber to a specific `ParkingSpot` for a date range, and the key design decision is where that spot lives: it has to be pulled out of the general free-spot bucket permanently, not just skipped during assignment, or a regular driver could still claim it on a busy day.\n\nConcretely, at reservation time I'd remove that spot from `Level`'s free-spot queue and track it separately in a `reservedSpots` map keyed by subscriber. The walk-in assignment strategy never sees it. At the gate, a subscriber's plate or badge routes them straight to their spot instead of through `SpotAssignmentStrategy` at all.\n\nI'd flag the edge case up front: what happens if the subscriber doesn't show up for a day — does the spot sit empty, or can it be released back to general parking for that day only? That trade-off, guaranteed spot vs utilization, is usually what the interviewer probes next.\n\n**Testing:** whether you can extend the core model with a new entity without breaking the invariant that a reserved spot is genuinely unavailable to everyone else.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Dedicated spots for subscribers"
    },
    {
      "id": "parking-lot-iq3",
      "question": "Your free-spot index is sharded per level to avoid lock contention — how do you handle a request for 'any available spot, any level' without scanning every shard's lock?",
      "answerMarkdown": "I'd keep a lock-free, or very cheaply locked, aggregate counter per level-and-type bucket, an atomic integer tracking approximately how many spots of this type are free on this level, updated whenever a spot is reserved or released. A \"give me any spot\" request first reads these counters, no locks needed for a read of an atomic value, to pick a level that looks like it has availability, and only then takes the actual per-level lock to do the real atomic pop from that level's free-spot index.\n\nThis means the expensive, lock-held operation, the actual reservation, only happens once, against the one level you've already guessed has space, instead of taking every level's lock in sequence to find one that does. The counter can occasionally be stale, another agent grabs the last spot on that level between your read and your reservation attempt, so the reservation itself still needs to handle \"level looked free but wasn't\" by falling back to the next-best level rather than failing outright, the same time-of-check-to-time-of-use pattern as the single-spot race, one level up.\n\nA concrete number: with 10 levels each holding their own lock, this avoids acquiring up to 10 locks per booking attempt in the worst case, dropping it to effectively 1 to 2 in the common case where the first guessed level actually has space.\n**Testing:** whether you can extend a single-resource concurrency pattern to a multi-shard system without regressing to a global lock.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Sharded concurrency"
    }
  ]
}
