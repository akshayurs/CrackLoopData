{
  "topicId": "issue-tracker-system",
  "interviewQuestions": [
    {
      "id": "issue-tracker-system-iq1",
      "question": "How would you add SLA timers or automatic status transitions on inactivity?",
      "answerMarkdown": "I'd frame this as an extension that shouldn't touch `Issue` or `Workflow`'s core transition logic at all — both are just triggering a normal, already-legal transition programmatically instead of via a user action.\n\nFor SLA timers, I'd attach an `slaDeadline` to specific statuses within a `Workflow` (e.g. \"In Review\" must resolve within 24 hours) and run a scheduled job that scans issues past their deadline, escalating — reassigning, notifying, or flagging — via the same `Workflow.transition()` call a human action would use, just invoked by the scheduler instead of a click.\n\nFor inactivity-based auto-transitions (\"move to Stale after 30 days untouched\"), I'd add a `lastActivityAt` timestamp on `Issue`, updated on any comment or status change, and have the same kind of scheduled job call `canTransition` before acting, so it can never attempt a transition the workflow doesn't actually allow.\n\nThe key point either way: the automation is just another *caller* of the existing transition API, not new transition logic.\n\n**Testing:** whether you extend a system by reusing its existing entry points instead of building a parallel path around them.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "SLA timers and automatic transitions"
    },
    {
      "id": "issue-tracker-system-iq2",
      "question": "How do you model permissions — who can transition, assign, or comment on an issue — without an if/else per role scattered through the codebase?",
      "answerMarkdown": "I'd draw the parallel to workflow-as-data directly: hardcoded `if (user.role == ADMIN)` checks have the same rigidity problem as a hardcoded status enum, just for permissions instead of state.\n\nMy fix is a `Permission` interface with one method, `canPerform(user, action, issue)`, and a separate implementation per rule — `CanTransitionCheck`, `CanAssignCheck`, `CanCommentCheck` — each encapsulating its own logic (assignee-or-admin, project-member-only, etc). The `Issue` service calls the appropriate check through the interface and never branches on a specific role name itself.\n\nI'd mention composition as the payoff: a rule like \"assignee OR reviewer OR admin can move to Done\" becomes combining existing checks with an OR, not writing a new nested if/else. Adding a new role later means writing one new check class, not touching every place a permission is enforced.\n\n**Testing:** whether you generalize a pattern (data-driven configurability) you've already applied once, to a second problem in the same system.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Pluggable permission checks"
    }
  ]
}
