{
  "topicId": "iterator-pattern",
  "interviewQuestions": [
    {
      "id": "iterator-pattern-iq1",
      "question": "How would you implement an iterator for a binary tree that yields values in sorted (in-order) sequence?",
      "answerMarkdown": "I'd implement a class with `hasNext()`/`next()` backed by an explicit stack rather than plain recursion, since recursion doesn't naturally pause between yields. The constructor pushes the leftmost path from the root onto the stack. Each `next()` pops the top node (the next smallest unvisited value), then pushes the leftmost path starting from that node's right child, so the next smallest value is always ready on top of the stack.\n\nI'd walk through why this beats collecting all values into an array first and returning an index-based iterator over that: the stack approach does true lazy, incremental traversal — it never holds more than the current path's worth of nodes in memory, which matters for very large or unbalanced trees.\n\nI'd also mention it generalizes: swap how you push nodes (pre-order, post-order, or BFS with a queue instead of a stack) and the same `hasNext()`/`next()` shape still works.\n\n**Testing:** whether you can implement Iterator's contract correctly, not just describe it, and reason about memory characteristics.",
      "mostAsked": true,
      "level": "beginner",
      "subTitle": "Implementing an in-order tree iterator"
    },
    {
      "id": "iterator-pattern-iq2",
      "question": "What's the difference between external and internal iterators, and can you give a real example of each?",
      "answerMarkdown": "External iterators put the caller in control of the loop — you explicitly call `hasNext()`/`next()` yourself, as with Java's `Iterator` or a hand-rolled `while` loop. Internal iterators put the collection in control — you pass a callback, and the collection calls it for you, as with JavaScript's `array.forEach(fn)` or `array.map(fn)`.\n\nI'd point out the practical trade-off: internal iterators are more concise for simple 'do this to every element' cases, but external iterators let you pause, break out early, or advance two collections in lockstep — things that are awkward or impossible with a bare callback-based forEach.\n\nI'd close with when I'd pick which: forEach/map for straightforward transforms, an explicit iterator when I need early exit or to interleave logic between elements.\n\n**Testing:** whether you know both styles exist and can pick the right one for a given control-flow need.",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "External vs internal iterators"
    }
  ]
}
