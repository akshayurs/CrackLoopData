{
  "topicId": "mediator-and-memento",
  "interviewQuestions": [
    {
      "id": "mediator-and-memento-iq1",
      "question": "How would you design a chat room system where many users need to send messages to each other without every user object holding references to every other?",
      "answerMarkdown": "I'd use the Mediator pattern: introduce a `ChatRoom` that every `User` holds exactly one reference to, and route all communication through it — `user.send(message)` calls `chatRoom.send(message, this)`, and the `ChatRoom` decides how to deliver it to the right recipients. No `User` ever holds a direct reference to another `User`.\n\nI'd quantify the win: with n users fully meshed, you'd need on the order of n² direct connections and every user's code would need updating whenever the group changes; with a mediator, it's n connections total, and users are added or removed by only touching the mediator's registration list.\n\nI'd flag the risk up front: it's tempting to keep piling responsibilities (persistence, spam filtering, permissions) into the `ChatRoom` until it becomes a god object — I'd keep it focused on routing and delegate those concerns to separate collaborators it calls out to.\n\n**Testing:** whether you can quantify the coupling problem Mediator solves and preempt its most common failure mode.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Designing a decoupled chat system"
    },
    {
      "id": "mediator-and-memento-iq2",
      "question": "How does the Memento pattern implement undo without breaking encapsulation, and how is that different from just exposing getters and setters?",
      "answerMarkdown": "If I exposed every private field via getters/setters so an external history manager could read and later rewrite them, any code with access to those methods could corrupt the object's invariants — not just the undo system. Memento avoids that by having the object itself (the originator) produce an opaque snapshot object it alone knows how to meaningfully read back; the external caretaker (a history stack) can store, pass around, and discard mementos, but never inspect or modify what's inside one.\n\nConcretely, an `Editor` would expose `save(): EditorMemento` and `restore(m: EditorMemento)`, while `EditorMemento` itself has no public API a `History` class would ever call — `History` only pushes and pops them.\n\nI'd close by naming the trade-off: this can mean storing full or large snapshots, which is more memory-intensive than Command-based undo when actions have a cheap natural inverse.\n\n**Testing:** whether you understand why encapsulation matters here, not just that Memento 'stores state for undo.'",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Memento and encapsulation"
    }
  ]
}
