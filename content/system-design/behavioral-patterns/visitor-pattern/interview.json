{
  "topicId": "visitor-pattern",
  "interviewQuestions": [
    {
      "id": "visitor-pattern-iq1",
      "question": "When would you reach for the Visitor pattern instead of just adding a method to each class in a hierarchy?",
      "answerMarkdown": "I'd frame it as a question about which axis of the code changes more often: the set of classes in the hierarchy, or the set of operations performed over them. If new element types (new AST node kinds, new shape types) are rare but new operations (a new compiler pass, a new export format) show up regularly, Visitor is the right fit \u2014 each new operation is one new Visitor class, and none of the existing element classes are touched.\n\nI'd flip it deliberately: if new element types are what actually shows up often, and the operation set is stable, Visitor is the wrong tool \u2014 adding a type means updating every existing visitor implementation, which is exactly the churn you were trying to avoid.\n\nI'd also flag the encapsulation cost either way: visitors need to read an element's internals, so those internals end up effectively public regardless of which axis is stable.\n\n**Testing:** whether you evaluate the trade-off against actual change patterns instead of reaching for Visitor because it sounds advanced.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "Visitor vs per-class methods, when to choose"
    },
    {
      "id": "visitor-pattern-iq2",
      "question": "How would you implement a Visitor to add a new operation, like type-checking, to an existing AST without modifying the node classes?",
      "answerMarkdown": "I'd start from the existing `accept(visitor)` method every node class already has (assuming Visitor was set up from the start) \u2014 each node's `accept()` calls the visitor's type-matched method, like `visitBinaryOp(this)`. To add type-checking, I write one new class, `TypeCheckVisitor`, implementing the `Visitor` interface's methods \u2014 `visitNumber()` returns a numeric type, `visitBinaryOp()` recursively type-checks both operands and validates the operator applies to their types.\n\nI'd emphasize what does NOT change: `NumberNode`, `BinaryOpNode`, and every other node class stay exactly as they are \u2014 I never touch them, and existing visitors like `PrintVisitor` are unaffected.\n\nI'd close with the one thing that could break this: if the AST later needs a new node type, `Visitor`'s interface needs a new method, and every existing visitor implementation (including this new `TypeCheckVisitor`) needs updating to handle it.\n\n**Testing:** whether you can actually implement double dispatch correctly, not just describe the pattern's intent.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Adding an operation via Visitor"
    },
    {
      "id": "visitor-pattern-iq3",
      "question": "In plain terms, what problem is double dispatch solving that a single method call can't?",
      "answerMarkdown": "A normal method call resolves based on one object's runtime type \u2014 `shape.draw()` picks the right `draw()` based on whether `shape` is a `Circle` or `Square`. Visitor needs to resolve based on *two* types at once: which kind of element it is (`Circle` vs `Square`) and which kind of operation is being applied (`AreaCalculator` vs `Renderer`) \u2014 and a language with single dispatch (Java, C#, most mainstream OO languages) can only pick a method based on one of those at a time.\n\nDouble dispatch fakes the second dispatch with two method calls instead of one: `element.accept(visitor)` first dispatches on the element's real type (so a `Circle`'s `accept` runs, not a generic one), and inside that `accept`, it calls `visitor.visit(this)` \u2014 now `this` is statically typed as `Circle`, so overload resolution picks `visit(Circle)` specifically, not a generic `visit(Shape)`. Two single-dispatch calls, chained, together resolve on both types.\n\nWithout this trick, adding a new operation (say, serialization) across an entire hierarchy means either an `instanceof` chain checking every possible type inside one method, or adding a new method to every single element class \u2014 Visitor's whole point is letting you add the new operation as one new visitor class instead.\n\n**Testing:** whether you understand double dispatch as solving a real limitation of single-dispatch languages, not just reciting the accept/visit mechanics.",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "Why double dispatch, in plain terms"
    }
  ]
}
