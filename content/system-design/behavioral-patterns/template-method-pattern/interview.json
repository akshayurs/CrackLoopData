{
  "topicId": "template-method-pattern",
  "interviewQuestions": [
    {
      "id": "template-method-pattern-iq1",
      "question": "How would you eliminate duplicated boilerplate across several classes that all follow the same overall algorithm but differ in a couple of steps?",
      "answerMarkdown": "I'd reach for Template Method: pull the shared sequence into a base class as one concrete method \u2014 the template method \u2014 that calls out to a fixed set of steps in a fixed order. Steps that are truly identical across every case (say, reading a file, saving to a database) live in the base class once; steps that genuinely differ (parsing, validation) become abstract methods each subclass must implement.\n\nI'd walk through the concrete payoff: a bug fix to the shared save logic now happens in exactly one place, and every subclass automatically follows the same sequence because it never controls the sequence \u2014 it only fills in specific hooks. That's the Hollywood Principle: the base class calls the subclass, not the other way around.\n\nI'd also flag the limit up front: this only works while the steps genuinely fit one fixed skeleton \u2014 a case that needs a fundamentally different sequence shouldn't be forced into the same base class.\n\n**Testing:** whether you reach for the right pattern for 'shared skeleton, varying steps' instead of generic inheritance advice.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Removing duplicated algorithm boilerplate"
    },
    {
      "id": "template-method-pattern-iq2",
      "question": "What's the difference between Template Method and Strategy, and when would you pick one over the other?",
      "answerMarkdown": "Both let you vary part of an algorithm, but the mechanism and granularity differ. Template Method uses inheritance: a base class fixes the overall sequence, and subclasses override individual steps \u2014 the algorithm's shape never changes, only specific pieces of it. Strategy uses composition: the entire algorithm is swapped as one object at runtime, and a single object instance can change its full behavior by having a different Strategy plugged in.\n\nI'd pick Template Method when the sequence itself is genuinely fixed and shared, and I want the guarantee that every implementation follows it correctly without being able to skip a step. I'd pick Strategy when I need runtime flexibility \u2014 the same object needs to switch its entire algorithm based on a setting or user choice, which inheritance-based Template Method can't do without creating a new object.\n\n**Testing:** whether you can distinguish 'vary a step via inheritance' from 'vary the whole algorithm via composition' with a concrete scenario for each.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Template Method vs Strategy, when to choose"
    },
    {
      "id": "template-method-pattern-iq3",
      "question": "When does Template Method's inheritance-based design become a liability at scale, and how would you migrate to Strategy without a big-bang rewrite?",
      "answerMarkdown": "Template Method ties every variant to a subclass of one base class, so the moment you need a variant that mixes steps from two different existing subclasses \u2014 a data importer that needs `CsvImporter`'s parse step but `JsonImporter`'s validation step \u2014 inheritance has no clean way to express that; you either duplicate code or contort the class hierarchy. This is the classic \"favor composition over inheritance\" pressure point, and it gets worse as the number of variant combinations grows, since subclass count can explode combinatorially.\n\nMigrating to Strategy without a rewrite: I'd do it one seam at a time. First, extract each overridden step (`parse()`, `validate()`) into its own standalone class implementing a small interface, while the base class still calls them \u2014 this doesn't change behavior yet, just isolates the varying pieces. Then flip the base class's algorithm from calling its own overridden methods to calling injected strategy objects passed into its constructor, and have each existing subclass become a simple factory that wires up the right combination of strategies. Existing callers that construct `CsvImporter` still work unchanged; only the internals moved from inheritance to composition.\n\nI'd do this only once the combinatorial pain is real, not preemptively \u2014 Template Method is genuinely simpler when there's truly one axis of variation.\n\n**Testing:** whether you have an actual incremental migration path, not just \"rewrite it with Strategy.\"",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Migrating Template Method to Strategy incrementally"
    }
  ]
}
