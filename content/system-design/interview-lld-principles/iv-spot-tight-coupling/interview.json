{
  "topicId": "iv-spot-tight-coupling",
  "interviewQuestions": [
    {
      "id": "iv-spot-tight-coupling-iq1",
      "question": "How do you spot tight coupling in a design?",
      "answerMarkdown": "I'd skip the textbook definition and go straight to concrete signals, since that's what's actually being tested. First: direct instantiation — a class `new`-ing a concrete collaborator inside its own constructor instead of receiving it, which welds it to that implementation at compile time. Second: shared data formats — business logic reaching directly into a raw external JSON shape, so any upstream schema change breaks it. Third: temporal coupling — an undocumented assumption that module A only works correctly if module B ran first.\n\nI'd ground it with an example: an `OrderService` that constructs `PostgresOrderRepository()` directly inside its constructor can't be tested or swapped to a different backend without editing that constructor — the fix is accepting the repository through the constructor instead.\n\nI'd also mention measuring it, not just eyeballing it: fan-out per class, and how many files a typical change touches in `git log`.\n\n**Testing:** whether you can name concrete, checkable signals instead of just defining the term.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Naming concrete coupling signals"
    },
    {
      "id": "iv-spot-tight-coupling-iq2",
      "question": "Is zero coupling between modules actually the goal you're designing toward?",
      "answerMarkdown": "No, and I'd flag that explicitly rather than let the question's phrasing push me toward \"eliminate every dependency.\" Modules that depend on nothing can't collaborate at all — some coupling is the entire point of composing a system out of parts. Chasing literal zero coupling produces absurd designs, like passing every value as a raw primitive with no shared types.\n\nThe real target is coupling to **abstractions** rather than concrete implementations, and coupling that's explicit — a documented interface — rather than implicit, like an undocumented assumption about call order between two components. An `OrderService` depending on a `Repository` interface is coupling, and that's fine; the same service depending on `PostgresOrderRepository` directly is the version worth fixing.\n\n**Testing:** whether you understand coupling as a spectrum to manage rather than a purity contest to win.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Coupling to abstractions vs eliminating it"
    },
    {
      "id": "iv-spot-tight-coupling-iq3",
      "question": "You spot tight coupling between two modules owned by different teams — what's the actual first step, given you can't unilaterally refactor someone else's code?",
      "answerMarkdown": "The first step isn't code, it's making the coupling visible and agreed-upon, because refactoring someone else's module without buy-in creates exactly the cross-team friction that caused the tight coupling in the first place. I'd write up the concrete symptom, \"ModuleA reaches into ModuleB's internal `_cache` field directly here, here, and here\" with file and line references, and share it with the owning team rather than silently fixing it in a surprise PR.\n\nThen I'd propose the smallest interface that removes the reach-through: instead of ModuleA touching ModuleB's internals, ModuleB exposes one public method that does exactly what ModuleA needs, `getActiveSessionCount()` instead of direct access to the cache, and I'd offer to write that method myself against their review, since it's a small, additive, low-risk change from their side. This keeps the fix scoped and collaborative instead of a unilateral rewrite of code I don't own.\n\nA concrete case: a billing module was reading a pricing module's in-memory rate-limit counter directly to decide whether to apply a discount, which meant any internal refactor of that counter's storage broke billing silently. The fix was a one-line public method `pricing.isRateLimited(userId)` that the pricing team added in a single small PR, after which the direct field access was deleted from billing's side.\n**Testing:** whether you can execute a cross-team fix diplomatically and incrementally, not just correctly identify the coupling.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Cross-team fix"
    }
  ]
}
