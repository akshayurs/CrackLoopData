{
  "topicId": "iv-dependency-inversion-in-practice",
  "interviewQuestions": [
    {
      "id": "iv-dependency-inversion-in-practice-iq1",
      "question": "What does dependency inversion look like in practice?",
      "answerMarkdown": "The \"in practice\" qualifier means I should show a refactor, not recite the definition. I'd start from `UserService` constructing `MySqlRepository()` directly inside its own constructor — the high-level module depending on a concrete, low-level detail, which means swapping databases or testing `UserService` without a real MySQL both require editing this class.\n\nThe fix: define a `Repository` interface shaped around what `UserService` actually needs, have `MySqlRepository` implement it, and change `UserService` to accept a `Repository` through its constructor instead of building one. Neither class constructs the other now — a small composition root, usually near the app's entry point, builds `MySqlRepository()` and hands it into `UserService`.\n\nI'd close by naming the concrete payoff: at test time, I pass a fake `Repository` instead of a real database, and `UserService` becomes unit-testable with zero setup.\n\n**Testing:** whether you can turn the abstract principle into an actual before/after refactor with the wiring in the right place.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "The before/after DIP refactor"
    },
    {
      "id": "iv-dependency-inversion-in-practice-iq2",
      "question": "What's the difference between dependency inversion and dependency injection?",
      "answerMarkdown": "I'd draw the distinction explicitly rather than treat them as synonyms, since conflating them is a common and telling mistake. DIP is about **direction** — which way the dependency arrow points; the high-level module should depend on an abstraction it owns the shape of, not a concrete low-level detail. DI is about **delivery mechanism** — how a class actually receives its dependency, whether through a constructor, a setter, or a framework doing it automatically.\n\nThe two aren't the same thing: you can have DI without DIP — injecting a concrete `MySqlRepository` through a constructor still violates DIP if the class depends on that concrete type rather than an abstraction. And you can have DIP without any DI framework at all — a single hand-written `build_app()` function that constructs and wires everything is DIP fully applied, with no Spring or Guice involved.\n\n**Testing:** whether you actually understand these as two different concepts that happen to work well together, rather than interchangeable jargon.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "DIP vs DI, precisely"
    }
  ]
}
