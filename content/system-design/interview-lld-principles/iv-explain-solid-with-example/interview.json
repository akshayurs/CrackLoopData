{
  "topicId": "iv-explain-solid-with-example",
  "interviewQuestions": [
    {
      "id": "iv-explain-solid-with-example-iq1",
      "question": "Can you explain SOLID with a real example?",
      "answerMarkdown": "I'd pick one small domain and thread it through all five letters rather than giving five disconnected definitions. I'd use a `NotificationSender` that emails users on events: it starts as one class doing formatting, sending, and logging — an SRP violation, so I split it into a formatter, a client, and a logger.\n\nFrom there, adding a new channel meant editing a switch statement — an OCP violation, so I extract a `Channel` interface and make each channel its own class. Any `Channel` subclass has to honor the `send()` contract without surprising callers — that's LSP. The interface itself stays to one method, `send`, so nobody implementing it is forced to support methods they don't need — ISP. And finally, `NotificationSender` depends on the abstract `Channel`, not a concrete `EmailChannel` — DIP — so swapping in Slack or SMS later needs zero edits to the sender.\n\nI'd close by naming which letter unlocked the rest: SRP first, since the split gave OCP and DIP somewhere sensible to attach.\n\n**Testing:** whether you understand SOLID as a coherent design story rather than five memorized definitions.",
      "mostAsked": true,
      "level": "beginner",
      "subTitle": "The threaded SOLID example"
    },
    {
      "id": "iv-explain-solid-with-example-iq2",
      "question": "Which SOLID principle would you reach for first when refactoring a messy class, and why?",
      "answerMarkdown": "I'd reach for SRP first, almost always. It's the cheapest principle to apply — it just requires naming the distinct reasons a class might change and splitting along those lines — and it's a precondition for the others to matter. OCP needs a sensible extension point to attach to, and DIP needs a meaningful abstraction to depend on; both are hard to place cleanly inside a tangled god-class that's still doing three unrelated jobs.\n\nConcretely: if I tried to add a `Channel` abstraction (DIP) to a `NotificationSender` that still mixed formatting, sending, and logging in one method, the abstraction would end up wrapping all three concerns at once, which defeats the point. Splitting first gives each piece a clean seam to abstract later, if it ever needs one.\n\n**Testing:** whether you see a dependency order between the five principles instead of treating them as an unordered checklist.",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "Which letter to apply first"
    },
    {
      "id": "iv-explain-solid-with-example-iq3",
      "question": "If you had to break one SOLID principle to ship this feature under a tight deadline, which one would you sacrifice first, and why?",
      "answerMarkdown": "I'd sacrifice Open/Closed before Single Responsibility or Liskov. OCP violations, editing an existing class instead of extending it, are the cheapest to fix later: you can retrofit a strategy or plugin point once the deadline pressure is off, since the fix is additive and localized to one class. SRP and LSP violations compound faster: a class already doing two jobs under deadline pressure tends to grow a third and fourth responsibility over following sprints because nobody schedules time to split it, and an LSP violation, a subclass silently changing a contract, can cause bugs in code nobody's looking at yet.\n\nConcretely: if I'm adding a new discount type to an `OrderPricer` and the clean move is a new `Strategy` implementation, but under deadline pressure I instead add an `if (discountType == 'BLACK_FRIDAY')` branch directly into the existing pricing method, that's an OCP violation, but it's contained, obvious in a diff, and trivial for the next person to lift into a proper strategy once there's time.\n\nWhat I wouldn't do is let that same time pressure push me into cramming pricing logic and inventory-decrement logic into the same method, that SRP violation is much harder to unwind once other code starts depending on the combined behavior.\n**Testing:** whether you can prioritize technical debt by how expensive it is to repay later, not just avoid all debt equally.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Prioritization"
    }
  ]
}
