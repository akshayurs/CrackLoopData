{
  "topicId": "iv-does-inheritance-violate-encapsulation",
  "interviewQuestions": [
    {
      "id": "iv-does-inheritance-violate-encapsulation-iq1",
      "question": "Is inheritance a violation of encapsulation?",
      "answerMarkdown": "Yes, partially — this is the fragile base class problem, and I'd name it precisely rather than wave at the general idea. A subclass's real dependency surface isn't just the base class's declared contract; it's that contract plus every implementation detail the subclass happens to rely on, like which method calls which internally, even though only the contract was ever documented as stable.\n\nConcretely: a `CountingCollection` subclass overrides `add()` to increment a counter, relying on the observed fact that `Collection.add_all()` calls `self.add()` internally. Nothing documents that call order as a promise — it's just how the base class happens to be implemented today. If a later \"optimization\" makes `add_all` append directly without calling `add`, `CountingCollection`'s count silently goes wrong with no test catching it.\n\nThe fix, when you want inheritance, is making the extension points explicit: mark methods not meant to be overridden as final, and expose a documented hook — like a template method — for what genuinely is meant to vary. That turns an implementation accident into a real, frozen contract.\n\n**Testing:** whether you know the specific mechanism behind \"inheritance breaks encapsulation\" rather than repeating the phrase as received wisdom.",
      "mostAsked": true,
      "level": "advanced",
      "subTitle": "The fragile base class problem"
    },
    {
      "id": "iv-does-inheritance-violate-encapsulation-iq2",
      "question": "Does using composition instead of inheritance fully solve this problem?",
      "answerMarkdown": "Mostly, but not entirely, and I'd be precise about why. Composition only exposes the composed object's public contract — there's no mechanism for the containing class to accidentally depend on the composed object's internal call order the way a subclass can through overriding. If `CountingCollection` held a `Collection` instance and called its public `add` method itself once per item, it would never be exposed to whatever `add_all` happens to do internally.\n\nThe \"mostly\" matters: composition can still leak if the composed object's *public* contract is underspecified — say, if a caller starts depending on the order it happens to iterate items, which was never promised either. The leak just moves from \"undocumented internals no outsider should see\" to \"underdocumented public behavior\" — a smaller, but not zero, version of the same risk.\n\nSo I wouldn't claim composition eliminates the class of problem entirely — it removes the specific inheritance-shaped mechanism, and shifts the remaining risk onto how carefully the public contract itself is specified.\n\n**Testing:** whether you understand composition's actual guarantee precisely, instead of treating it as a magic fix for every encapsulation concern.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Composition's real guarantee"
    },
    {
      "id": "iv-does-inheritance-violate-encapsulation-iq3",
      "question": "You've decided composition is safer here — how do you explain that trade-off to a teammate who just wants to inherit and move on?",
      "answerMarkdown": "I'd frame it around one question: does the subclass need to change what the base class does, or just reuse what it does? If it's pure reuse, wanting a `Stack` with the same storage as a `List`, composition wins immediately: wrap a `List` inside `Stack` and expose only `push`/`pop`/`peek`. Inheriting from `List` leaks every list method, like `insert` or `remove` at arbitrary indices, that breaks the stack invariant, so callers can corrupt state the class was supposed to protect.\n\nI'd show the concrete cost: with inheritance, a change to the base class's internal loop in `addAll()` silently changes subclass behavior even though nobody touched the subclass file, the fragile base class problem, invisible until it breaks in production during an unrelated refactor. With composition, the base class can change freely because the subclass only depends on the small interface it explicitly calls.\n\nThe pushback I'd expect is \"composition means more boilerplate, I have to write forwarding methods.\" That's true and worth naming honestly; for a two-method wrapper it's a few extra lines, not a redesign. I'd rather write four forwarding methods once than debug a fragile-base-class bug three sprints later when someone touches the parent for an unrelated reason.\n**Testing:** whether you can translate an abstract principle into a concrete, teachable argument a peer will actually accept, not just cite \"favor composition over inheritance\" as dogma.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Trade-offs"
    }
  ]
}
