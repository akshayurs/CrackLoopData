{
  "topicId": "iv-reducing-coupling-between-modules",
  "interviewQuestions": [
    {
      "id": "iv-reducing-coupling-between-modules-iq1",
      "question": "How do you reduce coupling between two modules?",
      "answerMarkdown": "I wouldn't just say \"decouple it\" — I'd name the coupling type first, since the fix depends on which kind it is. If `InventoryService` reaches directly into `PricingModule`'s internal cache, that's data coupling to a private detail — the fix is a narrow `PriceLookup` interface, shaped around exactly what `InventoryService` needs, that `PricingModule` implements. If one module passes a flag that changes the other's internal branching, that's control coupling — the fix is letting the callee own its own logic and receive only intent. If one module assumes the other already ran, with no enforced order, that's temporal coupling — the fix is an explicit contract or an event to wait on.\n\nI'd also check the level first: two classes in one process need less machinery than two services across a network. For same-process code, a plain interface usually suffices; for reactive coordination where both sides need to react to state changes, I'd reach for events instead of a direct call.\n\n**Testing:** whether you have an actual toolkit mapped to coupling types, not just a reflex to add an interface everywhere.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Naming the coupling type, then the fix"
    },
    {
      "id": "iv-reducing-coupling-between-modules-iq2",
      "question": "How do you know when you've decoupled two modules enough — is there a point of diminishing returns?",
      "answerMarkdown": "Yes, and I'd say so directly rather than imply more decoupling is always better. Every interface, event, or extra layer of indirection costs someone reading the code time to trace through — once a module depends only on a narrow, stable abstraction and no longer breaks when the other module's internals change, further decoupling adds ceremony without reducing any real risk.\n\nMy gut check: can anyone on the team name a concrete, plausible future change that would break the current design? If yes, there's still real coupling worth addressing. If nobody can point to one, I'd stop there rather than keep adding seams speculatively — which connects back to the YAGNI trade-off: a seam is only worth its cost if it's addressing a risk that's actually present.\n\nI'd also flag the shared-database anti-pattern here: sometimes what looks like \"more decoupling\" — routing everything through a generic intermediary — actually just hides coupling instead of removing it, which is worse than doing nothing.\n\n**Testing:** whether you treat decoupling as a cost/benefit trade-off rather than a virtue to maximize indefinitely.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Diminishing returns on decoupling"
    },
    {
      "id": "iv-reducing-coupling-between-modules-iq3",
      "question": "Two teams own the two modules on either side of the interface you introduced — how do you keep the contract from silently drifting once you're not in the room?",
      "answerMarkdown": "An interface without an enforced contract is just a suggestion that drifts the moment two teams stop talking daily, so I'd back it with something machine-checked, not documentation alone. Concretely: consumer-driven contract tests, Pact-style, where the consuming team writes assertions against the exact shape and values they depend on, and the providing team's CI runs those assertions against every change before it merges, a breaking change fails the provider's own build, not a downstream team's production deploy weeks later.\n\nI'd also version the interface explicitly rather than mutating it in place: adding a field is safe, removing or changing the meaning of one is a breaking change that gets a new version, with both versions supported for a deprecation window. That turns \"did this silently break someone\" into an explicit, scheduled conversation instead of an incident.\n\nA real example: a `UserProfile` module changed `phoneNumber` from a plain string to a structured object without bumping the contract version, three downstream consumers broke in production because they were parsing the old string shape, and nobody caught it because there was no contract test asserting the old shape, only integration tests that happened to use test data matching the new format by coincidence.\n**Testing:** whether you think about coupling as an ongoing organizational problem, not just a one-time class-design decision.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Cross-team contracts"
    }
  ]
}
