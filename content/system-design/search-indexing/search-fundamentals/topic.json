{
  "id": "search-fundamentals",
  "title": "Search Fundamentals",
  "area": "system-design",
  "group": "search-indexing",
  "order": 1,
  "slug": "search-fundamentals",
  "summary": "Why full-text search needs its own system instead of a SQL `LIKE` query — and what a real search system actually has to deliver.",
  "level": "beginner",
  "slideCount": 9,
  "estReadMinutes": 8,
  "slides": [
    {
      "id": "search-fundamentals-s01",
      "order": 1,
      "sectionTitle": "Snapshot",
      "subTitle": null,
      "type": "overview",
      "markdown": "Search feels like a solved problem until you try to build it on top of your existing database — then it falls over fast. **Full-text search** means finding documents by the words they contain, ranked by how relevant they are to a query, in milliseconds, even when the query is misspelled.\n\nThink of the difference between a library card catalog (exact title/author lookup) and a librarian who actually knows the books (\"something about a whale and obsession\" → *Moby Dick*). Search systems are built to be the librarian, not the catalog. This topic covers why that requires a dedicated system.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 32,
      "mcqIds": []
    },
    {
      "id": "search-fundamentals-s02",
      "order": 2,
      "sectionTitle": "Why LIKE '%term%' doesn't scale for text search",
      "subTitle": null,
      "type": "concept",
      "markdown": "A SQL `LIKE '%term%'` query has to scan every row and check every string for a substring match — there's no index that helps, because the term could start anywhere inside the text. On a table of a few thousand rows this is fine; on millions of documents it means a full table scan per query, and the query gets slower as the table grows.\n\nEven where it *doesn't* time out, `LIKE` only tells you whether a term is present — not whether the match is a good one. A search for \"jaguar\" returns the same result whether the word appears once in a footnote or ten times in the title, and it has zero concept of \"guitar\" being a near-miss for \"guitars.\"\n\nThe fix isn't a better query — it's a different data structure, one that maps terms directly to the documents containing them instead of scanning text on every request.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 48,
      "mcqIds": [
        "search-fundamentals-s02-q1"
      ]
    },
    {
      "id": "search-fundamentals-s03",
      "order": 3,
      "sectionTitle": "What \"search\" actually requires: relevance, typo tolerance, speed",
      "subTitle": null,
      "type": "concept",
      "markdown": "A real search system has to solve three problems a `LIKE` query never touches. **Relevance** — of the documents that match, which ones matter most, ranked so the best result is first, not just first-inserted. **Typo tolerance** — \"resturant\" should still find \"restaurant\"; users misspell constantly and a search box that requires exact spelling feels broken. **Speed** — results in well under 100ms, even across billions of documents, because search is usually in the critical path of a page load.\n\nEach of these gets its own machinery later in this group: an inverted index for fast lookup, ranking algorithms like BM25 for relevance, and fuzzy matching / edit-distance techniques for typos. None of them come from a general-purpose database for free.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 46,
      "mcqIds": []
    },
    {
      "id": "search-fundamentals-s04",
      "order": 4,
      "sectionTitle": "Diagram: a search request from query box to ranked results",
      "subTitle": null,
      "type": "diagram",
      "markdown": "![Query pipeline: parsing, index lookup, ranking, results](assets/search-pipeline.svg)\n\nA query doesn't go straight from the search box to matching documents. It's first **analyzed** the same way documents were when indexed (lowercased, tokenized, stemmed), then used to look up **posting lists** in the inverted index, and finally the matching documents are **scored and ranked** before anything is returned.\n\nThe same analysis step matters on both sides: if documents were indexed as stemmed lowercase tokens but the query isn't processed the same way, exact matches quietly stop working.",
      "assets": [{"alt": "Query pipeline: parsing, index lookup, ranking, results", "path": "assets/search-pipeline.svg"}],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 40,
      "mcqIds": []
    },
    {
      "id": "search-fundamentals-s05",
      "order": 5,
      "sectionTitle": "Structured queries vs full-text search: different problems",
      "subTitle": null,
      "type": "compare",
      "markdown": "It helps to be precise about which problem you're actually solving — they need different tools.\n\n| | Structured query | Full-text search |\n|---|---|---|\n| Example | `WHERE user_id = 42` | \"best noise cancelling headphones under $200\" |\n| Match type | Exact (or range) | Fuzzy, partial, ranked |\n| Index used | B-tree on the column | Inverted index on tokenized terms |\n| Result ordering | By query clause (`ORDER BY`) | By computed relevance score |\n| Right tool | Relational database | Search engine (Elasticsearch, Solr, etc.) |\n\n**How to decide:** if the query has a knowable exact predicate (an ID, a status, a date range), a database index is faster and simpler. If the query is \"find things about X, best matches first,\" reach for a search system.",
      "assets": [],
      "hasCode": false,
      "hasTable": true,
      "estReadSeconds": 42,
      "mcqIds": [
        "search-fundamentals-s05-q1"
      ]
    },
    {
      "id": "search-fundamentals-s06",
      "order": 6,
      "sectionTitle": "Where search fits in a system architecture",
      "subTitle": null,
      "type": "concept",
      "markdown": "Search almost always sits **beside** the primary database, not inside it. The database (Postgres, MySQL, DynamoDB) stays the source of truth for writes; a dedicated search engine holds a denormalized, indexed copy of whatever fields need to be searchable, kept in sync by a pipeline (covered in a later topic on search index freshness).\n\nThis split exists because the two systems optimize for opposite things: the database optimizes for consistent, transactional writes on normalized data; the search engine optimizes for fast, ranked reads on documents built for query-time scoring. Trying to make one system do both well is why bolted-on search struggles at scale.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 44,
      "mcqIds": []
    },
    {
      "id": "search-fundamentals-s07",
      "order": 7,
      "sectionTitle": "Pitfall: bolting search onto the primary OLTP database at scale",
      "subTitle": null,
      "type": "pitfall",
      "markdown": "**Wrong belief:** \"We already have Postgres — let's just add a few `LIKE` queries and full-text indexes and call it search.\"\n\n**Why it's wrong:** Postgres's built-in full-text search can genuinely work at small-to-medium scale, but it shares resources with your transactional workload — an expensive search query can slow down the checkout flow running on the same database. It also lacks the ranking sophistication (BM25 variants, boosting, typo tolerance) a dedicated engine gives you out of the box, so you end up rebuilding those features yourself.\n\n**Correct model:** treat search as a separate concern early — even a lightweight dedicated engine (Elasticsearch, OpenSearch, Meilisearch) isolates search load from transactional load and gives you ranking primitives for free, instead of accumulating custom SQL that never quite matches what a search engine does natively.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 46,
      "mcqIds": [
        "search-fundamentals-s07-q1"
      ]
    },
    {
      "id": "search-fundamentals-s08",
      "order": 8,
      "sectionTitle": "Precision vs recall: the two ways a search system can be wrong",
      "subTitle": null,
      "type": "concept",
      "markdown": "**Precision** asks: of the results returned, how many are actually relevant? **Recall** asks: of all the relevant documents that exist, how many did the search actually surface? A search system can fail at either independently, and improving one often costs the other.\n\nA search that only returns exact phrase matches has high precision (everything it returns is genuinely relevant) but low recall (it misses documents that are relevant but phrased differently). A search that fuzzy-matches aggressively, stemming heavily and tolerating typos broadly, raises recall but risks precision, since some of those loosely-matched results won't actually be what the user wanted.\n\nMost production tuning is explicitly a precision/recall trade-off: a legal-document search usually leans toward precision (missing a document is dangerous, but so is drowning a lawyer in irrelevant ones), while an e-commerce search usually leans toward recall (showing an imperfect but plausible product beats showing nothing).",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 55,
      "mcqIds": [
        "search-fundamentals-s08-q1"
      ]
    },
    {
      "id": "search-fundamentals-s09",
      "order": 9,
      "sectionTitle": "Faceted search: filtering results by structured attributes alongside free text",
      "subTitle": null,
      "type": "concept",
      "markdown": "Most real search boxes aren't pure free text — an e-commerce search for \"running shoes\" is usually paired with filters for brand, size, and price range, and a sidebar showing how many results exist for each filter value. That combination, free-text relevance plus structured attribute filtering with live counts, is called **faceted search**.\n\nMechanically, this means the search engine's index stores both the free-text fields (tokenized, analyzed, used for relevance scoring) and structured fields (brand, price, size) that support fast exact-match filtering and aggregation, in the same document. A query becomes \"match this text AND brand = Nike AND price between $50-$100,\" then a separate aggregation pass computes how many matching documents exist for each other filter value, so the UI can show \"Nike (340), Adidas (210)\" live.\n\nThis is why a search engine's document schema usually mixes both structured and unstructured fields rather than being purely free-text — the free-text half needs analysis (tokenizing, stemming) while the facet half explicitly should not be analyzed, so exact filter values match precisely.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 58,
      "mcqIds": []
    }
  ]
}
