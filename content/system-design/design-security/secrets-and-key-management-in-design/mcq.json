{
  "topicId": "secrets-and-key-management-in-design",
  "mcqs": [
    {
      "id": "secrets-and-key-management-in-design-s02-q1",
      "slideId": "secrets-and-key-management-in-design-s02",
      "question": "What is the main risk of committing a database password into a config file inside a Git repository?",
      "options": [
        "The credential now lives everywhere that repository or its history goes — every clone, every CI log, indefinitely",
        "Git repositories cannot store text files containing passwords at all",
        "It will cause the application to fail to start",
        "Config files are always slower to load than a secrets manager call"
      ],
      "correctIndex": 0,
      "explanation": "Once a credential is committed, it persists in every clone and in the repo's history even after a later commit removes it — that permanence is the real danger, not a technical limitation of Git or a performance issue. It won't stop the app from starting.",
      "difficulty": "easy",
      "level": "advanced"
    },
    {
      "id": "secrets-and-key-management-in-design-s04-q1",
      "slideId": "secrets-and-key-management-in-design-s04",
      "question": "What is the main advantage of a service fetching its secret from a vault at boot, compared to baking it into the container image?",
      "options": [
        "The service starts up faster in every case",
        "It requires no authentication mechanism between the service and the vault",
        "Rotating the secret only requires updating it in the vault — no image rebuild or redeploy needed",
        "It removes the need for the secret to be encrypted at all"
      ],
      "correctIndex": 2,
      "explanation": "Fetching at boot decouples the secret's lifecycle from the deployment lifecycle — rotate it once in the vault and every future boot picks up the new value. It typically requires the service to authenticate with its own identity, doesn't inherently start faster, and doesn't remove the need for encryption of the secret itself.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "secrets-and-key-management-in-design-s05-q1",
      "slideId": "secrets-and-key-management-in-design-s05",
      "question": "A system encrypts all data in transit with TLS everywhere but stores its database on an unencrypted disk. What threat is it still exposed to?",
      "options": [
        "A network attacker intercepting traffic between services",
        "Someone gaining access to a stolen or improperly disposed physical disk or backup",
        "A man-in-the-middle attack during the TLS handshake",
        "Nothing — TLS everywhere is sufficient protection for all data"
      ],
      "correctIndex": 1,
      "explanation": "Encryption at rest and in transit protect against different threats — TLS everywhere stops network interception but does nothing for someone who gets physical or logical access to unencrypted storage media. Treating them as one solved problem, rather than two independent checkboxes, is exactly the gap this creates.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "secrets-and-key-management-in-design-s06-q1",
      "slideId": "secrets-and-key-management-in-design-s06",
      "question": "Why is one shared master key encrypting every tenant's data a design risk, compared to a key hierarchy with per-tenant data keys?",
      "options": [
        "Master keys are always weaker cryptographically than per-tenant keys",
        "Compromising the one shared key exposes every tenant's data at once, and revoking one tenant's access means re-encrypting everyone's data",
        "A single shared key is slower to use than multiple per-tenant keys",
        "Shared keys cannot be stored in a KMS"
      ],
      "correctIndex": 1,
      "explanation": "The real risk is blast radius: one shared key means one compromise or one needed revocation touches all tenants at once. A key hierarchy — a master key encrypting per-tenant data keys — bounds that damage to one tenant. It isn't about the cipher being weaker or the key being unable to live in a KMS.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "secrets-and-key-management-in-design-s07-q1",
      "slideId": "secrets-and-key-management-in-design-s07",
      "question": "What is the design purpose of rotating a signing key every 30-90 days instead of using one key indefinitely?",
      "options": [
        "It makes the key cryptographically stronger over time",
        "It removes the need for a KMS entirely",
        "It reduces how often the application code needs to be redeployed",
        "It bounds the blast radius of a leak to the key's lifetime, so an old leaked key stops being useful after rotation"
      ],
      "correctIndex": 3,
      "explanation": "Rotation doesn't strengthen the cryptography or remove the need for key management — it limits how long a leaked key remains valid, so damage is capped by the rotation window rather than lasting indefinitely. Application code that asks for \"the current key\" doesn't need redeploying to pick up rotation.",
      "difficulty": "medium",
      "level": "advanced"
    },
    {
      "id": "secrets-and-key-management-in-design-s08-q1",
      "slideId": "secrets-and-key-management-in-design-s08",
      "question": "Why does envelope encryption have a service encrypt bulk data locally with a data encryption key (DEK) instead of sending the data straight to the KMS?",
      "options": [
        "The KMS cannot perform symmetric encryption",
        "KMS APIs are typically rate-limited and slow for per-record calls, so only the small DEK is sent to the KMS while bulk data is encrypted locally",
        "Local encryption is required by law in most jurisdictions",
        "DEKs never need to be encrypted themselves"
      ],
      "correctIndex": 1,
      "explanation": "The KMS is reserved for wrapping/unwrapping a small DEK because calling it per-record would be slow and hit request-rate limits; bulk data is encrypted locally with fast symmetric crypto. The DEK itself is still encrypted (by the KMS's master key) before being stored alongside the ciphertext.",
      "difficulty": "hard",
      "level": "advanced"
    },
    {
      "id": "secrets-and-key-management-in-design-s09-q1",
      "slideId": "secrets-and-key-management-in-design-s09",
      "question": "A service fetches secrets from a vault at runtime and never commits them to source control. Why might a secret still leak?",
      "options": [
        "Vault-fetched secrets can never actually leak once fetched at runtime",
        "The vault itself must be misconfigured for any leak to happen",
        "A CI job, crash handler, or verbose logger can print the secret into a log store that retains it long after the fact",
        "Runtime-fetched secrets are automatically rotated and cannot leak"
      ],
      "correctIndex": 2,
      "explanation": "Using a vault protects the secret at rest and in source control, but says nothing about what a consuming system does with it afterward — logging pipelines, CI debug output, and crash reports are a real leak path outside the vault's control, independent of whether the vault itself is misconfigured or the secret is rotated.",
      "difficulty": "medium",
      "level": "advanced"
    }
  ]
}
