{
  "topicId": "snake-and-ladder",
  "interviewQuestions": [
    {
      "id": "snake-and-ladder-iq1",
      "question": "How would you add a 'You've Been Framed'-style power-card variant to a live game without a rewrite?",
      "answerMarkdown": "I'd point out first that this is exactly the test the design was built to pass: `Game.playTurn()` never knows about board content today, so a new rule shouldn't need it to start.\n\nConcretely, I'd introduce a **pluggable `TurnEffect` chain** consulted right after a roll resolves — a list of effects (draw-a-card, skip-next-player, swap-positions) that each get a chance to modify the outcome before `Game` commits the new position. A power card is just a new `TurnEffect` implementation registered into that chain; the loop still calls the same three steps it always did (roll, resolve, apply) and doesn't grow a new branch.\n\nI'd flag one real design tension: if a power card can change *whose* turn is next (a skip card), the effect needs a way to signal that back to `Game`, which is a slightly wider interface than a plain position modifier — worth naming explicitly rather than hand-waving.\n\n**Testing:** whether you extend through composition (a new effect implementation) instead of proposing to edit the core loop for every new card.",
      "mostAsked": true,
      "level": "beginner",
      "subTitle": "Adding a power-card variant"
    },
    {
      "id": "snake-and-ladder-iq2",
      "question": "How would you replay a completed game for a UI animation?",
      "answerMarkdown": "I'd replay the **recorded move list** against a freshly-constructed board rather than trying to store intermediate render/animation state during the actual game.\n\nConcretely: `Game` (or a lightweight recorder wrapping it) logs each turn's outcome — player, dice roll, raw position, final position — as it happens. To animate later, the UI walks that list in order against a new `Board` instance, feeding each recorded step to the renderer at whatever pace it wants (fast-forward, slow-motion, pause). The game logic itself is untouched by this; replay is a pure read of history.\n\nI'd contrast this with the tempting alternative — storing animation frames or intermediate visual state inline during gameplay — which couples game logic to a specific UI and breaks the moment you want a second rendering (say, a text log instead of an animation).\n\n**Testing:** whether you separate the source of truth (the move log) from its presentation, rather than baking rendering concerns into the game model.",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "Replaying a game for animation"
    }
  ]
}
