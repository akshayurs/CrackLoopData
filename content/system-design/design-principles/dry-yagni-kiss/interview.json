{
  "topicId": "dry-yagni-kiss",
  "interviewQuestions": [
    {
      "id": "dry-yagni-kiss-iq1",
      "question": "Can you tell me about a time you deliberately duplicated code instead of extracting a shared abstraction?",
      "answerMarkdown": "I'd pick a specific, real example rather than a general statement about believing in duplication sometimes. Structure: describe the code that looked duplicated, explain why it wasn't really the same rule, and describe the cost avoided.\n\nFor instance: \"Our checkout discount and loyalty-program discount both multiplied a price by a percentage, so an earlier version merged them into one `applyDiscount` helper. When checkout needed a minimum-order threshold and loyalty needed a discount cap, the shared function grew two sets of flags that only one caller each used — the code became harder to read than the duplication it replaced. I split it back into `applyCheckoutDiscount` and `applyLoyaltyDiscount`. A few lines are duplicated, but each function now says exactly what it does and can evolve independently.\"\n\nI'd close by naming the principle tension directly: DRY pushed toward merging, but the two blocks represented different knowledge, so YAGNI and KISS were the better guides here.\n\n**Testing:** whether you treat DRY as a judgment call about knowledge, not a rule to apply to any two similar-looking blocks.",
      "mostAsked": true,
      "level": "beginner",
      "subTitle": "Deliberate duplication, with a reason"
    },
    {
      "id": "dry-yagni-kiss-iq2",
      "question": "How do you decide when applying DRY, YAGNI, or KISS would actually conflict with each other on a real piece of code?",
      "answerMarkdown": "I'd say the conflict usually shows up as: DRY wants to extract an abstraction, but YAGNI questions whether the abstraction is justified yet, and KISS questions whether it's worth the added complexity right now. My default order is to start with the simplest correct version for today's actual requirement (KISS), tolerate a bit of duplication rather than guess at a shared abstraction (YAGNI holding DRY back), and only extract the shared piece once I've seen the same real rule repeat — often on the third occurrence, not the second.\n\nI'd give a concrete trigger for reversing that default: if I'm confident two blocks encode the exact same business rule — not just similar-looking code — I'll extract immediately, since letting confirmed duplication sit around is real technical debt, not caution.\n\nI'd close by noting these principles are heuristics, not laws — the actual judgment call is whether the abstraction reflects genuine shared knowledge or just a coincidence.\n\n**Testing:** whether you can navigate these principles as trade-offs against each other, rather than reciting all three as always-good rules.",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "Resolving conflicts between the three"
    }
  ]
}
