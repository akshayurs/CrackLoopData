{
  "topicId": "ocp-open-closed",
  "interviewQuestions": [
    {
      "id": "ocp-open-closed-iq1",
      "question": "How would you design a payment system so adding a new payment method doesn't require touching existing code?",
      "answerMarkdown": "I'd start from an abstraction the whole checkout flow depends on, like a `PaymentProcessor` interface with a `charge(amount)` method. `CreditCardProcessor` and `PayPalProcessor` each implement it independently, and the checkout code calls `processor.charge()` without knowing which implementation it's talking to.\n\nAdding Apple Pay later means writing a new `ApplePayProcessor` class and registering it — zero edits to the checkout flow, the interface, or the other processors. I'd contrast this with the naive version: a single `processPayment()` method with an `if/else` per provider, where every new method risks the branches that already worked.\n\nI'd also flag the boundary judgment call: I wouldn't add this abstraction for a system with exactly one payment method and no roadmap for a second — that's speculative generality, not OCP.\n\n**Testing:** whether you can turn OCP into a concrete interface design, and whether you know when *not* to apply it.",
      "mostAsked": true,
      "level": "beginner",
      "subTitle": "Extensible payment processing"
    },
    {
      "id": "ocp-open-closed-iq2",
      "question": "What's the difference between the Open/Closed Principle and just writing extensible code?",
      "answerMarkdown": "I'd say OCP is a specific, testable version of \"extensible\": it's not just that new features are *possible*, it's that adding them requires zero edits to already-shipped, already-tested files. \"Extensible\" can quietly mean \"I can add a branch to this switch statement,\" which is still a modification and still re-risks existing paths.\n\nI'd give the concrete test: look at the git diff for the last feature addition. If it only added new files and didn't change existing ones, OCP held. If it touched a shared method's internals, it didn't — even if the change was \"easy.\"\n\nI'd close by noting OCP requires an abstraction to extend through (polymorphism), which is why it's tied to interface design, not just good intentions.\n\n**Testing:** whether you understand OCP as a concrete constraint (no edits to existing code) rather than a vague synonym for good design.",
      "mostAsked": false,
      "level": "beginner",
      "subTitle": "OCP vs vague extensibility"
    }
  ]
}
