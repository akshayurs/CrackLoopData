{
  "topicId": "design-by-contract-and-invariants",
  "interviewQuestions": [
    {
      "id": "design-by-contract-and-invariants-iq1",
      "question": "How would you make it impossible to call this method incorrectly?",
      "answerMarkdown": "I'd start by naming the contract explicitly: what does this method require from the caller (preconditions), and what does it promise back (postconditions)? For a `withdraw(amount)` method, the precondition is roughly `amount > 0 && amount <= balance`.\n\nThen I'd pick an enforcement level based on cost and risk. Cheapest: a runtime check that throws a clear, specific exception the moment the precondition is violated, instead of letting a vague downstream error surface later. Stronger: encode the constraint in the type system \u2014 a `PositiveAmount` value type that simply can't be constructed with a negative number, so the invalid state is unrepresentable rather than merely checked. I'd reserve documentation-only for genuinely low-risk internal contracts, since undocumented-but-unchecked constraints silently rot as the code changes hands.\n\nI'd close by naming the invariant angle too: if the object has a standing rule (balance never negative), I'd make sure it's checked at the end of every public method, not just this one.\n\n**Testing:** whether you can turn a vague \"make it robust\" ask into concrete precondition/postcondition/invariant enforcement choices.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Making a method impossible to misuse"
    },
    {
      "id": "design-by-contract-and-invariants-iq2",
      "question": "What's the difference between encapsulation and Design by Contract?",
      "answerMarkdown": "I'd distinguish them by what each one protects. Encapsulation protects an object's internal *state* \u2014 it hides fields behind methods so nothing outside can reach in and corrupt them directly. Design by Contract is about what those methods *promise* \u2014 their preconditions, postconditions, and invariants \u2014 regardless of whether the state behind them is hidden or not.\n\nI'd give the concrete gap: a class can be perfectly encapsulated \u2014 every field private, everything accessed through methods \u2014 and still leave its contract unclear. Does `withdraw()` throw on insufficient funds, silently clamp the balance to zero, or return a boolean success flag? Encapsulation alone doesn't answer that; it only guarantees callers can't bypass the method to find out by inspecting fields directly.\n\nI'd close by saying good API design needs both: encapsulation controls access, DbC controls expectations, and a class with only one of the two still leaves callers guessing about behavior even if the data is safe.\n\n**Testing:** whether you understand these as complementary, not interchangeable, concepts.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Encapsulation vs Design by Contract"
    },
    {
      "id": "design-by-contract-and-invariants-iq3",
      "question": "How would you enforce contracts across service boundaries in a microservices architecture, not just inside one class?",
      "answerMarkdown": "Inside one process, a contract violation throws an exception you can catch immediately. Across a network boundary, the caller and the implementation can be deployed independently, versioned independently, and fail independently \u2014 so the contract has to be explicit and checked at the boundary itself, not assumed.\n\nConcretely, I'd push contracts into the schema: an OpenAPI or protobuf definition with required fields and value constraints acts as the precondition, validated by a gateway or the service itself before business logic runs \u2014 reject with a 4xx immediately rather than let a malformed request corrupt downstream state. For postconditions and invariants that span multiple services (an order's total must equal the sum of its line items, even though pricing and inventory are separate services), I'd rely on contract tests between consumer and provider \u2014 Pact-style \u2014 run in CI, so a producer can't ship a change that silently breaks an implicit invariant a consumer depends on.\n\nThe key difference from single-process DbC: you can't rely on a shared in-memory assertion; you need versioned schemas, contract tests, and validation at every network edge, because the caller might be running last month's code.\n\n**Testing:** whether you can extend a single-class idea (contracts) to a distributed system where trust can't be assumed.",
      "mostAsked": false,
      "level": "advanced",
      "subTitle": "Contracts across service boundaries"
    }
  ]
}
