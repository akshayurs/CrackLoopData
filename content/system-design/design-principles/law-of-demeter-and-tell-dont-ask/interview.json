{
  "topicId": "law-of-demeter-and-tell-dont-ask",
  "interviewQuestions": [
    {
      "id": "law-of-demeter-and-tell-dont-ask-iq1",
      "question": "Your code review flags a long method chain — what's the actual risk, and how do you fix it?",
      "answerMarkdown": "I'd first explain the risk concretely, not just call it \"ugly.\" A chain like `order.getCustomer().getAddress().getCountry().getTaxRate()` means this one line depends on the internal structure of four separate objects — if any one of them changes shape (Customer stops having an Address, Country gets renamed to TaxJurisdiction), this line breaks, and likely so does every other place with the same chain.\n\nFor the fix, I'd push the traversal behind a method on the object closest to the caller — `Order.getTaxRate()` — that privately delegates through its own collaborators. The caller now only knows one method on one object it directly holds; `Order` is still allowed to know its own `Customer`.\n\nI'd add the caveat that I wouldn't blindly wrap every chain — if it's a builder or a fluent stream API designed for chaining, that's a different, acceptable pattern, not a Demeter violation.\n\n**Testing:** whether you can name the concrete coupling risk instead of a vague style objection, and whether you know where to draw the line.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Fixing a long method chain"
    },
    {
      "id": "law-of-demeter-and-tell-dont-ask-iq2",
      "question": "What's the difference between the Law of Demeter and Tell, Don't Ask, and how do they work together?",
      "answerMarkdown": "I'd frame them as two sides of the same coupling concern: Demeter is about *navigation* — which objects you're allowed to reach through to get something — while Tell, Don't Ask is about *behavior* — whether you pull data out to decide with it yourself, or ask the object to make the decision internally.\n\nA concrete example ties them together: `if (account.getBalance() < amount) throw ...` both asks for data instead of telling the object what to do (Tell/Ask violation) and, if `account` were itself a stranger reached through another object, would also be a Demeter violation. Fixing it with `account.withdraw(amount)` — a method the account itself defines and enforces — resolves both at once: you're no longer reaching into an object's data, and you're telling it what you want done.\n\nI'd close by noting they usually get violated together in practice: chains that reach deep into objects are often reaching that far specifically *to ask* for data to act on elsewhere.\n\n**Testing:** whether you see these as complementary facets of \"reduce what a caller needs to know,\" not two unrelated rules.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "How the two principles connect"
    }
  ]
}
