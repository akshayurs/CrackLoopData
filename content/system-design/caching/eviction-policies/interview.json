{
  "topicId": "eviction-policies",
  "interviewQuestions": [
    {
      "id": "eviction-policies-iq1",
      "question": "Implement an LRU cache — what follow-ups should you expect?",
      "answerMarkdown": "I'd start by naming the two operations that need to be O(1): `get` and `put`, and the structure that gets both — a hashmap from key to node, paired with a doubly linked list ordered by recency. I'd sketch the invariant out loud: the head is most-recently-used, the tail is least-recently-used, and every access moves a node to the head.\n\nI'd walk through `get`: look up the node via the hashmap, unlink it, reattach it at the head, return its value. Then `put`: if the key exists, update and move to front; otherwise insert at the front, and if we're now over capacity, remove the tail node and delete it from the hashmap too.\n\nCommon follow-ups I'd be ready for: \"make it thread-safe\" (a lock around the critical section, or sharding the cache), \"what if it needs to be distributed across machines\" (that's a different problem — consistent hashing plus a shared or per-shard LRU), and \"how would Redis do this at scale\" (approximated LRU via sampling, not exact tracking).\n\n**Testing:** whether you can implement the exact-LRU data structure cleanly and also reason about what changes once it has to run at scale.",
      "mostAsked": true,
      "level": "intermediate",
      "subTitle": "Implementing LRU and its follow-ups"
    },
    {
      "id": "eviction-policies-iq2",
      "question": "When would you choose LFU over LRU, and what does LFU cost you?",
      "answerMarkdown": "I'd choose LFU when I expect a stable set of consistently popular items that shouldn't be evicted just because of a short burst of unrelated traffic — a \"trending topics\" cache where a handful of items stay hot for weeks is a good example; plain LRU could flush one of them out during a brief spike of one-off requests.\n\nThe cost: LFU is slower to adapt when popularity genuinely shifts, since an old, no-longer-popular item can hold a high frequency count from its earlier heyday and resist eviction longer than it should. Real implementations often add aging or decay to frequency counts to mitigate this.\n\nI'd also flag that LFU needs more bookkeeping per key (a running count, not just a position in a list), which is part of why LRU remains the more common default.\n\n**Testing:** whether you understand LFU as a genuine trade-off, not a strictly-better version of LRU.",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "LFU vs LRU trade-off"
    }
  ]
}
