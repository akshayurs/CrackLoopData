{
  "topicId": "cache-penetration-and-negative-caching",
  "interviewQuestions": [
    {
      "id": "cache-penetration-and-negative-caching-iq1",
      "question": "Your cache hit rate looks fine but the DB is still getting hammered \u2014 why?",
      "answerMarkdown": "I'd point out that a healthy-looking hit rate can hide a separate problem: requests for keys that were never cacheable because they don't exist. Hit rate is usually computed over keys that at some point existed in the cache \u2014 repeated lookups for nonexistent IDs (cache penetration) miss every time and never show up as the kind of miss the metric was designed to catch.\n\nI'd check the database's query logs for lookups that consistently return \"not found,\" especially concentrated on a narrow, randomized range that looks probed rather than organic \u2014 that's the signature of penetration.\n\nThe fix I'd propose is negative caching: cache the \"not found\" result itself with a short TTL, so repeated requests for the same missing key get absorbed at the cache layer instead of reaching the database every time. If the traffic looks adversarial and high-volume, I'd add a Bloom filter in front to reject clearly-invalid keys even earlier.\n\n**Testing:** whether you know hit rate is a necessary but not sufficient signal, and can reason about traffic a naive hit-rate dashboard doesn't capture.",
      "mostAsked": true,
      "level": "expert",
      "subTitle": "Diagnosing cache penetration despite a healthy hit rate"
    },
    {
      "id": "cache-penetration-and-negative-caching-iq2",
      "question": "How would you defend a hot cache against a stampede caused by many keys expiring at the same synchronized moment?",
      "answerMarkdown": "I'd lay out a layered answer, since no single technique fully covers this. First, jittered TTLs: add a small random offset to each entry's expiry so a batch of keys written together doesn't all expire in the same instant. Second, request coalescing: if multiple requests do miss on the same key simultaneously, only let one of them recompute the value while the rest wait and share the result.\n\nFor the highest-traffic keys specifically, I'd add probabilistic early expiration (XFetch-style) \u2014 as a key's TTL nears its end, give each request a small, rising chance of triggering an early background refresh, so the cache tends to refresh itself just ahead of the crowd instead of everyone hitting the wall at once.\n\nI'd close by naming which of these I'd apply broadly versus selectively: jitter is cheap and worth doing everywhere; coalescing and early expiration are worth the extra complexity specifically for the small set of keys that are hot enough to matter.\n\n**Testing:** whether you can combine multiple mitigations appropriately instead of reaching for just one and calling it solved.",
      "mostAsked": false,
      "level": "expert",
      "subTitle": "Layered defenses against synchronized expiry"
    },
    {
      "id": "cache-penetration-and-negative-caching-iq3",
      "question": "What's the difference between a cache miss and cache penetration, and why does the distinction matter operationally?",
      "answerMarkdown": "A normal cache miss is for a key that exists in the source of truth but isn't currently cached \u2014 the app fetches it once, populates the cache, and every subsequent request for that key is a hit. Cache penetration is repeated lookups for a key that doesn't exist in the database at all, so there's never anything to cache: every single request for that key skips the cache and hits the database, forever, no matter how many times it's asked.\n\nThe operational difference is what \"scale\" does to each. A normal miss rate settles down as the cache warms up \u2014 it's a one-time cost per key. Penetration traffic doesn't settle; if an attacker or a buggy client hammers `/user/999999999` (a nonexistent ID) at high volume, every request is a full database round-trip, and the cache does nothing to absorb it, because the cache was never designed to represent \"this doesn't exist.\"\n\nThat's why negative caching and Bloom filters exist as distinct tools from ordinary caching: they specifically make \"not found\" itself a cacheable, checkable fact, closing the exact gap normal cache-aside leaves open.\n\n**Testing:** whether you can tell these apart by mechanism, not just by symptom (both look like \"DB load is high\").",
      "mostAsked": false,
      "level": "intermediate",
      "subTitle": "Cache miss vs cache penetration"
    }
  ]
}
