{
  "id": "load-balancing-fundamentals",
  "title": "Load Balancing Fundamentals",
  "area": "system-design",
  "group": "load-balancing",
  "order": 1,
  "slug": "load-balancing-fundamentals",
  "summary": "What a load balancer does, where it sits in the request path, and the L4-vs-L7 distinction that decides what it can see and act on.",
  "level": "beginner",
  "slideCount": 9,
  "estReadMinutes": 7,
  "slides": [
    {
      "id": "load-balancing-fundamentals-s01",
      "order": 1,
      "sectionTitle": "Snapshot",
      "subTitle": null,
      "type": "overview",
      "markdown": "A **load balancer** sits between clients and a fleet of backend servers, spreading requests across them so no single machine takes the whole load — and so the fleet keeps serving even when one node dies. Any service with more than one instance needs one.\n\nThink of it as a maitre d' directing customers to open tables: send everyone to the same table and it drowns while the rest sit empty. This topic covers what a load balancer actually does, the L4-vs-L7 split, and the redundancy trap of running just one.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 25,
      "mcqIds": []
    },
    {
      "id": "load-balancing-fundamentals-s02",
      "order": 2,
      "sectionTitle": "What a load balancer actually does",
      "subTitle": null,
      "type": "concept",
      "markdown": "A load balancer sits between clients and a pool of backend servers and decides, request by request, which server handles it. Without one, every client would need to know every server's address, and one overloaded or dead server would take down whichever clients happened to be pointed at it.\n\nConcretely: three app servers each comfortably handle ~1,000 req/s. Point all traffic at one directly and the whole system caps at 1,000 req/s; put a load balancer in front and the same three servers serve ~3,000 req/s combined, and losing one still leaves ~2,000 req/s of capacity.\n\nThe load balancer is also the first place failure gets handled — it can stop sending traffic to a server the moment that server looks unhealthy.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 45,
      "mcqIds": [
        "load-balancing-fundamentals-s02-q1"
      ]
    },
    {
      "id": "load-balancing-fundamentals-s03",
      "order": 3,
      "sectionTitle": "Diagram: where the load balancer sits — client, LB, app tier",
      "subTitle": null,
      "type": "diagram",
      "markdown": "![Client requests routed through load balancer to app servers](assets/request-path.svg)\n\nThe load balancer sits directly in the request path, not off to the side: every request from the client hits the load balancer first, and only then reaches whichever app server it picks. The client never talks to an app server directly — it only ever knows the load balancer's address, often a single DNS name or a virtual IP shared by a small cluster of load balancers for their own redundancy.\n\nThat single entry point is what lets you add, remove, or replace app servers without the client ever noticing — the load balancer is the only thing that has to know the current server list.",
      "assets": [
        {
          "alt": "Client requests routed through load balancer to app servers",
          "path": "assets/request-path.svg"
        }
      ],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 40,
      "mcqIds": []
    },
    {
      "id": "load-balancing-fundamentals-s04",
      "order": 4,
      "sectionTitle": "L4 vs L7 load balancing: what each layer can see and do",
      "subTitle": null,
      "type": "compare",
      "markdown": "A **Layer 4** load balancer routes based on IP address and TCP/UDP port alone — it never looks inside the packet's payload. A **Layer 7** load balancer terminates the connection and reads the actual HTTP request — path, headers, host, cookies, even the body — before deciding where it goes.\n\n| | L4 | L7 |\n|---|---|---|\n| Sees | IP + port | Full HTTP request (path, headers, host) |\n| Routes on | Which port opened the connection | URL path, header value, cookie |\n| Overhead | Lower — just forwards packets | Higher — terminates TLS, parses HTTP |\n| Examples | AWS NLB, raw TCP/LVS | AWS ALB, NGINX, Envoy, HAProxy (HTTP mode) |\n\n**How to decide:** if you need to route `/api/*` to one fleet and `/static/*` to another, or read a cookie for sticky sessions, you need L7 — an L4 balancer physically cannot see that far into the request.",
      "assets": [],
      "hasCode": false,
      "hasTable": true,
      "estReadSeconds": 50,
      "mcqIds": [
        "load-balancing-fundamentals-s04-q1"
      ]
    },
    {
      "id": "load-balancing-fundamentals-s05",
      "order": 5,
      "sectionTitle": "Hardware vs software vs cloud-managed load balancers",
      "subTitle": null,
      "type": "concept",
      "markdown": "Load balancers come in three flavors that trade cost, control, and operational burden. **Hardware appliances** (F5 BIG-IP is the classic example) are dedicated boxes — extremely fast, expensive, and you own patching and capacity planning. **Software load balancers** (NGINX, HAProxy, Envoy) run as ordinary processes on commodity servers or containers — cheaper and flexible, but you still operate them yourself.\n\n**Cloud-managed load balancers** (AWS ALB/NLB, Google Cloud Load Balancing) are a managed service: you configure rules and the provider handles scaling, patching, and multi-zone availability.\n\nMost new systems default to cloud-managed for the app tier and reach for software LBs like Envoy when they need control cloud offerings don't expose — custom routing logic, service-mesh integration, or on-prem deployment.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 45,
      "mcqIds": []
    },
    {
      "id": "load-balancing-fundamentals-s06",
      "order": 6,
      "sectionTitle": "A single load balancer as a new single point of failure — and the fix",
      "subTitle": null,
      "type": "concept",
      "markdown": "Putting one load balancer in front of many servers fixes the app tier's single-point-of-failure problem — and quietly creates a new one at the load balancer itself. If that one box or process dies, every server behind it becomes unreachable even though all of them are healthy.\n\nThe fix is to make the load balancer tier redundant too: run at least two instances behind a mechanism that survives one dying — a floating virtual IP managed by keepalived/VRRP, DNS with health-checked failover, or simply a cloud provider's managed LB, which is already redundant across availability zones internally.\n\nThe pattern repeats at every layer of a system: whatever sits in front of a redundant tier must itself be made redundant, or you've moved the single point of failure, not removed it.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 42,
      "mcqIds": [
        "load-balancing-fundamentals-s06-q1"
      ]
    },
    {
      "id": "load-balancing-fundamentals-s07",
      "order": 7,
      "sectionTitle": "Pitfall: putting one load balancer in front without redundancy",
      "subTitle": null,
      "type": "pitfall",
      "markdown": "**Wrong belief:** \"I added a load balancer, so my app tier is now highly available.\"\n\n**Why it's wrong:** a lone load balancer instance is itself a single point of failure. You've made the app tier redundant while leaving an even more critical single box in front of it — if it fails, every healthy app server behind it becomes unreachable at once, which is worse than losing one app server ever was.\n\n**Correct model:** redundancy has to apply to every tier in the request path, including the load balancer itself. Run at least two LB instances with automatic failover — VRRP/keepalived, or a cloud provider's zone-redundant managed LB — never a single instance in production.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 40,
      "mcqIds": [
        "load-balancing-fundamentals-s07-q1"
      ]
    },
    {
      "id": "load-balancing-fundamentals-s08",
      "order": 8,
      "sectionTitle": "Routing algorithms: round robin, least connections, weighted",
      "subTitle": null,
      "type": "concept",
      "markdown": "Once a load balancer knows which servers are healthy, it still needs a rule for *which one* gets the next request. **Round robin** cycles through servers in order — simple, and fine when every server and every request costs roughly the same. **Least connections** sends the next request to whichever server currently has the fewest active connections — this matters when request duration varies a lot, since round robin can pile several slow, long-running requests onto one server while others sit idle.\n\n**Weighted** variants of either (weighted round robin, weighted least connections) assign each server a capacity number, so a beefier server gets proportionally more traffic than a smaller one in the same pool — useful during a rolling upgrade to a bigger instance type, or a heterogeneous fleet.\n\nThe practical default: round robin is fine until request cost varies noticeably or the fleet is heterogeneous — the first sign you've outgrown it is one server visibly running hotter than its siblings despite getting the same request count.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 56,
      "mcqIds": [
        "load-balancing-fundamentals-s08-q1"
      ]
    },
    {
      "id": "load-balancing-fundamentals-s09",
      "order": 9,
      "sectionTitle": "The staff-level nuance: where TLS terminates changes what the load balancer can see",
      "subTitle": null,
      "type": "concept",
      "markdown": "An L7 load balancer routing on HTTP headers or paths can only do that if it can actually read the request — which means it must **terminate TLS**: decrypt the incoming HTTPS connection, inspect the plaintext HTTP, then either forward it in plaintext to the backend or re-encrypt for a second TLS hop (**TLS re-encryption**, common when the backend network itself isn't fully trusted).\n\nThis is a real design decision, not a detail: terminating TLS at the load balancer and forwarding plaintext internally is simpler and faster, and fine inside a private VPC where the internal network is trusted. Re-encrypting all the way to the backend costs extra CPU and latency but is what compliance regimes (PCI-DSS, some healthcare requirements) often mandate — \"encrypted at rest and in transit, including internally\" doesn't stop at the edge.\n\nThe interview-ready framing: an L4 load balancer never has this decision to make, because it never sees inside the encrypted stream at all — L7 features and TLS termination are a package deal, and where you re-encrypt (or don't) is a compliance and threat-model question, not just a performance one.",
      "assets": [],
      "hasCode": false,
      "hasTable": false,
      "estReadSeconds": 58,
      "mcqIds": [
        "load-balancing-fundamentals-s09-q1"
      ]
    }
  ]
}
